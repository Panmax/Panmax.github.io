<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>贾攀家</title>
  
  <subtitle>Panmax&#39;s Blog</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://jpanj.com/"/>
  <updated>2019-03-13T01:45:53.313Z</updated>
  <id>https://jpanj.com/</id>
  
  <author>
    <name>Panmax</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>2019 Flags</title>
    <link href="https://jpanj.com/2019/flag-2019/"/>
    <id>https://jpanj.com/2019/flag-2019/</id>
    <published>2019-12-31T15:59:59.000Z</published>
    <updated>2019-03-13T01:45:53.313Z</updated>
    
    <content type="html"><![CDATA[<ul><li>至少阅读 3 本计算机相关的砖头书<ul><li>[ ] 深入理解计算机系统</li><li>[ ] 代码大全</li><li>[ ] 程序员修炼之道 - 从小工到专家（二刷）</li><li>[ ] UNINX 编程艺术</li><li>[ ] 算法</li><li>[ ] 计算机网络：自顶向下方法</li></ul><ul><li>参考：<a href="https://www.zhihu.com/question/273973062" target="_blank" rel="noopener">https://www.zhihu.com/question/273973062</a></li></ul></li><li>每月至少读一本非计算机类的书</li><li>熟练掌握 Go 语言，并用它完成一个大中型项目</li><li>阅读 &amp; 解析 Spring、Eureka 源码</li><li>学习 Team Leader</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;ul&gt;
&lt;li&gt;至少阅读 3 本计算机相关的砖头书&lt;ul&gt;
&lt;li&gt;[ ] 深入理解计算机系统&lt;/li&gt;
&lt;li&gt;[ ] 代码大全&lt;/li&gt;
&lt;li&gt;[ ] 程序员修炼之道 - 从小工到专家（二刷）&lt;/li&gt;
&lt;li&gt;[ ] UNINX 编程艺术&lt;/li&gt;
&lt;li&gt;[ ] 算法
      
    
    </summary>
    
    
      <category term="flag" scheme="https://jpanj.com/tags/flag/"/>
    
  </entry>
  
  <entry>
    <title>Java 单例模式完整指南</title>
    <link href="https://jpanj.com/2019/singleton-design-pattern-in-java/"/>
    <id>https://jpanj.com/2019/singleton-design-pattern-in-java/</id>
    <published>2019-12-17T03:27:12.000Z</published>
    <updated>2019-12-17T03:50:37.761Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>设计模式一直流行于程序员之间，本文讨论被许多人认为最简单但最有争议的设计模式 —— 单例模式</p></blockquote><p><img src="1.jpeg" alt></p><h2 id="设计模式概述"><a href="#设计模式概述" class="headerlink" title="设计模式概述"></a>设计模式概述</h2><p>在软件工程中，设计模式描述了如何解决重复出现的设计问题，以设计出灵活、可复用的面向对象的应用程序。设计模式一共有 23 种，可以将它们分为三个不同的类别 —— 创建型、结构型和行为型。</p><h3 id="创建型设计模式"><a href="#创建型设计模式" class="headerlink" title="创建型设计模式"></a>创建型设计模式</h3><p>创建型设计模式是处理对象创建机制的模式，试图以适合的方式创建对象。</p><p>对象创建的基本形式可能会导致设计问题或增加设计的复杂性。创建型设计模式通过某种方式控制对象的创建来解决此问题。</p><h3 id="结构型设计模式"><a href="#结构型设计模式" class="headerlink" title="结构型设计模式"></a>结构型设计模式</h3><p>结构型设计模式处理类和对象的组成。这类模式使我们将对象和类组装为更大的结构，同时保持结构的高效和灵活。</p><h3 id="行为型设计模式"><a href="#行为型设计模式" class="headerlink" title="行为型设计模式"></a>行为型设计模式</h3><p>行为型设计模式讨论对象的通信以及它们之间如何交互。</p><h2 id="单例设计模式"><a href="#单例设计模式" class="headerlink" title="单例设计模式"></a>单例设计模式</h2><p>我们对设计模式和其类型进行了概述，接下来我们重点介绍单例设计模式。</p><p>单例模式提供了控制程序中允许创建的实例数量的能力，同时确保程序中有一个单例的全局访问点。</p><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>对单个实例的访问控制</li><li>减少对象数量</li><li>允许完善操作和表示</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>被很多程序员视为反模式</li><li>在可能不需要单例的情况下被误用</li></ul><h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>单例设计模式可以通过多种方式实现。每一种都有其自身的优点和局限性，我们可以通过以下几种方式实现单例模式：</p><ul><li>预先初始化（Eager Initialization）</li><li>静态块预初始化</li><li>延迟初始化（Lazy Initialization）</li><li>线程安全的延迟初始化</li><li>双重检查的延迟初始化</li><li>单个实例的枚举</li></ul><h2 id="实现单例"><a href="#实现单例" class="headerlink" title="实现单例"></a>实现单例</h2><p>本节我们将讨论实现单例模式的各种方法。</p><h3 id="预先初始化（Eager-Initialization）"><a href="#预先初始化（Eager-Initialization）" class="headerlink" title="预先初始化（Eager Initialization）"></a>预先初始化（Eager Initialization）</h3><ul><li>用预先初始化方法，对象在创建之前就已被初始化</li><li>由于预先初始化，所以可能会出现程序并不需要的情况下初始化</li><li>如果单例类很简单并且不需要太多资源，这个方法会很有用。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">public class EagerInitialization &#123;</span><br><span class="line"></span><br><span class="line">    private static EagerInitialization INSTANCE = new EagerInitialization();</span><br><span class="line"></span><br><span class="line">    private EagerInitialization() &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static EagerInitialization getInstance() &#123;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="静态块预初始化"><a href="#静态块预初始化" class="headerlink" title="静态块预初始化"></a>静态块预初始化</h3><ul><li>在前面讨论的预先初始化方法中，没有提供任何异常处理逻辑</li><li>在此实现中，对象是在静态块中创建的，因此在对象初始化时可以进行异常处理</li><li>这种方法和预先初始化有同样的问题：即使程序可能不使用对象，对象也会被提前创建出来</li></ul><h3 id="延迟初始化（Lazy-Initialization）"><a href="#延迟初始化（Lazy-Initialization）" class="headerlink" title="延迟初始化（Lazy Initialization）"></a>延迟初始化（Lazy Initialization）</h3><ul><li>按需创建对象</li><li>与预先初始化不同，延迟初始化会在需要时创建对象</li><li>此实现<strong>不是线程安全的</strong></li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">import java.util.Objects;</span><br><span class="line"></span><br><span class="line">public class LazyInit &#123;</span><br><span class="line"></span><br><span class="line">    private static LazyInit INSTANCE = null;</span><br><span class="line"></span><br><span class="line">    private LazyInit() &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static LazyInit getInstance() &#123;</span><br><span class="line">        if (Objects.isNull(INSTANCE)) &#123;</span><br><span class="line">            synchronized (LazyInit.class) &#123;</span><br><span class="line">                INSTANCE = new LazyInit();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="线程安全的延迟初始化"><a href="#线程安全的延迟初始化" class="headerlink" title="线程安全的延迟初始化"></a>线程安全的延迟初始化</h3><ul><li>添加了用以处理多线程的同步方案</li><li>因为每次调用都需要进行方法级同步，而过度的同步会降低性能</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">import java.util.Objects;</span><br><span class="line"></span><br><span class="line">public class LazyInitialization &#123;</span><br><span class="line"></span><br><span class="line">    private static LazyInitialization INSTANCE = null;</span><br><span class="line"></span><br><span class="line">    private LazyInitialization() &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public synchronized static LazyInitialization getInstance() &#123;</span><br><span class="line">        if (Objects.isNull(INSTANCE)) &#123;</span><br><span class="line">            INSTANCE = new LazyInitialization();</span><br><span class="line">        &#125;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="双重检查的延迟初始化"><a href="#双重检查的延迟初始化" class="headerlink" title="双重检查的延迟初始化"></a>双重检查的延迟初始化</h3><ul><li>解决方法级同步的问题</li><li>为对象可为空性执行双重检查</li><li>尽管这种方法似乎可以完美的工作，但是在多线程场景下不太适用。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">import java.util.Objects;</span><br><span class="line"></span><br><span class="line">public class DoubleCheckSingleton &#123;</span><br><span class="line"></span><br><span class="line">    private static DoubleCheckSingleton INSTANCE;</span><br><span class="line"></span><br><span class="line">    private DoubleCheckSingleton()&#123;&#125;</span><br><span class="line"></span><br><span class="line">    public static DoubleCheckSingleton getInstance() &#123;</span><br><span class="line">        if(Objects.isNull(INSTANCE))&#123;</span><br><span class="line">            synchronized (DoubleCheckSingleton.class) &#123;</span><br><span class="line">                if(Objects.isNull(INSTANCE))&#123;</span><br><span class="line">                    INSTANCE = new DoubleCheckSingleton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="单个实例的枚举"><a href="#单个实例的枚举" class="headerlink" title="单个实例的枚举"></a>单个实例的枚举</h3><ul><li>使用 Java 枚举类型创建单例</li><li>此方法为处理反射、序列化和多线程场景提供了本地支持</li><li>枚举类型有些不灵活</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">public enum Singleton &#123;</span><br><span class="line">    INSTANCE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="保护单例"><a href="#保护单例" class="headerlink" title="保护单例"></a>保护单例</h2><h3 id="使单例不受反射访问的影响"><a href="#使单例不受反射访问的影响" class="headerlink" title="使单例不受反射访问的影响"></a>使单例不受反射访问的影响</h3><p>在所有的单例实现中（枚举方法除外），我们通过提供私有构造函数来确保单例。但是，可以通过<strong>反射</strong>来访问私有构造函数，反射是在运行时检查或修改类的运行时行为的过程。<br><br>让我们演示如何通过反射访问单例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">import java.lang.reflect.Constructor;</span><br><span class="line">import java.lang.reflect.InvocationTargetException;</span><br><span class="line"></span><br><span class="line">public class SingletonWithReflection &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line"></span><br><span class="line">        EagerInitializedSingleton firstSingletonInstance = EagerInitializedSingleton.getInstance();</span><br><span class="line">        EagerInitializedSingleton secondSingletonInstance = null;</span><br><span class="line"></span><br><span class="line">        try&#123;</span><br><span class="line">           Class&lt;EagerInitializedSingleton&gt; clazz = EagerInitializedSingleton.class;</span><br><span class="line">           Constructor&lt;EagerInitializedSingleton&gt; constructor = clazz.getDeclaredConstructor();</span><br><span class="line">           constructor.setAccessible(true);</span><br><span class="line">           secondSingletonInstance = constructor.newInstance();</span><br><span class="line">        &#125;</span><br><span class="line">        catch(NoSuchMethodException | InvocationTargetException | IllegalAccessException | InstantiationException e)&#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(&quot;Instance 1 hashcode: &quot;+firstSingletonInstance.hashCode());</span><br><span class="line">        System.out.println(&quot;Instance 2 hashcode: &quot;+secondSingletonInstance.hashCode());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上边代码输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Instance 1 hashcode: 21049288</span><br><span class="line">Instance 2 hashcode: 24354066</span><br></pre></td></tr></table></figure><h3 id="解决："><a href="#解决：" class="headerlink" title="解决："></a>解决：</h3><p>如果单例对象已经初始化，则可以通过禁止对构造函数的访问来防止通过反射访问单例类。如果在对象初始化之后调用构造函数，可以通过抛出异常的方式来实现。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">public final class EagerInitializedSingleton &#123;</span><br><span class="line"></span><br><span class="line">    private static final EagerInitializedSingleton INSTANCE = new EagerInitializedSingleton();</span><br><span class="line"></span><br><span class="line">    private EagerInitializedSingleton()&#123;</span><br><span class="line">        if(Objects.nonNull(INSTANCE))&#123;</span><br><span class="line">            throw new RuntimeException(&quot;This class can only be access through getInstance()&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static EagerInitializedSingleton getInstance()&#123;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="使单例在序列化时安全"><a href="#使单例在序列化时安全" class="headerlink" title="使单例在序列化时安全"></a>使单例在序列化时安全</h2><p>在分布式应用程序中，有时我们会序列化一个对象，以将对象状态保存在持久化存储中，并用以之后的检索。保存对象状态的过程称为<strong>序列化</strong>，而检索操作称为<strong>反序列化</strong>。</p><p>如果单例没有被正确实现，那么可能出现一个单例对象有两个实例的情况。</p><p>让我们看看如何出现这种情况：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line">import java.io.FileInputStream;</span><br><span class="line">import java.io.FileOutputStream;</span><br><span class="line">import java.io.ObjectInputStream;</span><br><span class="line">import java.io.ObjectOutputStream;</span><br><span class="line"></span><br><span class="line">public class SingletonWithSerialization &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line"></span><br><span class="line">        EagerInitializedSingleton firstSingletonInstance = EagerInitializedSingleton.getInstance();</span><br><span class="line">        EagerInitializedSingleton secondSingletonInstance = null;</span><br><span class="line"></span><br><span class="line">        ObjectOutputStream outputStream = null;</span><br><span class="line">        ObjectInputStream inputStream = null;</span><br><span class="line">        try&#123;</span><br><span class="line">            // 将对象状态保存到文件中</span><br><span class="line">            outputStream = new ObjectOutputStream(new FileOutputStream(&quot;FirstSingletonInstance.ser&quot;));</span><br><span class="line">            outputStream.writeObject(firstSingletonInstance);</span><br><span class="line">            outputStream.close();</span><br><span class="line"></span><br><span class="line">            // 从文件中检索对象状态</span><br><span class="line">            inputStream = new ObjectInputStream(new FileInputStream(&quot;FirstSingletonInstance.ser&quot;));</span><br><span class="line">            secondSingletonInstance = (EagerInitializedSingleton) inputStream.readObject();</span><br><span class="line">            inputStream.close();</span><br><span class="line">        &#125;</span><br><span class="line">        catch(Exception e)&#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(&quot;FirstSingletonInstance hashcode: &quot;+firstSingletonInstance.hashCode());</span><br><span class="line">        System.out.println(&quot;SecondSingletonInstance hashcode: &quot;+secondSingletonInstance.hashCode());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上代码输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">FirstSingletonInstance hashcode: 23090923</span><br><span class="line">SecondSingletonInstance hashcode: 19586392</span><br></pre></td></tr></table></figure><p>这说明现在有两个单例实例。</p><blockquote><p>注意，单例类必须实现 <code>Serializable</code> 接口才能序列化实例。</p></blockquote><p>为了避免序列化产生多个实例，我们可以在单例类中实现 <code>readResolve()</code> 方法。这个方法将会替换从流中读取的对象。实现代码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">import java.io.Serializable;</span><br><span class="line">import java.util.Objects;</span><br><span class="line"></span><br><span class="line">public class EagerInitializedSingleton implements Serializable &#123;</span><br><span class="line"></span><br><span class="line">    private static final long serialVersionUID = 1L;</span><br><span class="line"></span><br><span class="line">    private static final EagerInitializedSingleton INSTANCE = new EagerInitializedSingleton();</span><br><span class="line"></span><br><span class="line">    private EagerInitializedSingleton()&#123;</span><br><span class="line">        if(Objects.nonNull(INSTANCE))&#123;</span><br><span class="line">            throw new RuntimeException(&quot;This class can only be access through getInstance()&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static EagerInitializedSingleton getInstance()&#123;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    protected Object readResolve()&#123;</span><br><span class="line">        return getInstance();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再次执行 <code>SingletonWithSerialization</code> 输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">FirstSingletonInstance hashcode: 24336889</span><br><span class="line">SecondSingletonInstance hashcode: 24336889</span><br></pre></td></tr></table></figure><h2 id="Java-API-中的单例示例"><a href="#Java-API-中的单例示例" class="headerlink" title="Java API 中的单例示例"></a>Java API 中的单例示例</h2><p>Java API 中有很多类是用单例设计模式设计的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">java.lang.Runtime#getRuntime()</span><br><span class="line">java.awt.Desktop#getDesktop()</span><br><span class="line">java.lang.System#getSecurityManager()</span><br></pre></td></tr></table></figure><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>单例模式是最重要和最常用的设计模式之一。尽管很多人批评它是一种反模式，并且有很多实现时的注意事项，但在实际生活中有很多使用这种模式的示例。本文尝试介绍了常见的单例设计和与之相关的缺陷。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;设计模式一直流行于程序员之间，本文讨论被许多人认为最简单但最有争议的设计模式 —— 单例模式&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpeg&quot; alt&gt;&lt;/p&gt;
&lt;h2 id=&quot;设计模式概述&quot;&gt;&lt;a href=&quot;#设计模
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>HTTPie介绍——一个轻量级HTTP客户端</title>
    <link href="https://jpanj.com/2019/httpie-introduce/"/>
    <id>https://jpanj.com/2019/httpie-introduce/</id>
    <published>2019-12-17T02:28:42.000Z</published>
    <updated>2019-12-17T02:32:16.983Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>HTTPie 是一个用于与 HTTP 服务器进行交互的命令行客户端。</p></blockquote><p><img src="1.jpg" alt></p><h2 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h2><p>HTTPie（发音为 H-T-T-派）是一个基于命令行的 HTTP 客户端，可以提供更加人类友好的命令行交互，HTTPie 可用于测试、调试以及与 HTTP 服务器进行交互。</p><p>HTTPie 提供了一个 <code>http</code> 命令，这个命令可以使用简单自然的语法发送任意 HTTP 请求，并以精美的彩色输出作为响应结果。</p><p>在这篇文章中，我们将学习如何使用此工具访问 REST 服务。</p><h2 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h2><p>作为一个现代化命令行工具，HTTPie 提供了如下功能：</p><ul><li>简单、直观的 HTTP 命令语法</li><li>漂亮的格式化输出</li><li>天然的 JSON 支持</li><li>表单和文件上传</li><li>支持自定义 HTTP 头</li><li>主流操作系统支持 —— Linux、macOS、Windows</li><li>通过插件扩展额外功能</li></ul><p>在后边的文章中，你将看到这些功能的介绍。</p><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>可以通过多种方式来安装 HTTPie。</p><h3 id="macOS"><a href="#macOS" class="headerlink" title="macOS"></a>macOS</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install httpie</span><br></pre></td></tr></table></figure><h3 id="Linux（Ubuntu）"><a href="#Linux（Ubuntu）" class="headerlink" title="Linux（Ubuntu）"></a>Linux（Ubuntu）</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">apt-get install httpie</span><br></pre></td></tr></table></figure><h3 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pip install --upgrade pip setuptools</span><br><span class="line">pip install --upgrade httpie</span><br></pre></td></tr></table></figure><p>或者</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">easy_install httpie</span><br></pre></td></tr></table></figure><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>现在 HTTPie 已经安装在了本地电脑上，可以来调用各种 HTTP 接口。</p><p>后边的文章中，我将会使用下边三个网站来演示相关功能：</p><ul><li><strong><a href="http://example.org/" target="_blank" rel="noopener">http://example.org/</a></strong> 一个示例网站，用来演示基本的 HTTP 命令</li><li><strong><a href="http://httpbin.org/" target="_blank" rel="noopener">http://httpbin.org/</a></strong> 一个简单的 HTTP 请求、响应服务</li><li><strong><a href="https://jsonplaceholder.typicode.com" target="_blank" rel="noopener">https://jsonplaceholder.typicode.com</a></strong> 一个可以提供模拟 REST API 调用的网站</li></ul><h3 id="调用-http"><a href="#调用-http" class="headerlink" title="调用 http"></a>调用 <code>http</code></h3><p>HTTPie 提供 <code>http</code> 命令来访问 HTTP 服务器。以下是 <code>http</code> 命令最基本的用法，返回了 HTTP 响应头和其他服务器信息。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">➜ http httpie.org</span><br><span class="line"></span><br><span class="line">HTTP/1.1 301 Moved Permanently</span><br><span class="line">CF-RAY: 543ebdd4cad6eb79-LAX</span><br><span class="line">Cache-Control: max-age=3600</span><br><span class="line">Connection: keep-alive</span><br><span class="line">Date: Thu, 12 Dec 2019 09:41:15 GMT</span><br><span class="line">Expires: Thu, 12 Dec 2019 10:41:15 GMT</span><br><span class="line">Location: https://httpie.org/</span><br><span class="line">Server: cloudflare</span><br><span class="line">Transfer-Encoding: chunked</span><br><span class="line">Vary: Accept-Encoding</span><br></pre></td></tr></table></figure><h3 id="获取数据"><a href="#获取数据" class="headerlink" title="获取数据"></a>获取数据</h3><p>最常见的 HTTP 操作是从服务器检索信息，通常通过 HTTP GET 方法来实现。HTTP GET 请求的查询参数是可选的。</p><p>下边是一个 HTTPie 的 GET 方法示例（无查询参数）：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http GET http://httpbin.org/get</span><br></pre></td></tr></table></figure><p>但是，不带查询参数的 GET 请求很少见。可以通过在原始请求后边追加 <code>param==value</code> 的方式来添加参数。</p><p>下边的示例演示了如何在 GET 请求中携带参数。我们来获取 userId 为 1 的所有帖子。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">➜ http https://jsonplaceholder.typicode.com/posts userId==1</span><br></pre></td></tr></table></figure><p>下边是多个参数的例子：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">➜ http https://jsonplaceholder.typicode.com/posts userId==1 id==9</span><br><span class="line"></span><br><span class="line">HTTP/1.1 200 OK</span><br><span class="line">// 忽略响应头</span><br><span class="line"></span><br><span class="line">[</span><br><span class="line">    &#123;</span><br><span class="line">        &quot;body&quot;: &quot;consectetur animi nesciunt iure dolore\nenim quia ad\nveniam autem ut quam aut nobis\net est aut quod aut provident voluptas autem voluptas&quot;,</span><br><span class="line">        &quot;id&quot;: 9,</span><br><span class="line">        &quot;title&quot;: &quot;nesciunt iure omnis dolorem tempora et accusantium&quot;,</span><br><span class="line">        &quot;userId&quot;: 1</span><br><span class="line">    &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>在 HTTP 请求头中携带信息是很常见的做法，在 HTTPie 中我们可以使用 <code>Header:Value</code> 格式添加 HTTP 请求头，如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http example.org X-Foo:Bar Sample:Value</span><br></pre></td></tr></table></figure><h3 id="发布和更新数据"><a href="#发布和更新数据" class="headerlink" title="发布和更新数据"></a>发布和更新数据</h3><p>HTTP 的 POST 方法通常用于在服务器上创建资源，下边的示例演示了如何使用内联方式提供 JSON 数据并发送 POST 请求，<strong>注意：非字符串类型参数的格式为 <code>Param:=Value</code></strong>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">➜ http POST https://jsonplaceholder.typicode.com/posts title=foo body=bar userId:=9</span><br><span class="line"></span><br><span class="line">HTTP/1.1 201 Created</span><br><span class="line">// 忽略响应头</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    &quot;body&quot;: &quot;bar&quot;,</span><br><span class="line">    &quot;id&quot;: 101,</span><br><span class="line">    &quot;title&quot;: &quot;foo&quot;,</span><br><span class="line">    &quot;userId&quot;: 9</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>HTTPie 允许我们将 JSON 数据存入文件中，并在命令行中指定这个文件的路径。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">➜ cat data.txt</span><br><span class="line">&#123;</span><br><span class="line">  &quot;title&quot;: &quot;foo&quot;,</span><br><span class="line">  &quot;body&quot;: &quot;bar&quot;,</span><br><span class="line">  &quot;userId&quot;: 9</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜ http POST https://jsonplaceholder.typicode.com/posts data=@data.txt</span><br><span class="line"></span><br><span class="line">HTTP/1.1 201 Created</span><br><span class="line">// 忽略响应头</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    &quot;data&quot;: &quot;&#123;\n  \&quot;title\&quot;: \&quot;foo\&quot;,\n  \&quot;body\&quot;: \&quot;bar\&quot;,\n  \&quot;userId\&quot;: 9\n&#125;\n&quot;,</span><br><span class="line">    &quot;id&quot;: 101</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>HTTP PUT 方法通常用于更新服务器中已存在的资源，用法和 POST 类似：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">➜ http PUT https://jsonplaceholder.typicode.com/posts/10002 data=@data.txt</span><br></pre></td></tr></table></figure><h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><p>HTTP DELETE 方法用于删除 HTTP 服务器中的资源，示例如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">➜ http DELETE https://jsonplaceholder.typicode.com/posts/1</span><br></pre></td></tr></table></figure><h2 id="通过-HTTPie-进行认证"><a href="#通过-HTTPie-进行认证" class="headerlink" title="通过 HTTPie 进行认证"></a>通过 HTTPie 进行认证</h2><p>上边的示例中我们演示了 HTTPie 的核心用法，在这些示例中，我们假设资源都是可以在不需要任何身份认证的情况下就能够访问的。但在实际场景中很少有这种情况，大多数服务都有安全防护，并强制要求它的用户在访问资源前进行身份认证。</p><p>现代化 HTTP 客户端程序为多种认证模式提供了很好的支持，HTTPie 也不例外，支持主流如：Basic、摘要、密码等认证类型。</p><h3 id="使用-Basic-认证访问资源"><a href="#使用-Basic-认证访问资源" class="headerlink" title="使用 Basic 认证访问资源"></a>使用 Basic 认证访问资源</h3><p>HTTP Basic 认证是 HTTP 协议中的身份验证方案。在 Basic 认证中，HTTP <code>Authorization</code> 请求头设置为 <code>Basic</code>，用户名和密码以明文形式提供。Basic 认证总是需要配合其他安全机制，如：HTTPS。</p><p>以下示例演示如何访问一个要求用户通过 Basic 认证来进行身份验证的资源：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">➜ http --default-scheme=https https://httpbin.org/basic-auth/username/password</span><br><span class="line"></span><br><span class="line">HTTP/1.1 401 UNAUTHORIZED</span><br><span class="line">Access-Control-Allow-Credentials: true</span><br><span class="line">Access-Control-Allow-Origin: *</span><br><span class="line">// 其他响应头</span><br></pre></td></tr></table></figure><p>可以看到，请求在未提供用户名和密码的情况下，服务器的响应状态码为 <code>401 UNAUTHORIZED</code>。HTTPie 通过以 <code>-a username:password</code> 的方式提供 Basic 认证所需要的用户名密码：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">➜ http --default-scheme https https://httpbin.org/basic-auth/username/password -a username:password</span><br><span class="line">HTTP/1.1 200 OK</span><br><span class="line">Access-Control-Allow-Credentials: true</span><br><span class="line">Access-Control-Allow-Origin: *</span><br><span class="line">Connection: keep-alive</span><br><span class="line">// 其他响应头</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    &quot;authenticated&quot;: true,</span><br><span class="line">    &quot;user&quot;: &quot;username&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="使用摘要认证访问资源"><a href="#使用摘要认证访问资源" class="headerlink" title="使用摘要认证访问资源"></a>使用摘要认证访问资源</h3><p>Basic 认证的主要问题是它将用户名和密码以明文的方式发送至服务器。摘要认证略有不同，在摘要认证而非明文模式中，它采用基于哈希的方法与服务器传递凭据。</p><p>以下是摘要认证的流程：</p><ol><li>客户端请求一个需要认证的页面，但是不提供用户名和密码。</li><li>服务器返回 <code>401 Unauthorized</code> 响应代码，并提供认证域(realm)，以及一个随机生成的、只使用一次的数值，称为密码随机数 nonce。</li><li>客户端以上一步中得到的随机数（nonce）、用户名、密码和 realm 的哈希值作为响应</li><li>服务器利用这些信息对客户端进行身份验证，如果身份验证成功，则返回客户端所请求的资源</li></ol><p>HTTPie 使用 <code>-A 摘要标志</code> 并通过 <code>-a</code> 参数提供相应的用户名和密码即可进行摘要认证，如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">➜ http --default-scheme https -A digest -a aa:bb https://httpbin.org/digest-auth/auth/aa/bb</span><br><span class="line">HTTP/1.1 200 OK</span><br><span class="line">Access-Control-Allow-Credentials: true</span><br><span class="line">Access-Control-Allow-Origin: *</span><br><span class="line">Connection: keep-alive</span><br><span class="line">// 其他响应头</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    &quot;authenticated&quot;: true,</span><br><span class="line">    &quot;user&quot;: &quot;aa&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>插件方面，HTTPie 还支持其他身份验证机制，如：jwt-auth、OAuth 等。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>HTTPie 是一个轻量但强大的工具，可以轻松与 HTTP 服务器通信。通过 <code>http</code> 命令并配合合理参数调用各种 HTTP 方法的能力使其成为 RESTful 和微服务生态的理想选择。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;HTTPie 是一个用于与 HTTP 服务器进行交互的命令行客户端。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;h2 id=&quot;概览&quot;&gt;&lt;a href=&quot;#概览&quot; class=&quot;headerlin
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>聊聊软件架构和软件设计</title>
    <link href="https://jpanj.com/2019/difference-between-architecture-and-design/"/>
    <id>https://jpanj.com/2019/difference-between-architecture-and-design/</id>
    <published>2019-12-09T04:04:21.000Z</published>
    <updated>2019-12-09T11:03:12.680Z</updated>
    
    <content type="html"><![CDATA[<p><img src="0.jpeg" alt></p><p>很多人并不了解软件架构和软件设计之间的区别。即使对于开发人员来说，对两者的界限也很模糊，他们可能还会把架构模式和设计模式中的内容搞混。作为一名开发人员，我想简述一下这些概念并解释软件设计和软件架构之间的区别。另外，我还会证明为什么软件架构和软件设计对我们来说很重要。</p><h2 id="软件架构的定义"><a href="#软件架构的定义" class="headerlink" title="软件架构的定义"></a>软件架构的定义</h2><p>简单来说，<strong>软件架构是将软件特性（如灵活性、可伸缩性、可行性和安全性）转换为满足技术和业务期望的结构化解决方案的过程。</strong>这个定义使我们开始去思考可能会影响软件架构设计的各种特性。除了技术需求外，还有其他方面会影响软件架构（如商业需求或运营需求）。</p><h2 id="软件架构的特点"><a href="#软件架构的特点" class="headerlink" title="软件架构的特点"></a>软件架构的特点</h2><p>如上所述，软件特性描述了软件在操作和技术层面上的需求和期望。所以，当老板说我们正在一个快速变化的市场当中竞争，企业需要迅速调整现有的商业模式，这时如果企业的业务需求很紧急，要求在短时间内完成的话，这个软件就应该有「可扩展、模块化和可维护」的特性。作为软件架构师，我们应该将<strong>性能（performance）、低容错性（low fault tolerance）、扩展性（scalability）和可用性（reliability）</strong>作为我们软件的关键特性。在定义了上边的几个特性后，老板告诉你我们当前预算有限，此时又要将另一个特性考虑进来，也就是「<strong>可行性</strong>」。</p><p>这个维基百科中列出了全部的软件特性：<a href="https://en.wikipedia.org/wiki/List_of_system_quality_attributes" target="_blank" rel="noopener">https://en.wikipedia.org/wiki/List_of_system_quality_attributes</a></p><h2 id="软件架构模式"><a href="#软件架构模式" class="headerlink" title="软件架构模式"></a>软件架构模式</h2><p>很多人之前可能听说过「<strong>微服务</strong>」。<strong>微服务</strong>是众多软件架构模式之一，其他的架构模式还有分层模式（Layered）、事件驱动模式（Event-Driven）、无服务模式（Serverless）等。我会在后后面介绍几个常见的架构模式。微服务模式在被亚马逊和 Netflix 采纳后收获了很大的影响力。现在，让我们更深入地研究架构模式。</p><p><strong>这里提醒一句，不要把设计模式（如工厂模式或适配器模式）与架构模式搞混，我们在稍后讨论设计模式。</strong></p><h3 id="无服务架构"><a href="#无服务架构" class="headerlink" title="无服务架构"></a>无服务架构</h3><p>无服务架构指的是依赖第三方服务来管理服务器和后端复杂基础设施的应用解决方案。无服务架构可以分为两类：第一类是「后端即服务（BaaS）」，另一类是「函数即服务（FaaS）」。</p><p>无服务架构帮助我们节省了大量服务器部署和例行维护任务所花费的时间。最著名的无服务 API 提供商是亚马逊的 AWS Lambda。</p><h3 id="事件驱动架构"><a href="#事件驱动架构" class="headerlink" title="事件驱动架构"></a>事件驱动架构</h3><p>事件驱动架构依赖事件生产者（ Event Producers）和事件消费者（Event Consumers）。它的主要思想是将系统各个模块进行解耦，当有某个模块中的一个事件发生后，对这个事件感兴趣的其他模块会被触发。听起来很复杂？我们来举个简单的例子：假如你设计了一个在线购物系统，它包含两个模块：订单模块和供应商模块。如果客户产生了购买行为，订单模块会生成一个 <code>ORDER_PENDING</code> 事件。由于供应商模块对 <code>ORDER_PENDING</code> 事件感兴趣，所以它会监听这个事件，用以触发后续的行为。一旦供应商模块收到这个事件，它会执行一些任务或者触发其他后续事件（比如从某个供货商处订购更多的商品、通知仓库发货等）。</p><p>需要记住的是，事件生产者并不知道有哪些事件消费者在监听哪些事件。</p><h3 id="微服务架构"><a href="#微服务架构" class="headerlink" title="微服务架构"></a>微服务架构</h3><p>微服务架构已成为近几年最受欢迎的架构。它依赖于开发小而独立的模块化服务，其中每个服务都可以解决特定的问题或执行独特的任务，这些模块通过定义明确的 API 互相通信来实现业务目标。对于微服务架构无需介绍太多，来看看下边这张图：</p><p><img src="1.png" alt></p><h2 id="软件设计"><a href="#软件设计" class="headerlink" title="软件设计"></a>软件设计</h2><p>软件架构负责软件框架和基础设施的选型，软件设计负责代码级别的设计，例如每个模块的作用、类的范围和函数用途等。</p><p>作为一名开发人员，了解 <code>SOLID</code> 原则和如何通过设计模式来解决常规问题是非常重要的。</p><p><code>SOLID</code> 指的是单一职责原则（<strong>S</strong>ingle Responsibility）、开闭原则（<strong>O</strong>pen Closed）、里式替换原则（<strong>L</strong>iskov substitution）、接口隔离原则（<strong>I</strong>nterface Segregation）和依赖反转原则（<strong>D</strong>ependency Inversion）。</p><ul><li><strong>单一职责原则</strong>意味着每个类只有一个单一的目的和责任。</li><li><strong>开闭原则</strong>：一个类应该对扩展开放、对修改关闭。详细表述一下就是，添加一个新的功能应该是，在已有代码基础上扩展代码（新增模块、类、方法等），而非修改已有代码（修改模块、类、方法等）。</li><li><strong>里式替换原则</strong>：这个原则指导开发人员任何情况下使用继承都不要破坏应用逻辑。举个例子，如果子类 <code>XyClass</code> 继承自 <code>AbClass</code>，那么 <code>XyClass</code> 不可以改变父类已实现功能的行为。因此你可以放心地使用 <code>XyClass</code> 对象而不是 <code>AbClass</code> 对象，而不用担心破坏应用的逻辑。</li><li><strong>接口隔离原则</strong>：简单来说，因为一个类可以实现多个接口，所以应该合理的组织代码，使一个类无需被迫实现与其目的无关的方法。因此，要把接口分好类。</li><li><strong>依赖反转原则</strong>：如果你遵循 TDD（测试驱动开发Test-Driven Development）的方式进行应用开发，那么你就会知道将代码解耦对于可测试性和模块化是多么重要。举个例子，如果 <code>Order</code> 类依赖于 <code>User</code> 类，那么 <code>User</code> 对象应该在 <code>Order</code> 类之外进行实例化。</li></ul><h2 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h2><h3 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h3><p><strong>工厂模式</strong>是 OOP 世界中最常用的设计模式，因为通过这个模式，我们在创建对象时不会对客户端暴露创建逻辑，并且是通过使用一个共同的接口来指向新创建的对象。来看个例子：</p><p>我们现在有一个接口和三个实现了这个接口的类：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">public class Rectangle implements Shape &#123;</span><br><span class="line"> </span><br><span class="line">   @Override</span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      System.out.println(&quot;Inside Rectangle::draw() method.&quot;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Square implements Shape &#123;</span><br><span class="line"> </span><br><span class="line">   @Override</span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      System.out.println(&quot;Inside Square::draw() method.&quot;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Circle implements Shape &#123;</span><br><span class="line"> </span><br><span class="line">   @Override</span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      System.out.println(&quot;Inside Circle::draw() method.&quot;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假如你现在要实例化一个方形 <code>Shape</code>，有两种方式可以实现：</p><p>第一种：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Shape shape = new Square();</span><br></pre></td></tr></table></figure><p>第二种：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Shape shape = ShapeFactory.getShape(&quot;SQUARE&quot;);</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">public class ShapeFactory &#123;</span><br><span class="line">   public static Shape getShape(String shapeType)&#123;</span><br><span class="line">      if(shapeType == null)&#123;</span><br><span class="line">         return null;</span><br><span class="line">      &#125;        </span><br><span class="line">      if(shapeType.equalsIgnoreCase(&quot;CIRCLE&quot;))&#123;</span><br><span class="line">         return new Circle();</span><br><span class="line">      &#125; else if(shapeType.equalsIgnoreCase(&quot;RECTANGLE&quot;))&#123;</span><br><span class="line">         return new Rectangle();</span><br><span class="line">      &#125; else if(shapeType.equalsIgnoreCase(&quot;SQUARE&quot;))&#123;</span><br><span class="line">         return new Square();</span><br><span class="line">      &#125;</span><br><span class="line">      return null;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我更喜欢第二种方式，有三点原因。首先一个调用者想创建一个对象，只要知道其名称就可以了。其次扩展性高，如果想增加一个新的形状，只要扩展 <code>ShapeFactory</code> 工厂类就可以。最后屏蔽了具体的实现，调用者只用关心 <code>Shape</code> 接口，即使需要传额外参数来进行实例化，调用者也无需去关心。</p><h3 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h3><p><strong>适配器模式</strong>是结构设计模式之一。根据这个名字可以判断出，我们可以期望它把类的意外用法转为我们所预期的用法。</p><p>假如我们的应用要调用了百度的 API，需要在发起请求前需要调用 <code>getBaiduToken()</code> 获取 token。我们在 20 多个不同的地方调用了这个函数。之后百度在发布的新版本中把这个函数改名为了 <code>getAccessToken()</code>。</p><p>现在我们必须在应用代码的所有位置找到并替换这个函数名，或者可以创建一个适配器类：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">public class BaiduAdapter &#123;</span><br><span class="line"></span><br><span class="line">    public static String getToken() &#123;</span><br><span class="line">        Baidu baidu = new Baidu();</span><br><span class="line">        String token = baidu.getBaiduToken;</span><br><span class="line">        return token;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>应用中的调用改为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">token = BaiduAdapter.getToken();</span><br></pre></td></tr></table></figure><p>这种情况下，即使百度修改了函数名，我们也只需修改一行代码，应用程序的其余部分将保持正常工作。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">public class BaiduAdapter &#123;</span><br><span class="line"></span><br><span class="line">    public static String getToken() &#123;</span><br><span class="line">        Baidu baidu = new Baidu();</span><br><span class="line">        String token = baidu.getAccessToken;</span><br><span class="line">        return token;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>本文没有详细讨论各种设计模式，如果你想了解更多，我推荐 2 本关于设计模式的书：</p><ul><li>《设计模式》<ul><li>学习设计模式，不知道 GoF 的《设计模式》估计会被人笑话的。这本书比较晦涩难懂，对于初学者不建议从这本书看起。</li></ul></li><li>《Head First 设计模式》<ul><li>这本书最大的特点就是口语化、场景化。整本书围绕几个人的对话来展开。里面的例子比较脱离实践，但比较容易看懂。</li></ul></li></ul><h2 id="架构师-vs-程序员"><a href="#架构师-vs-程序员" class="headerlink" title="架构师 vs 程序员"></a>架构师 vs 程序员</h2><p>最后再来说说软件架构师和软件开发人员之间的区别。</p><p>架构师通常是具有丰富经验的 team leader，他们对现有解决方案有很好的了解，这些方案可以帮助他们在计划阶段做出正确的决策。软件开发人员应该去了解更多的软件设计，并对软件架构有足够的了解，以使团队内部的沟通更佳高效。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;0.jpeg&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;很多人并不了解软件架构和软件设计之间的区别。即使对于开发人员来说，对两者的界限也很模糊，他们可能还会把架构模式和设计模式中的内容搞混。作为一名开发人员，我想简述一下这些概念并解释软件设计和软件架构之间的区别。另外，
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Spring 中最常用的 5 个注解</title>
    <link href="https://jpanj.com/2019/top-5-spring-annotation/"/>
    <id>https://jpanj.com/2019/top-5-spring-annotation/</id>
    <published>2019-12-07T04:46:15.000Z</published>
    <updated>2019-12-07T04:48:19.946Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>Java 中注解的引入改变了 Java 开发人员配置应用的方式。注解在 Java 的 1.5 版本中引入进来，它使开发人员能够在代码中维护配置而不必依赖于外部配置文件。</p></blockquote><p><img src="1.jpeg" alt></p><p>注解是可以添加到 Java 类、方法、变量、参数或者包中的一种语法元数据。</p><p>Spring 框架推荐开发人员通过使用它提供的大量内置注解来配置应用。在这篇文章中，我们重点介绍 <code>Spring Core</code> 框架中最常用的几个注解。</p><h2 id="1-Autowired-注解"><a href="#1-Autowired-注解" class="headerlink" title="1. @Autowired 注解"></a>1. @Autowired 注解</h2><p>这个注解用于声明类中的依赖项。基于这个注解，Spring DI 框架可以注入对应的依赖。<code>@Autowired</code> 可以用在构造函数、属性和 <code>setter</code> 方法上。它是 JSR-330（Java 依赖注入）<code>@Inject</code> 注解的替代方法。</p><h3 id="属性注入"><a href="#属性注入" class="headerlink" title="属性注入"></a>属性注入</h3><p>下面的代码演示了如何将属性作为依赖项注入：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">import org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"></span><br><span class="line">public class UserController &#123;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    private UserRepository userRepository;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Setter-方法注入"><a href="#Setter-方法注入" class="headerlink" title="Setter 方法注入"></a>Setter 方法注入</h3><p>也可以通过 <code>setter</code> 方法完成依赖注入，如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">import org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"></span><br><span class="line">public class UserController &#123;</span><br><span class="line"></span><br><span class="line">    private UserRepository userRepository;</span><br><span class="line"></span><br><span class="line">    public UserRepository getUserRepository() &#123;</span><br><span class="line">        return userRepository;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    public void setUserRepository(UserRepository userRepository) &#123;</span><br><span class="line">        this.userRepository = userRepository;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="构造函数注入"><a href="#构造函数注入" class="headerlink" title="构造函数注入"></a>构造函数注入</h3><p>还可以在构造函数上使用：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">import org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"></span><br><span class="line">public class UserController &#123;</span><br><span class="line"></span><br><span class="line">    private UserRepository userRepository;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    public UserController(UserRepository userRepository) &#123;</span><br><span class="line">        this.userRepository = userRepository;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-Bean-注解"><a href="#2-Bean-注解" class="headerlink" title="2. @Bean 注解"></a>2. @Bean 注解</h2><p>这个注解应用在方法上，并生成由 Spring 管理的 bean。Spring 配置类通常包含 bean 声明。通常，应用的 <code>POJO</code> 部分被声明为 Spring 组件，并且 Spring 提供的组件扫描机制会在 Spring IoC 容器中自动创建 <code>bean</code>。但是，当 <code>POJO</code> 的代码不可用并且我们需要创建 Sprint 管理的 <code>bean</code> 时，<code>@bean</code> 注解就会非常有用。</p><p>以下代码演示了 <code>@Bean</code> 注解的用法：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">public class SampleConfiguration &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public User user()&#123;</span><br><span class="line">        return new User();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Bean(name = &quot;admin&quot;, initMethod = &quot;&quot;, destroyMethod = &quot;&quot;)</span><br><span class="line">    public Admin admin()&#123;</span><br><span class="line">        return new Admin();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此外，这个注解还提供给了一些属性用以管理 <code>bean</code> 配置，例如：名称、初始化方法、销毁方法等。</p><h2 id="3-Value-注解"><a href="#3-Value-注解" class="headerlink" title="3. @Value 注解"></a>3. @Value 注解</h2><p>Spring 的 <code>@Value</code> 注解非常有用，可以方便地使用 Spring 表达式语言（Spring Expression Language，SpEL）提供默认值或控制变量的值。</p><h3 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h3><p>在下边的示例中，<code>name</code> 变量配置了 <code>@Value</code> 注解。如果实例化 <code>User</code> 类时没有为 <code>name</code> 提供值，就会使用 <code>@Value</code> 配置的默认值 <code>default-user</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">import org.springframework.beans.factory.annotation.Value;</span><br><span class="line"></span><br><span class="line">public class User &#123;</span><br><span class="line"></span><br><span class="line">    @Value(&quot;default-user&quot;)</span><br><span class="line">    private String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="从环境中读取属性"><a href="#从环境中读取属性" class="headerlink" title="从环境中读取属性"></a>从环境中读取属性</h3><p>下面的示例演示如何从环境中读取一个值并赋给变量：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">public class User &#123;</span><br><span class="line"></span><br><span class="line">    @Value(&quot;$&#123;NAME&#125;&quot;)</span><br><span class="line">    private String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="使用-Spring-表达式语言"><a href="#使用-Spring-表达式语言" class="headerlink" title="使用 Spring 表达式语言"></a>使用 Spring 表达式语言</h3><p>下边的示例演示如何使用 Spring 表达式语言来获取值并赋值给变量。注意这里使用了 <code>#</code> 来替代之前使用的 <code>$</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">public class User &#123;</span><br><span class="line"></span><br><span class="line">    @Value(&quot;#&#123;systemProperties[&apos;user.name&apos;]&#125;&quot;)</span><br><span class="line">    private String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-Profile-注解"><a href="#4-Profile-注解" class="headerlink" title="4. @Profile 注解"></a>4. @Profile 注解</h2><p>在开发的生命周期中，应用会经历多个环境和阶段。比如，<code>dev</code>、<code>test</code>、<code>uat</code>（预发布）、<code>industry</code>（生产）等。根据不同的环境和阶段，需要有不同的配置。在这种情况下，<code>@Profile</code> 注解非常方便，它使开发人员可以灵活地控制应该激活的组件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">@Profile(&quot;dev&quot;)</span><br><span class="line">public class DevDataSource &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@Profile(&quot;test&quot;)</span><br><span class="line">public class QADataSource &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上边的示例中，我们提供了两个配置，一个用于 <code>dev</code> 环境，另一个用于 <code>test</code> 环境。根据环境类型，我们可以提供不同的配置，Spring 将会确保加载与之对应的配置。</p><h2 id="5-Import-注解"><a href="#5-Import-注解" class="headerlink" title="5. @Import 注解"></a>5. @Import 注解</h2><p><code>@Import</code> 注解使我们可以将一个或多个组件的配置导入到另一个配置类中。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">@Import(SampleConfiguration.class)</span><br><span class="line">public class AnotherConfiguration &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public Product product()&#123;</span><br><span class="line">        return new Product();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上边的配置类中，我们导入了另一个配置类中定义的配置。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文我们演示了在 Spring 应用开发中使用最频繁的一些注解。尽管有大量的 Spring 注解，但那些注解在大多数 Spring 应用中用到的不多。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;Java 中注解的引入改变了 Java 开发人员配置应用的方式。注解在 Java 的 1.5 版本中引入进来，它使开发人员能够在代码中维护配置而不必依赖于外部配置文件。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpeg&quot; 
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Serverless 入门</title>
    <link href="https://jpanj.com/2019/serverless-introduce/"/>
    <id>https://jpanj.com/2019/serverless-introduce/</id>
    <published>2019-12-04T01:58:00.000Z</published>
    <updated>2019-12-04T02:19:40.858Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.png" alt></p><blockquote><p>云技术已经彻底改变了我们管理应用程序的方式，尽管很多公司早已不再使用物理服务器，但他们仍然从服务器的角度来看待他们的系统。</p></blockquote><p>如果我们试图把服务器的概念忘掉，并开始把基于云的应用程序视为工作流、分布式逻辑和外部管理的数据存储，会是什么情况？</p><p>本文文我们一起探讨下 Serverless。</p><p>和其它软件开发趋势一样，Serverless 并没有一个清晰的概念，它可以用在两种不同但又有些相似的领域：</p><ul><li>Serverless 最初是用来描述那些结合第三方、云托管来管理服务器端逻辑和状态的应用。通常是一些「富客户端」应用，比如单页 web 应用或者手机 APP，它们可以使用第三方提供的庞大生态系统来进行云端存储（比如：国内的 LeanCloud，国外的 Parse、Firbase）。这些类型的服务之前被称为「后端即服务」或「BaaS」。</li><li>Serverless 还可以表示另一种情况，开发人员仍然编写服务器端应用代码，但是与传统架构不同，这个应用运行在无状态的容器中，这些容器是事件触发、短暂（可能只调用一次）并且由第三方来管理的。这种做法通常被理解为「函数即服务」或「FaaS」。AWS Lambda 是目前提供「函数即服务」的实现之一。</li></ul><p>尽管有 Serverless 这个名字，但实际并不是在没有服务器的情况下运行代码。之所以使用「无服务器计算（serverless computing）」这个名称，是因为拥有系统的企业或个人不必为运行后端应用而采购、租用、配置服务器或虚拟机。</p><p>Serverless 有以下优势：</p><ul><li>无服务器管理（无需管理任何形式的服务器）</li><li>按执行付费（不为空闲时间买单）</li><li>自动伸缩（根据需求伸缩）</li><li>函数作为应用的逻辑单元</li></ul><p>Serverless 模式鼓励将开发重点放在定义明确的业务逻辑单元上，而无需考虑如何部署、扩容或其它一些过早优化。因此开发的重点也应该是单个功能或模块，而不是一个具有大范围功能的服务。Serverless 将开发人员从部署的麻烦中解放出来，使得他们能够专注于按照逻辑封装应用。</p><p>一个典型的例子是将图片上传到文件存储，此事件调用一个 Serverless 函数，这个函数创建图片的缩略图然后把该缩略图存入文件存储中，并将缩略图位置记录在 NoSQL 数据库中。数据写入 NoSQL 数据库的事件可能还会触发其他函数。这个缩略图创建函数只需按需运行，唯一的成本是调用该函数的次数。</p><p>和其他技术一样，Serverless 并不完美。它的缺点是应用监控和调试将会变得困难，只能依靠于服务产生的日志记录。同时，在有服务间调用事件时，可能会出现供应商锁定。并且现有的 IDE 对 Serverless 函数支持也不够友好。</p><h2 id="简单-HTTP-服务示例"><a href="#简单-HTTP-服务示例" class="headerlink" title="简单 HTTP 服务示例"></a>简单 HTTP 服务示例</h2><blockquote><p>Serverless 框架 —— 可以构建由微服务组成的应用，这些微服务在响应事件时运行，并且可以自动扩容、只在运行期间收费。</p></blockquote><p>下边的例子将演示如何实现一个简单的 HTTP <code>GET</code> 端点，调用它时会返回当前的时间。内部函数名为 <code>currentTime</code>，HTTP 端点为 <code>ping</code>。</p><h3 id="快速上手-Serverless"><a href="#快速上手-Serverless" class="headerlink" title="快速上手 Serverless"></a>快速上手 Serverless</h3><ul><li>通过 npm 安装 serverless 程序</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install -g serverless</span><br></pre></td></tr></table></figure><ul><li>配置安全凭证<ul><li><a href="https://github.com/serverless/serverless/blob/master/docs/providers/aws/guide/credentials.md" target="_blank" rel="noopener">https://github.com/serverless/serverless/blob/master/docs/providers/aws/guide/credentials.md</a></li><li>或参考我之前的文章：<a href="/2019/telegram-bot-send-taily-message/">不到 40 行代码实现 Telegram 自动发消息机器人</a></li></ul></li><li>创建服务</li></ul><p>我们需要新建 <code>handler.js</code> 和 <code>serverless.yml</code> 文件来描述和部署我们的 severless 函数。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">// handler.js</span><br><span class="line"></span><br><span class="line">&apos;use strict&apos;;</span><br><span class="line"></span><br><span class="line">module.exports.endpoint = (event, context, callback) =&gt; &#123;</span><br><span class="line">  const response = &#123;</span><br><span class="line">    statusCode: 200,</span><br><span class="line">    body: JSON.stringify(&#123;</span><br><span class="line">      message: `Hello, the current time is $&#123;new Date().toTimeString()&#125;.`,</span><br><span class="line">    &#125;),</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  callback(null, response);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">// serverless.yml</span><br><span class="line"></span><br><span class="line">service: serverless-simple-http-endpoint</span><br><span class="line"></span><br><span class="line">frameworkVersion: &quot;&gt;=1.1.0 &lt;2.0.0&quot;</span><br><span class="line"></span><br><span class="line">provider:</span><br><span class="line">  name: aws</span><br><span class="line">  runtime: nodejs8.10</span><br><span class="line"></span><br><span class="line">functions:</span><br><span class="line">  currentTime:</span><br><span class="line">    handler: handler.endpoint</span><br><span class="line">    events:</span><br><span class="line">      - http:</span><br><span class="line">          path: ping</span><br><span class="line">          method: get</span><br></pre></td></tr></table></figure><h3 id="本地函数调用"><a href="#本地函数调用" class="headerlink" title="本地函数调用"></a>本地函数调用</h3><p>在命令行中执行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">serverless invoke local --function currentTime</span><br></pre></td></tr></table></figure><p>返回结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;statusCode&quot;: 200,</span><br><span class="line">    &quot;body&quot;: &quot;&#123;\&quot;message\&quot;:\&quot;Hello, the current time is 21:46:18 GMT+0800 (CST).\&quot;&#125;&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h3><p>部署应用只需执行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">serverless deploy</span><br></pre></td></tr></table></figure><p>在安全凭证配置正确的情况下会看到类似下边的结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">Serverless: Packaging service...</span><br><span class="line">Serverless: Excluding development dependencies...</span><br><span class="line">Serverless: Uploading CloudFormation file to S3...</span><br><span class="line">Serverless: Uploading artifacts...</span><br><span class="line">Serverless: Uploading service serverless-simple-http-endpoint.zip file to S3 (331 B)...</span><br><span class="line">Serverless: Validating template...</span><br><span class="line">Serverless: Updating Stack...</span><br><span class="line">Serverless: Checking Stack update progress...</span><br><span class="line">..............................</span><br><span class="line">Serverless: Stack update finished...</span><br><span class="line">Service Information</span><br><span class="line">service: serverless-simple-http-endpoint</span><br><span class="line">stage: dev</span><br><span class="line">region: us-east-1</span><br><span class="line">stack: serverless-simple-http-endpoint-dev</span><br><span class="line">resources: 11</span><br><span class="line">api keys:</span><br><span class="line">  None</span><br><span class="line">endpoints:</span><br><span class="line">  GET - https://qnye7m4dwf.execute-api.us-east-1.amazonaws.com/dev/ping</span><br><span class="line">functions:</span><br><span class="line">  currentTime: serverless-simple-http-endpoint-dev-currentTime</span><br><span class="line">layers:</span><br><span class="line">  None</span><br></pre></td></tr></table></figure><h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>现在，我们可以直接调用 AWS Lambda 服务，并且可以同时获取执行日志：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">serverless invoke --function currentTime --log</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    &quot;statusCode&quot;: 200,</span><br><span class="line">    &quot;body&quot;: &quot;&#123;\&quot;message\&quot;:\&quot;Hello, the current time is 14:03:57 GMT+0000 (UTC).\&quot;&#125;&quot;</span><br><span class="line">&#125;</span><br><span class="line">--------------------------------------------------------------------</span><br><span class="line">START RequestId: 002cbcce-fda6-4d84-98a2-2fb19d325812 Version: $LATEST</span><br><span class="line">END RequestId: 002cbcce-fda6-4d84-98a2-2fb19d325812</span><br><span class="line">REPORT RequestId: 002cbcce-fda6-4d84-98a2-2fb19d325812Duration: 0.61 msBilled Duration: 100 msMemory Size: 1024 MBMax Memory Used: 59 MB</span><br></pre></td></tr></table></figure><p>或者使用如 <code>curl</code> 等工具发送一个 <code>HTTP</code> 请求来查看结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">curl https://qnye7m4dwf.execute-api.us-east-1.amazonaws.com/dev/ping</span><br><span class="line"></span><br><span class="line">&#123;&quot;message&quot;:&quot;Hello, the current time is 14:03:49 GMT+0000 (UTC).&quot;&#125;</span><br></pre></td></tr></table></figure><p>甚至可以直接用浏览器访问：</p><p><img src="2.png" alt></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.png&quot; alt&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;云技术已经彻底改变了我们管理应用程序的方式，尽管很多公司早已不再使用物理服务器，但他们仍然从服务器的角度来看待他们的系统。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;如果我们试图把服务器的概
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>我对肥胖、营养与三餐的看法</title>
    <link href="https://jpanj.com/2019/fat-nutrition-and-meal/"/>
    <id>https://jpanj.com/2019/fat-nutrition-and-meal/</id>
    <published>2019-12-01T06:16:10.000Z</published>
    <updated>2019-12-01T06:17:50.723Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.jpg" alt></p><blockquote><p>我之前在一年多的时间里，在很少运动的情况下，减掉了 50 多斤的体重（从 90 多公斤到目前稳定在 65 公斤内），同时内脏脂肪也降了下来（我的体脂称告诉我的），今年体检后各项指标也都正常，所以对本文谈到的东西也算有话语权。</p></blockquote><blockquote><p>本文不会给出任何具体的减肥方法，只是表达我的一些观点，不带有任何异端邪说请放心服用。</p></blockquote><h2 id="肥胖"><a href="#肥胖" class="headerlink" title="肥胖"></a>肥胖</h2><p>肥胖是一种状态，是身体发炎的表现。肥胖是慢性病的主角，就像一股闷火在体内 24 小时不停的烧，烧着烧着就出了大问题。</p><p>以前人们提到心血管疾病都只是简单的归结为心血管阻塞，为什么阻塞，以前把罪嫁祸于胆固醇。</p><p>但是如果你的血管是健康的，它是没有理由被任何东西阻塞的，血管内壁是很光滑的组织，只有发炎的时候才会变得粗糙，才会粘附东西，不过粘附的也不一定是胆固醇。</p><p>胆固醇并没有罪，所以还是请大家多吃鸡蛋，不要害怕吃鸡蛋。</p><p>香烟在 2016 年被世界卫生组织认为是健康最大的杀手，但是 2016 年的下半年，世卫组织改口称肥胖才是人类最大的杀手，香烟已经退居到了第二位，所以最害怕的人其实是既肥胖又抽烟的人，这种绝对就是在自杀。</p><p>世界卫生组织关于肥胖的说明：</p><p><img src="1.png" alt></p><p>发炎时我们第一个想到的就是去吃消炎药，这是不对的。最好的方法是减肥，体重降了炎症也就降了，慢性病就会渐渐的消失。</p><p>结合上边的香烟有害健康，可以得出<strong>减肥、戒烟可以治百病</strong>。</p><p>减肥吃减肥药是件最愚蠢的事，如果尝试过吃药减肥的人，都会感到很痛苦，但凡减肥药，都会有一种叫做<strong>安非他命</strong>的成分，安非他命是一种中枢神经兴奋剂，吃完会心跳加快、没有食欲。也早就已经被证明是一种无效的手段。</p><h2 id="热量-营养"><a href="#热量-营养" class="headerlink" title="热量 != 营养"></a>热量 != 营养</h2><p>你认为你吃了一顿很丰盛的饭，你觉得很营养吗？其实并不是，大部分都是吃了很多的热量，热量不等于营养。</p><p>每克蛋白质、脂肪、淀粉中的热量分别为4卡、9卡、4卡，这个叫做热量。维他命 C 是营养，可是它并没有热量。</p><p>拿我本人来说，自从瘦下来后对冷的敏感度就提高了，这也只是说明我的热量小了，不代表我营养不良，没什么大不了的，多穿几件衣服，睡觉的时候盖厚点就可以了。</p><p>肥胖的状态就是热量过剩，还可能伴随营养不良。脂肪是没有营养的，但是我们都知道它的热量也是最多的。</p><p>但是脂肪是个很好的东西，不要害怕它，减肥就是要燃烧脂肪。我们要做的是热量尽量少，营养尽量多。</p><p>现代人都知道病是吃出来的，吃出病来后又在想我要吃些什么才能把病治好？</p><p>答案并不是要吃什么，而是不吃什么。</p><h3 id="每天一定要吃三餐吗？"><a href="#每天一定要吃三餐吗？" class="headerlink" title="每天一定要吃三餐吗？"></a>每天一定要吃三餐吗？</h3><p>人类吃三餐的历史并不长，其实现在还有很多地方保持一日两餐的习惯。</p><p>三餐是工业化后的产物，餐饮业越来越发达、便利店越来越多，让我们吃东西越来越方便，方便的后果就是吃东西吃过剩、吃太多。</p><p>来看一个英文单词：breakfast。</p><p>我们都知道它是早餐的意思，但是它是两个单词的组合，break 和 fast。</p><p>break 的意思是 打破、阻断、破坏。</p><p>fast 大多数人只知道它有快的意思，但实际上它是一个医学专有名词，叫做<strong>「禁食」</strong>。</p><p>breakfast 这个单词已经告诉我们了，早餐是破坏禁食状态，不吃才是对的，常态应该是不吃。但是很多人就会跳起来反驳我，不吃我会饿啊。别急，听我继续说。</p><p>现在的上班族，大多是 8 点多吃早餐，不到 12 点又要去吃午餐，甚至有的是 10 点吃早餐，12 点又要去吃，为什么？</p><p>这是因为我们认为吃饭的<strong>时间到了</strong>。大家都知道时间到了，就要去吃啊。</p><p>这种想法是不对的，正确的应该是饿了才去吃。</p><p>什么叫饿？饿是可以训练的，具体如何做不在本文的讨论范围内，不然又会到具体的方法上，又会被认为是异端邪说。</p><p>好了，今天就聊这些，本文的意图只是给大家提供一些新的视角来思考关于健康的问题。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;我之前在一年多的时间里，在很少运动的情况下，减掉了 50 多斤的体重（从 90 多公斤到目前稳定在 65 公斤内），同时内脏脂肪也降了下来（我的体脂称告诉我的），今年体检后各项指标也都正常，
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>通过蜜罐技术自制一个暴力破解字典库</title>
    <link href="https://jpanj.com/2019/ssh-honeypot/"/>
    <id>https://jpanj.com/2019/ssh-honeypot/</id>
    <published>2019-11-28T09:02:51.000Z</published>
    <updated>2019-12-01T06:14:37.197Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.jpg" alt></p><blockquote><p>蜜罐指具有缺陷的，用于吸引网络的计算机病毒侵占以便用于病毒的研究和破解的计算机。</p></blockquote><p>互联网就像一个黑暗丛林，当你拥有一个面向公网的服务器时，永远不知道会有多少双眼睛在盯着你。</p><p>基于这个信条，我相信我的几台 vps 经常受到各种 ssh 的暴力破解的骚扰，为了安全考虑我也早就把默认 ssh 端口号 22 改为了某个随机值（有些是运营商强行修改）。</p><p>前段时间突发奇想，是否可以监听下 22 端口，感受下在这个黑暗丛林中来自各方的打击？继而又想到，既然来感受打击，为何不把这些打击详细记录一下，假以时日，我是不是就可以得到一个「丛林常用爆破密码库」了？</p><h2 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h2><p>说干就干，选择了我其中一个坐落于米国的服务器来部署蜜罐程序。修改 ssh 默认端口号的步骤就不再介绍了，直奔主题。</p><p>为了方便，我通过 Docker 来部署，只需 1 行命令：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -itd --name ssh-honeypot -p 22:22 txt3rob/docker-ssh-honey</span><br></pre></td></tr></table></figure><p>这里用 Docker 启动了一个 ssh 蜜罐镜像，然后把蜜罐的 22 端口映射到本地 22 端口，验证一下 22 端口的开放情况：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># lsof -i:22</span><br><span class="line">COMMAND     PID USER   FD   TYPE   DEVICE SIZE/OFF NODE NAME</span><br><span class="line">docker-pr 20832 root    4u  IPv6 22035968      0t0  TCP *:ssh (LISTEN)</span><br></pre></td></tr></table></figure><p>后边就坐等蜜罐来收集登录信息吧。</p><p>程序默认把登录日志输出到 Docker 容器的控制台中，日志格式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">[Thu Nov  7 17:59:00 2019] 187.189.55.192 root 123456</span><br><span class="line">[Thu Nov  7 17:59:00 2019] 187.189.55.192 root password</span><br><span class="line">[Thu Nov  7 17:59:00 2019] 187.189.55.192 root default</span><br><span class="line">[Thu Nov  7 17:59:01 2019] 187.189.55.192 root root</span><br><span class="line">[Thu Nov  7 17:59:01 2019] 187.189.55.192 root 000000</span><br><span class="line">[Thu Nov  7 17:59:01 2019] 187.189.55.192 root 111111</span><br><span class="line">[Thu Nov  7 17:59:01 2019] 187.189.55.192 root password</span><br></pre></td></tr></table></figure><p>我们可以通过管道（<code>|</code>） + 重定向（<code>&gt;</code>）的方式把结果导出出来：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker logs  $(docker ps -f name=ssh-honeypot -q) | grep -v &apos;Error exchanging&apos; | grep -v &apos;Session&apos; | awk &apos;&#123;print $6, $7, $8&#125;&apos; &gt; ./ssh_password.log</span><br></pre></td></tr></table></figure><p>上边命令只保留了 ip、username、password 三列，同时过滤掉了程序自己打印的日志（包含 <code>Session</code>或者 <code>Error exchanging</code> 的行）。</p><h3 id="拿到数据后需要再进行一下简单的清洗："><a href="#拿到数据后需要再进行一下简单的清洗：" class="headerlink" title="拿到数据后需要再进行一下简单的清洗："></a>拿到数据后需要再进行一下简单的清洗：</h3><p>去掉行首行尾的空格<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sed -i &apos;s/^[ \t]*//g&apos; ssh_password.log</span><br><span class="line">sed -i &apos;s/[ \t]*$//g&apos; ssh_password.log</span><br></pre></td></tr></table></figure></p><p>去除空行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sed -i &apos;/^$/d&apos; ssh_password.log</span><br></pre></td></tr></table></figure><p>去掉数据结尾的 <code>^M</code></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dos2unix -f ssh_password.log</span><br></pre></td></tr></table></figure><h2 id="收网"><a href="#收网" class="headerlink" title="收网"></a>收网</h2><p>运行三周后，我一共收到了 1340万+ 的用户名密码（看到结果后有些震惊），去重后（<code>cat ssh_password.log | awk &#39;{print $2$3}&#39; | uniq -c | wc -l</code>）也有 1290万，之后我对这些数据进行了统计。</p><h3 id="统计常用用户名的-top5："><a href="#统计常用用户名的-top5：" class="headerlink" title="统计常用用户名的 top5："></a>统计常用用户名的 top5：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># awk &apos;$2!=&quot;&quot; &#123;sum[$2]+=1&#125; END &#123;for(k in sum) print k &quot;:&quot; sum[k]&#125;&apos; ssh_password.log  | sort -n -r -k 2 -t &apos;:&apos; | head -n 5</span><br><span class="line">admin:7179731</span><br><span class="line">root:6236949</span><br><span class="line">test:1062</span><br><span class="line">guest:814</span><br><span class="line">mysql:799</span><br></pre></td></tr></table></figure><h3 id="统计常用密码的-top5："><a href="#统计常用密码的-top5：" class="headerlink" title="统计常用密码的 top5："></a>统计常用密码的 top5：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># awk &apos;$3!=&quot;&quot; &#123;sum[$3]+=1&#125; END &#123;for(k in sum) print k &quot;:&quot; sum[k]&#125;&apos; ssh_password.log  | sort -n -r -k 2 -t &apos;:&apos; | head -n 5</span><br><span class="line">admin:727668</span><br><span class="line">12345:726670</span><br><span class="line">1234:726205</span><br><span class="line">password:725527</span><br><span class="line">default:724872</span><br></pre></td></tr></table></figure><h3 id="统计常用用户名密码组合的-top5："><a href="#统计常用用户名密码组合的-top5：" class="headerlink" title="统计常用用户名密码组合的 top5："></a>统计常用用户名密码组合的 top5：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># awk &apos;$2!=&quot;&quot; &#123;sum[$2&quot;_&quot;$3]+=1&#125; END &#123;for(k in sum) print k &quot;:&quot; sum[k]&#125;&apos; ssh_password.log  | sort -n -r -k 2 -t &apos;:&apos; | head -n 5</span><br><span class="line">admin_admin:478249</span><br><span class="line">admin_1234:478020</span><br><span class="line">admin_pfsense:477780</span><br><span class="line">admin_12345:477733</span><br><span class="line">admin_admin1234:477222</span><br></pre></td></tr></table></figure><p>数据很有意思，也确实是我们最常用的那些用户名密码。</p><h2 id="关于安全"><a href="#关于安全" class="headerlink" title="关于安全"></a>关于安全</h2><p>简单通过修改 ssh 端口号的方式也不是最为稳妥的方法，真正安全的做法应该是：</p><ol><li>配置 ssh 密钥</li><li>禁用密码登录</li><li>配置 ssh ip 登录白名单</li></ol><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>我把我的蜜罐中采到的蜂蜜分享出来，下载链接：<a href="http://developer.jpanj.com/ssh_password.log" target="_blank" rel="noopener">http://developer.jpanj.com/ssh_password.log</a></p><p>大家一起来享用这份喜悦吧。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;蜜罐指具有缺陷的，用于吸引网络的计算机病毒侵占以便用于病毒的研究和破解的计算机。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;互联网就像一个黑暗丛林，当你拥有一个面向公网的服务器时，永远不知道
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Dockerfile 下 ADD 与 COPY 的区别</title>
    <link href="https://jpanj.com/2019/dockerfile-add-vs-copy/"/>
    <id>https://jpanj.com/2019/dockerfile-add-vs-copy/</id>
    <published>2019-11-27T01:15:14.000Z</published>
    <updated>2019-11-27T01:27:52.897Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.png" alt></p><p><code>COPY</code> 和 <code>ADD</code> 都是 <code>Dockerfile</code> 中的指令，有着类似的作用。它们允许我们将文件从特定位置复制到 Docker 镜像中。</p><h2 id="COPY"><a href="#COPY" class="headerlink" title="COPY"></a>COPY</h2><p><code>COPY</code> 指令从 <code>&lt;src&gt;</code> 复制新的文件或目录，并将它们添加到 Docker 容器文件系统的 <code>&lt;dest&gt;</code> 的路径下。</p><p><code>COPY</code> 有两种格式：</p><ul><li><code>COPY [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</code></li><li><code>COPY [--chown=&lt;user&gt;:&lt;group&gt;] [&quot;&lt;src&gt;&quot;,... &quot;&lt;dest&gt;&quot;]</code>（包含空格的路径使用这种格式）</li></ul><h2 id="ADD"><a href="#ADD" class="headerlink" title="ADD"></a>ADD</h2><p><code>ADD</code> 有两种格式：</p><ul><li><code>ADD [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</code></li><li><code>ADD [--chown=&lt;user&gt;:&lt;group&gt;] [&quot;&lt;src&gt;&quot;,... &quot;&lt;dest&gt;&quot;]</code>（包含空格的路径使用这种格式）</li></ul><h2 id="从-URL-复制的-Dockerfile-最佳实践"><a href="#从-URL-复制的-Dockerfile-最佳实践" class="headerlink" title="从 URL 复制的 Dockerfile 最佳实践"></a>从 URL 复制的 Dockerfile 最佳实践</h2><p>通过 URL 进行复制的效率通常很低，最佳实践是使用其他策略来包含所需的远程文件。</p><p><code>COPY</code> 只支持基础的复制：将本地文件复制到容器中。</p><p>而 ADD 有一些额外的功能 ：</p><ul><li>ADD 指令可以让你使用 URL 作为 <code>&lt;src&gt;</code> 参数。当遇到 URL 时候，可以通过 URL 下载文件并且复制到 <code>&lt;dest&gt;</code>。</li><li>ADD 的另一个特性是自动解压文件的能力。如果 <code>&lt;src&gt;</code> 参数是一个可识别压缩格式（tar, gzip, bzip2, …）的<strong>本地文件</strong>（<em>注：无法实现同时下载并解压</em>），就会被解压到指定容器文件系统的路径 <code>&lt;dest&gt;</code> 下。</li></ul><p>因此，<code>ADD</code> 的最佳用途是将本地压缩包文件自动提取到镜像中：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ADD code.tar.gz /app/</span><br></pre></td></tr></table></figure><p>由于镜像的体积很重要，所以强烈建议不要使用 <code>ADD</code> 从远程 URL 获取文件，我们应该使用 <code>curl</code> 或 <code>wget</code> 来代替。这样我们可以在解压后删除这些不再需要的文件，同时还也可以避免在镜像中生成额外的层。</p><p>我们应该避免以下操作：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ADD http://example.com/big.tar.xz /usr/src/things/</span><br><span class="line">RUN tar -xJf /usr/src/things/big.tar.xz -C /usr/src/things \</span><br><span class="line">    &amp;&amp; make -C /usr/src/things all \</span><br><span class="line">    &amp;&amp; rm -f /usr/src/things/big.tar.xz</span><br></pre></td></tr></table></figure><p>这个压缩包解压后，<code>rm</code> 命令处于独立的镜像层。</p><p>我们可以这样做：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">RUN mkdir -p /usr/src/things \</span><br><span class="line">    &amp;&amp; curl -SL http://example.com/big.tar.xz \</span><br><span class="line">    | tar -xJC /usr/src/things \</span><br><span class="line">    &amp;&amp; make -C /usr/src/things all</span><br></pre></td></tr></table></figure><p><code>curl</code> 会下载这个压缩包并通过管道传给 <code>tar</code> 命令进行解压，这样也就不会在文件系统中留下这个压缩文件了。</p><p>对于不需要自动解压的文件或目录，应该始终使用 <code>COPY</code>。</p><p>最后，认准一个原则：总是使用 <code>COPY</code>（除非我们明确需要 <code>ADD</code>）。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.png&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;COPY&lt;/code&gt; 和 &lt;code&gt;ADD&lt;/code&gt; 都是 &lt;code&gt;Dockerfile&lt;/code&gt; 中的指令，有着类似的作用。它们允许我们将文件从特定位置复制到 Docker 镜像中。&lt;/
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>如何在 Python 中判断列表是否为空</title>
    <link href="https://jpanj.com/2019/how-to-check-if-a-list-is-empty-in-python/"/>
    <id>https://jpanj.com/2019/how-to-check-if-a-list-is-empty-in-python/</id>
    <published>2019-11-24T03:39:19.000Z</published>
    <updated>2019-11-27T01:14:16.077Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>在判断列表是否为空时，你更喜欢哪种方式？决定因素是什么？</p></blockquote><p><img src="1.jpg" alt></p><p>在 Python 中有很多检查列表是否是空的方式，在讨论解决方案前，先说一下不同方法涉及到的不同因素。</p><p>我们可以把判断表达式分为两个阵营：</p><ol><li>对空列表的显式比较</li><li>对空列表的隐式求值</li></ol><p>这是什么意思？</p><h2 id="显式比较"><a href="#显式比较" class="headerlink" title="显式比较"></a>显式比较</h2><p>我们从显式比较开始说起，无论我们使用列表符号 <code>[]</code> 还是声明空列表的函数 <code>list()</code>，遵循的策略是查看待检查列表是否与空列表完全相等。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 都是用来创建空列表</span><br><span class="line">a = []</span><br><span class="line">b = list()</span><br><span class="line">print(a == b) # True</span><br></pre></td></tr></table></figure><p>另外，我们可以使用 len() 函数返回列表中的元素个数。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">a = []</span><br><span class="line">if len(a) == 0:</span><br><span class="line">   print(&quot;The list is empty&quot;)</span><br></pre></td></tr></table></figure><h2 id="隐式求值"><a href="#隐式求值" class="headerlink" title="隐式求值"></a>隐式求值</h2><p>和显式比较相反，隐式求值遵循的策略是：将空列表求值为布尔值的 <code>False</code>，将有元素填充的列表求值为布尔值的 <code>True</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">a = []</span><br><span class="line">b = [1]</span><br><span class="line">if a:</span><br><span class="line">   print(&quot;Evaluated True&quot;)</span><br><span class="line">else:</span><br><span class="line">   print(&quot;Evaluated False&quot;)</span><br><span class="line">if b:</span><br><span class="line">   print(&quot;Evaluated True&quot;)</span><br><span class="line">else:</span><br><span class="line">   print(&quot;Evaluated False&quot;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"># 输出</span><br><span class="line">Evaluated False</span><br><span class="line">Evaluated True</span><br></pre></td></tr></table></figure><p>那么，显式比较和隐式求值有什么区别呢？</p><p>很多人习惯于使用显式比较的方式。但是如果你遵循鸭子类型的设计风格，那么会更加偏向于使用的是隐式方法。</p><h2 id="什么是鸭子类型"><a href="#什么是鸭子类型" class="headerlink" title="什么是鸭子类型"></a>什么是鸭子类型</h2><p>「鸭子类型」这个词来自以下短语：</p><blockquote><p>当看到一只鸟走起来像鸭子、游泳起来像鸭子、叫起来也像鸭子，那么这只鸟就可以被称为鸭子。</p></blockquote><p>从功能上讲，这是对对象实际数据类型压力较小的一种确认。在鸭子类型中，关注点在于对象的行为，能做什么（比如，可迭代 iterable）；而不是关注对象所属的类型。鸭子类型在动态语言中经常使用，非常灵活。</p><p>鸭子类型优先考虑便利性而非安全性，从而可以使用更灵活的代码来适应更广泛的用途，它不会像传统方式那么严格。</p><h2 id="我们应该使用哪种方式？"><a href="#我们应该使用哪种方式？" class="headerlink" title="我们应该使用哪种方式？"></a>我们应该使用哪种方式？</h2><p>当我们越了解隐式求值，就越倾向于使用这种方式，因为我们知道空列表将被求值为 <code>False</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">a = []</span><br><span class="line">print(bool(a)) # False</span><br></pre></td></tr></table></figure><p>这使得我们可以合并那些很长的检查表达式，如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># 之前</span><br><span class="line">if isinstance(a, list) and len(a) &gt; 0:</span><br><span class="line">   print(&quot;Processing list...&quot;)</span><br><span class="line"># 之后</span><br><span class="line">if a:</span><br><span class="line">   print(&quot;Processing list...&quot;)</span><br></pre></td></tr></table></figure><p>当然，最终的选择还取决于本次判断的意图：</p><ul><li>如果你检查空列表是为了对其进行迭代，那么隐式求值是更合适的方法。</li><li>如果你检查空列表是为了在之后调用列表中的方法，那么可以考虑使用显式比较来同时验证数据类型。</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;在判断列表是否为空时，你更喜欢哪种方式？决定因素是什么？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;在 Python 中有很多检查列表是否是空的方式，在讨论解决方案前，先说一下不同方法涉及到
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>五分钟彻底解决 OutOfMemoryError: Unable to create native threads</title>
    <link href="https://jpanj.com/2019/Unable-to-create-native-threads/"/>
    <id>https://jpanj.com/2019/Unable-to-create-native-threads/</id>
    <published>2019-11-22T11:23:33.000Z</published>
    <updated>2019-11-24T04:52:40.885Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>作为 Java 程序员，我们几乎都会碰到 java.lang.OutOfMemoryError 异常。</p></blockquote><p><img src="1.jpeg" alt></p><p>JVM 在抛出 <code>java.lang.OutOfMemoryError</code> 时，除了会打印出一行描述信息，还会打印堆栈跟踪，因此我们可以通过这些信息来找到导致异常的原因。</p><p>其中一种异常是 <code>java.lang.OutOfMemoryError: Unable to create native threads</code>，我们通过这篇文章来彻底搞懂它。</p><p>抛出这个异常的过程大概是这样的：</p><ol><li>Java 程序向 JVM 请求创建一个新的 Java 线程。</li><li>JVM 本地代码（Native Code）代理该请求，通过调用操作系统 API 去创建一个操作系统级别的线程 Native Thread。</li><li>操作系统尝试创建一个新的 Native Thread，需要同时分配一些内存给该线程，每一个 Native Thread 都有一个线程栈，线程栈的大小由 JVM 参数-Xss决定。</li><li>由于各种原因，操作系统创建新的线程可能会失败，下面会详细谈到。</li><li>JVM 抛出 <code>java.lang.OutOfMemoryError: Unable to create new native thread</code> 错误。</li></ol><p>因此关键在于第四步线程创建失败，JVM 就会抛出 <code>OutOfMemoryError</code>，那具体有哪些因素会导致线程创建失败呢？</p><ol><li>JVM 内存大小限制</li><li><code>ulimit -u</code> 限制</li><li>参数 <code>sys.kernel.threads-max</code> 限制</li><li>参数 <code>sys.kernel.pid_max</code> 限制</li></ol><p>第一种失败原因简单说一下：Java 创建一个线程需要消耗一定的栈空间，并通过-Xss参数指定。需要注意的是栈空间如果过小，可能会导致 <code>StackOverflowError</code>，尤其是在递归调用的情况下，但是栈空间过大会占用过多内存。</p><p>同时还要注意，对于一个 32 位 Java 应用来说，用户进程空间是 4GB，内核占用 1GB，那么用户空间就剩下 3GB，因此它能创建的线程数大致可以通过这个公式算出来：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Max memory（3GB） = [-Xmx] + [-XX:MaxMetaSpaceSize] + number_of_threads * [-Xss]</span><br></pre></td></tr></table></figure><p>不过对于 64 位的应用，由于虚拟进程空间近乎无限大，因此不会因为线程栈过大而耗尽虚拟地址空间。但是请你注意，64 位的 Java 进程能分配的最大内存数仍然受物理内存大小的限制。</p><p>下边重点来介绍后边三种失败因素。</p><p>在搞明白 <code>pid_max</code>、<code>ulimit -u</code> 和 <code>thread_max</code> 的区别前，需要先明白进程和线程之间的区别。</p><blockquote><p>一个最典型的区别是，（同一个进程内的）线程运行时共享内存空间，而进程在独立的内存空间中运行。</p></blockquote><h2 id="pid-max"><a href="#pid-max" class="headerlink" title="pid_max"></a>pid_max</h2><p><code>pid_max</code> 参数表示系统全局的 <code>PID</code> 号数值的限制，每一个进程都有 ID，ID 的值超过这个数，进程就会创建失败，<code>pid_max</code> 参数可以通过以下命令查看：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/sys/kernel/pid_max</span><br></pre></td></tr></table></figure><p>默认情况下，执行以上命令返回 <strong>32768</strong>，这意味着我们可以在系统中同时运行 <strong>32768</strong> 个<strong>进程</strong>，这些<strong>进程</strong>可以在<strong>独立的内存空间中</strong>运行。</p><h3 id="修改方法"><a href="#修改方法" class="headerlink" title="修改方法"></a>修改方法</h3><p><code>echo 65535 &gt; /proc/sys/kernel/pid_max</code></p><p>上面只是临时生效，重启机器后会失效</p><p>永久生效方法：</p><p>在 <code>/etc/sysctl.conf</code> 中添加 <code>kernel.pid_max = 65535</code></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">vi /etc/sysctl.conf</span><br><span class="line">kernel.pid_max = 65535</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">echo &quot;kernel.pid_max = 65535&quot; &gt;&gt; /etc/sysctl.conf</span><br></pre></td></tr></table></figure><h2 id="threads-max"><a href="#threads-max" class="headerlink" title="threads-max"></a>threads-max</h2><p><code>treamd-max</code> 用来限制操作系统全局的线程数，通过以下命令查看 <code>treamd-max</code> 参数：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/sys/kernel/threads-max</span><br></pre></td></tr></table></figure><p>上边的命令返回 <strong>126406</strong>，这意味着我可以在共享内存空间中拥有 <strong>126406</strong> 个<strong>线程</strong>。</p><h2 id="ulimit-u"><a href="#ulimit-u" class="headerlink" title="ulimit -u"></a>ulimit -u</h2><p><code>limit -u</code> 表示当前用户可以运行的最大<strong>进程数</strong>。</p><h3 id="这个值怎么来的"><a href="#这个值怎么来的" class="headerlink" title="这个值怎么来的"></a>这个值怎么来的</h3><p><code>root</code> 账号下 <code>ulimit -u</code> 得到的值默认是 <code>cat /proc/sys/kernel/threads-max</code> 的值 / 2，即系统线程数的一半。</p><p>普通账号下 <code>ulimit -u</code> 得到的值默认是 <code>/etc/security/limits.d/20-nproc.conf</code>文件中指定的：</p><p><img src="1.png" alt></p><h3 id="修改方式"><a href="#修改方式" class="headerlink" title="修改方式"></a>修改方式</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ulimit -u 65535</span><br></pre></td></tr></table></figure><h3 id="ulimit-受到全局限制"><a href="#ulimit-受到全局限制" class="headerlink" title="ulimit 受到全局限制"></a>ulimit 受到全局限制</h3><p><code>ulimit -u</code> 的值受全局的 <code>kernel.pid_max</code> 的值限制。也就是说如果 <code>kernel.pid_max=1024</code>，那么即使你的 <code>ulimit -u</code> 的值是 63203，用户能打开的最大进程数还是 1024。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;作为 Java 程序员，我们几乎都会碰到 java.lang.OutOfMemoryError 异常。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpeg&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;JVM 在抛出 &lt;code&gt;java.la
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Docker 部署 Go 服务并实现热加载</title>
    <link href="https://jpanj.com/2019/go-docker-reload/"/>
    <id>https://jpanj.com/2019/go-docker-reload/</id>
    <published>2019-11-21T01:52:24.000Z</published>
    <updated>2019-11-24T04:52:27.575Z</updated>
    
    <content type="html"><![CDATA[<p>Docker 足够轻量、也非常易用，并且可以确保我们所有的运行环境保持一致。</p><p>在这篇文章中，我将通过创建 Docker 容器来部署一个 Go API 服务。当我对源码进行修改时，这个 Go 服务也会立即重新加载。</p><p>通过这个方式我们就不需要再在开发过程中多次重新编译 Docker 镜像了。</p><p><img src="0.png" alt></p><h2 id="创建-Go-模块"><a href="#创建-Go-模块" class="headerlink" title="创建 Go 模块"></a>创建 Go 模块</h2><p>官方在 Go 的 1.13 版本中介绍了模块的概念。这意味着我们不再需要把整个工程放在 Go 的工作空间下了。</p><p>开始前，我创建一个新的目录 <code>go-docker</code> 来放置所有文件。</p><p>然后初始化一个 Git 仓库并创建 Go 模块。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git init</span><br><span class="line">git remote add origin git@github.com:Panmax/go-docker.git</span><br><span class="line">go mod init github.com/Panmax/go-docker</span><br></pre></td></tr></table></figure><p>你会看到在项目目录下出现了一个 <code>go.mod</code> 文件。这个文件将存有这个模块下所有的依赖，类似于 Node 开发中用到的 <code>package.json</code> 或 Python 中的 <code>requirements.txt</code>。</p><h2 id="构建-API"><a href="#构建-API" class="headerlink" title="构建 API"></a>构建 API</h2><p>模块设置好了，现在来构建一个简单的 API 服务。</p><p>我准备在构建这个 API 服务时使用 <code>gorilla/mux</code> 路由包。我也可以只用 Go 中提供的标准模块来实现路由，但我想确保模块依赖可以按照预期工作，并且利用 <code>mux</code> 可以支持我们构建更加复杂的应用。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">go get -u github.com/gorilla/mux</span><br></pre></td></tr></table></figure><p>执行这个命令后，你会看到它被作为依赖写入了 <code>go.mod</code> 文件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">### module github.com/Panmax/go-docker</span><br><span class="line"></span><br><span class="line">go 1.13</span><br><span class="line"></span><br><span class="line">require github.com/gorilla/mux v1.7.3 // indirect</span><br></pre></td></tr></table></figure><p>接下来，创建这个 Go 项目的主文件 <code>commands/runserver.go</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">&quot;fmt&quot;</span><br><span class="line">&quot;github.com/gorilla/mux&quot;</span><br><span class="line">&quot;net/http&quot;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">r := mux.NewRouter()</span><br><span class="line"></span><br><span class="line">r.HandleFunc(&quot;/&quot;, func(w http.ResponseWriter, r *http.Request) &#123;</span><br><span class="line">fmt.Fprintf(w, &quot;Hello, World!&quot;)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">fmt.Println(&quot;Server listening!&quot;)</span><br><span class="line">http.ListenAndServe(&quot;:80&quot;, r)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个 API 只是简单返回一条消息：「Hello World!」</p><p>在把这个程序放进 Docker 容器前我们最好先来测试一下。通过 <code>go run</code> 命令来运行这个服务。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">go run commands/runserver.go</span><br><span class="line">Server listening!</span><br></pre></td></tr></table></figure><p><img src="1.png" alt></p><p>API 服务可以正常工作。</p><h2 id="配置-Docker"><a href="#配置-Docker" class="headerlink" title="配置 Docker"></a>配置 Docker</h2><p>我们开始为这个项目构建 Docker 镜像。Docker 镜像包含一组用来告诉 Docker 需要提供什么环境的指令。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">FROM golang:latest</span><br><span class="line"></span><br><span class="line">WORKDIR /app</span><br><span class="line"></span><br><span class="line">COPY ./ /app</span><br><span class="line"></span><br><span class="line">RUN go mod download</span><br><span class="line"></span><br><span class="line">ENTRYPOINT go run commands/runserver.go</span><br></pre></td></tr></table></figure><p>使用 <code>golang:latest</code> 镜像作为这个自定义镜像的基础镜像。这样就可以免去 Go 开发环境的配置。</p><p>将整个项目拷贝到了镜像的 <code>/app</code> 目录下，然后通过 <code>go mod download</code> 下载依赖。</p><p>最后，我们告诉 Docker 执行 <code>go run commands/runserver.go</code> 命令来启动服务。</p><p>执行以下命令来构建这个镜像：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker build -t go-docker-image .</span><br></pre></td></tr></table></figure><p>现在我已经构建好了 Docker 镜像，接下来我们实际启动一下这个 Docker。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker run go-docker-image</span><br><span class="line">Server listening!</span><br></pre></td></tr></table></figure><p>服务已经监听在了 Docker 容器中，但是当我通过浏览器中打开 <code>localhost</code> 时却发现无法访问。</p><p><img src="2.png" alt></p><p>出现这个情况的原因是，虽然程序在 Docker 容器内监听了 80 端口的传入请求，但是它并没有在宿主机的 80 端口上进行监听。因此我们给 <code>localhsot</code> 发送一个 GET 请求，它是找不到正在运行的服务的。</p><p>我用一张逻辑图来表述一下这个问题：</p><p><img src="3.png" alt></p><p>为了解决这个问题，我们需要把容器内的 80 端口映射到主机的 80 端口。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -p 80:80 go-docker-image</span><br></pre></td></tr></table></figure><p>端口映射后的逻辑图如下：</p><p><img src="4.png" alt></p><p>现在再来访问 <code>localhost</code>，就可以看到「Hello World!」显示在了页面上。</p><h2 id="修改源码"><a href="#修改源码" class="headerlink" title="修改源码"></a>修改源码</h2><p>我们来对这个 API 做一点调整：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">&quot;fmt&quot;</span><br><span class="line">&quot;github.com/gorilla/mux&quot;</span><br><span class="line">&quot;net/http&quot;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">r := mux.NewRouter()</span><br><span class="line"></span><br><span class="line">r.HandleFunc(&quot;/&quot;, func(w http.ResponseWriter, r *http.Request) &#123;</span><br><span class="line">fmt.Fprintf(w, &quot;Hello, World!\n世界，你好！&quot;)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">fmt.Println(&quot;Server listening!&quot;)</span><br><span class="line">http.ListenAndServe(&quot;:80&quot;, r)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我在这个 API 的返回结果中新加了一行消息，我们再来启动一个新的 Docker 容器。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -p 80:80 go-docker-image</span><br></pre></td></tr></table></figure><p>但是如果我现在访问 <code>localhost</code>，看到的仍然是旧消息。</p><p><img src="1.png" alt></p><p>这是因为 Docker 镜像没有变化。为了使变更生效，我们必须重新构建这个镜像。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker build -t go-docker-image .</span><br><span class="line">docker run -p 80:80 go-docker-image</span><br></pre></td></tr></table></figure><p>现在就可以看到更新后的消息了。</p><p><img src="5.png" alt></p><h2 id="配置热加载"><a href="#配置热加载" class="headerlink" title="配置热加载"></a>配置热加载</h2><p>每次对代码修改后，重新构建 Docker 镜像会花费很长时间，我们来让这个系统更好用一点。</p><p>我要使用的是 <a href="https://github.com/githubnemo/CompileDaemon" target="_blank" rel="noopener">Compile Daemon</a> 包。如果有任何 Go 源码发生了变更，这个包会重新编译并重启我们的 Go 程序。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">FROM golang:latest</span><br><span class="line"></span><br><span class="line">WORKDIR /app</span><br><span class="line"></span><br><span class="line">COPY ./ /app</span><br><span class="line"></span><br><span class="line">RUN go mod download</span><br><span class="line"></span><br><span class="line">RUN go get github.com/githubnemo/CompileDaemon</span><br><span class="line"></span><br><span class="line">ENTRYPOINT CompileDaemon --build=&quot;go build commands/runserver.go&quot; --command=./runserver</span><br></pre></td></tr></table></figure><p>我修改了 <code>Dockerfile</code> 来下载 <code>CompileDaemon</code> 包。</p><p>之后修改了 <code>ENTRYPOINT</code> 后面的命令来运行 <code>CompileDaemon</code> 程序，同时为它指定了项目编译和服务启动命令。每次有文件变化后，以上命令就会被执行。</p><p>重新编译这个镜像：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker build -t go-docker-image .</span><br></pre></td></tr></table></figure><p>启动 Docker 时，我添加了 <code>-v ~/Projects/go-docker:/app</code> 参数。这样就可以把我本机的 <code>go-docker</code> 目录挂载到 Docker 容器内的 <code>/app</code> 目录下了。</p><p>当我修改了本机 <code>go-docker</code> 目录内的文件时，容器 <code>/app</code> 目录下的文件也会变化。</p><p>最终的启动命令如下。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -v ~/Projects/go-docker:/app -p 80:80 go-docker-image</span><br></pre></td></tr></table></figure><p>容器运行过程中，尝试修改源码，你会看到更改自动生效了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">2019/11/20 11:59:53 Running build command!</span><br><span class="line">2019/11/20 11:59:53 Build ok.</span><br><span class="line">2019/11/20 11:59:53 Hard stopping the current process..</span><br><span class="line">2019/11/20 11:59:53 Restarting the given command.</span><br><span class="line">2019/11/20 11:59:53 stdout: Server listening!</span><br><span class="line">2019/11/20 12:00:24 Running build command!</span><br><span class="line">2019/11/20 12:00:25 Build ok.</span><br><span class="line">2019/11/20 12:00:25 Hard stopping the current process..</span><br><span class="line">2019/11/20 12:00:25 Restarting the given command.</span><br><span class="line">2019/11/20 12:00:25 stdout: Server listening!</span><br></pre></td></tr></table></figure><h2 id="使用-Docker-Compose"><a href="#使用-Docker-Compose" class="headerlink" title="使用 Docker Compose"></a>使用 Docker Compose</h2><p>每次运行容器时，我都要输入很长的启动命令：<code>docker run -v ~/Projects/go-docker:/app -p 80:80 go-docker-image</code>。在这个项目中倒没有太大问题，毕竟我才只有一个容器要启动。</p><p>但假设我有一个需要启动很多容器的项目，执行多个 <code>docker run</code> 命令会非常麻烦。</p><p>解决方案是使用 Docker Compose。利用这个工具，我们可以指定运行 <code>docker-compose up</code> 命令时要启动哪些容器。</p><p>为了配置它，我们需要创建一个 <code>docker-compose.yml</code> 文件：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">version: &quot;3&quot;</span><br><span class="line">services:</span><br><span class="line">  go-docker-image:</span><br><span class="line">    build: ./</span><br><span class="line">    ports:</span><br><span class="line">      - &apos;80:80&apos;</span><br><span class="line">    volumes:</span><br><span class="line">      - ./:/app</span><br></pre></td></tr></table></figure><p>这里，我声明了要创建一个名为 <code>go-docker-image</code> 的镜像。这个镜像使用当前目录下的 <code>Dockerfile</code> 来构建。同时我配置了端口映射和目录挂载。</p><p>执行 <code>docker-compose up</code> 来启动 <code>docker-compose.yml</code> 中指定的容器。</p><p>现在，我有了一个运行在 Docker 内的 API 服务，与此同时，当代码变化时这个服务也会自动重新加载。</p><p>可以在这里查看项目源码：<a href="https://github.com/Panmax/go-docker" target="_blank" rel="noopener">https://github.com/Panmax/go-docker</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Docker 足够轻量、也非常易用，并且可以确保我们所有的运行环境保持一致。&lt;/p&gt;
&lt;p&gt;在这篇文章中，我将通过创建 Docker 容器来部署一个 Go API 服务。当我对源码进行修改时，这个 Go 服务也会立即重新加载。&lt;/p&gt;
&lt;p&gt;通过这个方式我们就不需要再在开发
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>用一个现实世界中的例子介绍 Go 接口</title>
    <link href="https://jpanj.com/2019/real-world-go-interface/"/>
    <id>https://jpanj.com/2019/real-world-go-interface/</id>
    <published>2019-11-19T01:48:29.000Z</published>
    <updated>2019-11-19T01:51:30.982Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.jpeg" alt></p><p>假设我现在要用 Go 编写一个 Web 应用。在这个应用里，我要实现给用户发送消息的功能。我可以通过邮件或短信等方式来发送这条消息，这是一个完美的接口使用场景。</p><p>在这个虚构的 Web 应用中，先来创建如下 <code>main.go</code> 文件：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import &quot;fmt&quot;</span><br><span class="line"></span><br><span class="line">type User struct &#123;</span><br><span class="line">Name string</span><br><span class="line">Email string</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">type UserNotifier interface &#123;</span><br><span class="line">SendMessage(user *User, message string) error</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">user := &amp;User&#123;&quot;Panmax&quot;, &quot;panmax@email.com&quot;&#125;</span><br><span class="line"></span><br><span class="line">fmt.Printf(&quot;Welcome %s\n&quot;, user.Name)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>User</code> 结构体代表一个用户。</p><p>可以看到我创建了只有一个函数 <code>SendMessage()</code> 的 <code>UserNotifier</code> 接口。</p><p>为了实现这个接口，我需要创建一个结构体来实现 <code>SendMessage()</code> 函数。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">type EmailNotifier struct &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (notifier EmailNotifier) SendMessage(user *User, message string) error &#123;</span><br><span class="line">_, err := fmt.Printf(&quot;Sending email to %s with content %s\n&quot;, user.Name, message)</span><br><span class="line">return err</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>正如你看到的，我创建了一个新的 <code>EmailNotifier</code> 结构体。然后我给这个结构体实现了 <code>SendMessage()</code> 方法。在这个例子中，<code>EmailNotifier</code> 只是简单打印了一条消息。在现实世界中你可能需要调用发送邮件的 API，比如 <a href="https://www.mailgun.com/" target="_blank" rel="noopener">Mailgun</a>。</p><p>到此，<code>UserNotifier</code> 接口已经实现了，就是这么简单。</p><p>下一步要做的是使用 <code>UserNotifier</code> 接口为用户发送一份邮件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">user := User&#123;&quot;Panmax&quot;, &quot;panmax@email.com&quot;&#125;</span><br><span class="line">fmt.Printf(&quot;Welcome %s\n&quot;, user.Name)</span><br><span class="line"></span><br><span class="line">var userNotifier UserNotifier</span><br><span class="line">userNotifier = EmailNotifier&#123;&#125;</span><br><span class="line">userNotifier.SendMessage(&amp;user, &quot;Interfaces all the way!&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行这个程序，<code>EmailNotifier</code> 的 <code>SendMessage</code> 方法被正确调用了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">go build -o main main.go</span><br><span class="line">./main</span><br><span class="line">Welcome Panmax</span><br><span class="line">Sending email to Panmax with content Interfaces all the way!</span><br></pre></td></tr></table></figure><p>下边我们来实现发送短信的接口。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">type SmsNotifier struct &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (notifier SmsNotifier) SendMessage(user *User, message string) error &#123;</span><br><span class="line">_, err := fmt.Printf(&quot;Sending SMS to %s with content %s\n&quot;, user.Name, message)</span><br><span class="line">return err</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们可以把 <code>Notifier</code> 放进用户结构体中，这样每个用户都有一个属于自己的 <code>Notifier</code>，是不是很酷。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">type User struct &#123;</span><br><span class="line">Name string</span><br><span class="line">Email string</span><br><span class="line">Notifier UserNotifier</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后，我们向 <code>User</code> 结构体中添加一个便捷方法 <code>notify()</code>，这个方法使用 <code>UserNotifier</code> 接口给用户发送消息。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">func (user *User) notify(message string) error &#123;</span><br><span class="line">return user.Notifier.SendMessage(user, message)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后，我在 <code>main()</code> 函数中创建两个用户，分别调用了它们的 <code>notify()</code> 方法。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">user1 := User&#123;&quot;Dirk&quot;, &quot;dirk@email.com&quot;, EmailNotifier&#123;&#125;&#125;</span><br><span class="line">user2 := User&#123;&quot;Justin&quot;, &quot;bieber@email.com&quot;, SmsNotifier&#123;&#125;&#125;</span><br><span class="line"></span><br><span class="line">user1.notify(&quot;Welcome Email user!&quot;)</span><br><span class="line">user2.notify(&quot;Welcome SMS user!&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最终结果正是我们所预期的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">go build -o main main.go</span><br><span class="line">./main</span><br><span class="line">Sending email to Panmax with content Welcome Email user!</span><br><span class="line">Sending SMS to Panmax with content Welcome SMS user!</span><br></pre></td></tr></table></figure><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本文介绍了 Go 接口是如何工作的，同时用一个现实中简单的例子进行了演示。</p><p>希望对你有所帮助。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.jpeg&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;假设我现在要用 Go 编写一个 Web 应用。在这个应用里，我要实现给用户发送消息的功能。我可以通过邮件或短信等方式来发送这条消息，这是一个完美的接口使用场景。&lt;/p&gt;
&lt;p&gt;在这个虚构的 Web 应用中，先来创建
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>不到 40 行代码实现 Telegram 自动发消息机器人</title>
    <link href="https://jpanj.com/2019/telegram-bot-send-taily-message/"/>
    <id>https://jpanj.com/2019/telegram-bot-send-taily-message/</id>
    <published>2019-11-16T04:45:06.000Z</published>
    <updated>2019-11-16T04:48:06.508Z</updated>
    
    <content type="html"><![CDATA[<p><img src="head.png" alt></p><p><em>创建一个 Telegram 机器人，定时发送消息，并部署到 AWS Lambda。</em></p><blockquote><p>AWS Lambda 是一项计算服务，可使你无需预配置或管理服务器即可运行代码。</p></blockquote><h2 id="我们要做什么？"><a href="#我们要做什么？" class="headerlink" title="我们要做什么？"></a>我们要做什么？</h2><ul><li>创建一个 Telegram 机器人</li><li>自动发送日常信息</li><li>把它部署到 <a href="https://aws.amazon.com/lambda/" target="_blank" rel="noopener">AWS Lambda</a></li></ul><h2 id="需要准备什么？"><a href="#需要准备什么？" class="headerlink" title="需要准备什么？"></a>需要准备什么？</h2><ul><li>一个 Telegram 帐号</li><li>Python 3.6</li><li>Node.js</li><li>一个 AWS 帐号</li></ul><p>AWS Lambda 可以在一定配额内免费使用，所以需要避免发送大量请求。</p><p>AWS Lambda 定价方案如下：</p><p><img src="6.png" alt></p><h2 id="创建机器人"><a href="#创建机器人" class="headerlink" title="创建机器人"></a>创建机器人</h2><p>待办清单上的第一件事是创建一个机器人，遵循 Telegram 官方说明：</p><ul><li>在 Teletram 中搜索用户 <code>@BotFather</code>。</li><li>发送命令 <code>/newbot</code> 并为你的机器人指定 <code>name</code> 和 <code>username</code>。</li><li>拿到 token 并记录在一个安全的地方，后边会用到。</li></ul><p>现在机器人准备好了，开始编写代码。</p><h2 id="准备部署设施"><a href="#准备部署设施" class="headerlink" title="准备部署设施"></a>准备部署设施</h2><p>有很多部署 Lambda 的方法，我准备使用 <a href="https://serverless.com/" target="_blank" rel="noopener">serverless</a> 框架，所以我们先来安装它：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ npm install serverless --global</span><br></pre></td></tr></table></figure><p>Serverless 的文档中提供了<a href="https://serverless.com/framework/docs/providers/aws/examples/hello-world/python/" target="_blank" rel="noopener">一些范例</a>，还支持生成模板，像下边这样：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serverless create --template aws-python3 --path scheduled_telegram_bot</span><br></pre></td></tr></table></figure><p>执行这个命令后，会创建出一个 <code>scheduled_telegram_bot</code> 目录，并已经生成了 3 个文件：<br><code>.gitignore</code>，<code>serverless.yml</code> 和 <code>handler.py</code>。</p><p><code>serverless.yml</code> 文件用来描述：部署什么、何时运行、如何运行。 <code>handler.py</code> 文件包含将要运行的代码，所以我们先来编写它。</p><h2 id="编写代码"><a href="#编写代码" class="headerlink" title="编写代码"></a>编写代码</h2><p>我们将使用是一个封装好的包来调用 Telegram 的 API：<a href="https://github.com/python-telegram-bot/python-telegram-bot" target="_blank" rel="noopener">python-teletram-bot</a>，创建一个新的文件 <code>requirements.txt</code> 写入：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python-telegram-bot==12.2.0</span><br></pre></td></tr></table></figure><p>我们需要在程序中导入这个库，不过后边我们会遇到一个问题：由于 <code>python-telegram-bot</code> 不是 AWS Lambda 所提供的标准库，因此我们在部署时需要同时包含这个包中的文件。</p><p>所以我们后边会在本地安装这个包的所有内容。现在让我们来定义一个发送消息的函数，打开 <code>handler.py</code> 修改内容如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">import telegram</span><br><span class="line">import os</span><br><span class="line"></span><br><span class="line">TOKEN = os.environ[&apos;TELEGRAM_TOKEN&apos;]</span><br><span class="line">CHAT_ID = 000000  # Change this</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def send_message(event, context):</span><br><span class="line">    bot = telegram.Bot(token=TOKEN)</span><br><span class="line">    bot.sendMessage(chat_id=CHAT_ID, text=&apos;Hey there!&apos;)</span><br></pre></td></tr></table></figure><p>你需要把 <code>CHAT_ID</code> 修改为你想让机器人互动的群组、频道或者会话的 ID。获取 ID 的方法如下，我以频道为例：</p><p>首先创建自己的频道，将机器人拉入频道并设置为管理员，随意在频道内发送一个消息。</p><p>添加 <a href="https://telegram.me/getidsbot" target="_blank" rel="noopener">GetIDsBot</a>，并将上边发的那条消息转发给这个机器人，它会返回这个频道相关信息：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">👤 You</span><br><span class="line"> ├ id: xxxxxxx</span><br><span class="line"> ├ is_bot: false</span><br><span class="line"> ├ first_name: xxxxx</span><br><span class="line"> ├ username: xxxxxx</span><br><span class="line"> ├ language_code: zh-hans (-)</span><br><span class="line"> └ created: ~ 9/2017 (?)</span><br><span class="line"></span><br><span class="line">💬 Origin chat</span><br><span class="line"> ├ id: -1001156324531</span><br><span class="line"> ├ title: Panmax Channel</span><br><span class="line"> └ type: channel</span><br><span class="line"></span><br><span class="line">📃 Message</span><br><span class="line"> ├ message_id: 82</span><br><span class="line"> └ forward_date: Fri, 15 Nov 2019 15:38:41 GMT</span><br></pre></td></tr></table></figure><p>这样可以得到我所在这个频道的 ID 为 <code>-1001156324531</code>。</p><h2 id="部署定义"><a href="#部署定义" class="headerlink" title="部署定义"></a>部署定义</h2><p>现在我们来定义如何运行我们的代码。</p><p>编辑 <code>serverless.yml</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">service: scheduled-telegarm-bot</span><br><span class="line"></span><br><span class="line">frameworkVersion: &quot;&gt;=1.2.0 &lt;2.0.0&quot;</span><br><span class="line"></span><br><span class="line">provider:</span><br><span class="line">  name: aws</span><br><span class="line">  runtime: python3.6</span><br><span class="line">  environment:</span><br><span class="line">    TELEGRAM_TOKEN: $&#123;env:TELEGRAM_TOKEN&#125;</span><br><span class="line"></span><br><span class="line">functions:</span><br><span class="line">  cron:</span><br><span class="line">    handler: handler.send_message</span><br><span class="line">    events:</span><br><span class="line">      - schedule: cron(*/2 * * * ? *)</span><br></pre></td></tr></table></figure><p>这里我们告诉了 AWS 我们所需要的运行环境，并且让它从我们的环境变量中获取 Telegram token，这样我们就不需要把 token 硬编码到代码中了。</p><p>最后我们还定义了一个定时器，声明每两分钟触发一次这个函数。当然，定时器有很多选项，通过这个<a href="https://docs.aws.amazon.com/AmazonCloudWatch/latest/events/ScheduledEvents.html" target="_blank" rel="noopener">文档</a>可以了解更多配置方式，比如每小时或者每周一发送消息。</p><h2 id="汇总"><a href="#汇总" class="headerlink" title="汇总"></a>汇总</h2><p>我们已经准备好了所有需要的东西。</p><p>好吧，准确来说是几乎所有的东西。我们还需要获取 AWS 的凭证，然后和 token 一样，在部署前设置为环境变量，获取凭证步骤如下：</p><p>通过 AWS 的控制台：</p><p>进入 <em>我的安全凭证</em> - <em>用户</em> - <em>添加用户</em></p><p><img src="7.jpg" alt></p><p>设置用户名并选择<em>编程访问</em></p><p><img src="8.png" alt></p><p>下一步：选择<em>直接附加现有策略</em> - <em>AdministratorAccess</em></p><p><img src="9.png" alt></p><p>下一步会来到添加标签页，直接点击下一步，确认信息无误后点击 <em>创建用户</em>，将 <em>访问密钥 ID</em> 和 <em>私有访问密钥</em> 拷贝并存放起来。</p><p>现在，让我们把 AWS 凭证和 Telegram token 导出到环境变量。打开终端，输入：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ export AWS_ACCESS_KEY_ID=[your key goes here]</span><br><span class="line">$ export AWS_SECRET_ACCESS_KEY=[your key goes here]</span><br><span class="line">$ export TELEGRAM_TOKEN=[your token goes here]</span><br></pre></td></tr></table></figure><p>在本地安装 Python 的依赖包（这也是 AWS Lambda 所需要的）：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ pip3 install -r requirements.txt -t .</span><br></pre></td></tr></table></figure><p>最后将所有的东西部署到 AWS：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serverless deploy</span><br></pre></td></tr></table></figure><p>如果前边配置没有问题，会看到如下输出：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Serverless: Packaging service...</span><br><span class="line">Serverless: Excluding development dependencies...</span><br><span class="line">Serverless: Uploading CloudFormation file to S3...</span><br><span class="line">Serverless: Uploading artifacts...</span><br><span class="line">Serverless: Uploading service scheduled-telegarm-bot.zip file to S3 (5.64 MB)...</span><br><span class="line">Serverless: Validating template...</span><br><span class="line">Serverless: Updating Stack...</span><br><span class="line">Serverless: Checking Stack update progress...</span><br><span class="line">.........</span><br><span class="line">Serverless: Stack update finished...</span><br></pre></td></tr></table></figure><p>完成！机器人会在每 2 分钟给我们发送一次消息。</p><p><img src="10.png" alt></p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>AWS Python Scheduled Cron Example：<a href="https://github.com/serverless/examples/tree/master/aws-python-scheduled-cron" target="_blank" rel="noopener">https://github.com/serverless/examples/tree/master/aws-python-scheduled-cron</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;head.png&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;&lt;em&gt;创建一个 Telegram 机器人，定时发送消息，并部署到 AWS Lambda。&lt;/em&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;AWS Lambda 是一项计算服务，可使你无需预配置或管理服务器即
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>离线安装 Python requests 包</title>
    <link href="https://jpanj.com/2019/install-requests-offline/"/>
    <id>https://jpanj.com/2019/install-requests-offline/</id>
    <published>2019-11-14T09:18:14.000Z</published>
    <updated>2019-11-14T11:22:16.949Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><code>requests</code> 是一个简单优雅的 Python HTTP 库，相较于 Python 标准库中的 <code>urllib</code> 和 <code>urllib2</code>，<code>requests</code> 更加的便于理解使用。</p></blockquote><h2 id="背景介绍"><a href="#背景介绍" class="headerlink" title="背景介绍"></a>背景介绍</h2><p>由于某地区热点事件持续升温，我们的客户想要通过我们系统的搜索功能导出一批数据，目前我们的搜索结果是不支持导出的，并且搜索功能也是通过调用几个子服务后对数据进行了合并，所以无法直接通过 <code>ElasticSearch</code> 来捞数据。</p><p>我们在评估需求后，预计编写这个统计程序大概需要 1 天的时间，但是客户认为事态紧急，当天就要结果，我们本着顾客就是上帝的原则，又进行了一番讨论，结论是可以写一个类似爬虫的工具，来「爬取」我们自己的搜索接口来拿到这些数据。</p><p>用 <code>Python</code> 来实现最合适不过，而且我对编写爬虫也比较熟悉，所以就采用了最简单粗暴的方法：用 <code>requests</code> 包作为一个 <code>HTTP Client</code> 来收发请求，但是客户现场是个离线环境，之前我们也没有安装过 <code>requests</code>，所以才有了本文：在离线环境中安装 <code>requests</code>。</p><h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><h3 id="资源准备"><a href="#资源准备" class="headerlink" title="资源准备"></a>资源准备</h3><p>为方便后期使用，我将所有用到的文件打包在了一起，可直接解压使用，无需从网上东奔西走寻找资源。</p><p>压缩包内涉及到的文件如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">setuptools-41.1.0.post1.tar</span><br><span class="line">pip-19.2.2.tar.gz</span><br><span class="line">certifi-2019.9.11-py2.py3-none-any.whl</span><br><span class="line">chardet-3.0.4-py2.py3-none-any.whl</span><br><span class="line">idna-2.8-py2.py3-none-any.whl</span><br><span class="line">urllib3-1.25.7-py2.py3-none-any.whl</span><br><span class="line">requests-2.22.0.tar.gz</span><br></pre></td></tr></table></figure><p>打包资源下载链接：<a href="http://developer.jpanj.com/requests-offline.tar.gz" target="_blank" rel="noopener">http://developer.jpanj.com/requests-offline.tar.gz</a></p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>解压 <code>requests-offline.tar.gz</code> 后进入 requests-offline 目录开始安装。</p><h3 id="安装-setuptools"><a href="#安装-setuptools" class="headerlink" title="安装 setuptools"></a>安装 setuptools</h3><blockquote><p><code>setuptools</code> 能帮助我们更好的创建和分发 Python 的包，尤其是具有复杂依赖关系的包。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -zxvf setuptools-41.1.0.post1.tar.gz</span><br><span class="line">cd setuptools-41.1.0.post1/</span><br><span class="line">python setup.py install</span><br></pre></td></tr></table></figure><h3 id="安装-pip"><a href="#安装-pip" class="headerlink" title="安装 pip"></a>安装 pip</h3><blockquote><p><code>pip</code> 是 Python 官方推荐的包管理工具。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -zxvf pip-19.2.2.tar.gz</span><br><span class="line">cd pip-19.2.2/</span><br><span class="line">python setup.py install</span><br></pre></td></tr></table></figure><h3 id="安装-requests-所需的其他依赖"><a href="#安装-requests-所需的其他依赖" class="headerlink" title="安装 requests 所需的其他依赖"></a>安装 requests 所需的其他依赖</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># CA 认证模块</span><br><span class="line">pip install certifi-2019.9.11-py2.py3-none-any.whl</span><br><span class="line"># 字符编码检测模块</span><br><span class="line">pip install chardet-3.0.4-py2.py3-none-any.whl</span><br><span class="line"># 域名解析模块</span><br><span class="line">pip install idna-2.8-py2.py3-none-any.whl</span><br><span class="line"># 线程安全的 HTTP 库</span><br><span class="line">pip install urllib3-1.25.7-py2.py3-none-any.whl</span><br></pre></td></tr></table></figure><h3 id="安装-requests"><a href="#安装-requests" class="headerlink" title="安装 requests"></a>安装 requests</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -zxvf requests-2.22.0.tar.gz</span><br><span class="line">cd requests-2.22.0/</span><br><span class="line">python setup.py install</span><br></pre></td></tr></table></figure><h3 id="测试是否成功成功"><a href="#测试是否成功成功" class="headerlink" title="测试是否成功成功"></a>测试是否成功成功</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">➜ python</span><br><span class="line">&gt;&gt;&gt; import requests</span><br></pre></td></tr></table></figure><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>客户是下午两点半提出的需求，内部讨论好实现方案后三点半，我在准备好安装 <code>requests</code> 所需资源后直接奔赴现场，而不是在公司编码完再过去，因为我相信只要有我们的人到达现场客户就可以踏下心来了，同时我也想挑战一下自己，所以本次点亮了一个技能点：现场使用 vi 进行 coding 和 debug。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;&lt;code&gt;requests&lt;/code&gt; 是一个简单优雅的 Python HTTP 库，相较于 Python 标准库中的 &lt;code&gt;urllib&lt;/code&gt; 和 &lt;code&gt;urllib2&lt;/code&gt;，&lt;code&gt;requests&lt;/cod
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>何时使用事件溯源</title>
    <link href="https://jpanj.com/2019/when-to-use-event-sourcing/"/>
    <id>https://jpanj.com/2019/when-to-use-event-sourcing/</id>
    <published>2019-11-14T06:17:27.000Z</published>
    <updated>2019-11-14T06:24:09.350Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>事件溯源也许很不错，但这确实增加了系统的复杂性。</p></blockquote><p><img src="1.jpg" alt></p><h2 id="什么是事件溯源"><a href="#什么是事件溯源" class="headerlink" title="什么是事件溯源"></a>什么是事件溯源</h2><p>大多数 Web 应用将系统<em>状态</em>存放在数据库中。</p><p>假设让你来做一个在线购物网站的数据库设计，按照传统的数据库设计方案将会有 <code>users</code>、<code>products</code> 和 <code>orders</code> 表 —— 用来代表系统的<em>状态</em>。</p><p>再假设你完成了前期的编码工作并发布上线了这个在线购物网站，几周后你的老板想知道用户平均更新电子邮箱的次数。</p><p>在这种传统的数据库设计中，当用户更新电子邮箱时，执行的查询语句大致如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">UPDATE users SET email=&apos;newemail@mail.com&apos; WHERE id=1;</span><br></pre></td></tr></table></figure><p>问题在于，我们并没有在数据库中存储修改电子邮件的事件日志。</p><p>你可以创建一个额外的列 <code>event_log</code> 并在每次用户更新电子邮箱后记录一次 <code>user changed email</code>。但这样仍存在一些问题：</p><ul><li>需要额外的开发工作才能支持这个特性</li><li>使得数据库设计更加复杂</li><li>只有在实现这个功能后才能生成这些事件，无法对之前产生过的事件进行追溯</li></ul><p>这是让事件溯源派上用场的绝佳场景。</p><p>根据事件溯源设计，你不需要存储系统<em>状态</em>。取而代之的是存储<em>事件</em>。</p><p>比如：当用户注册时，一个 <code>UserCreated</code> 事件被存储。之后当用户更新电子邮箱时，一个 <code>UserChangedEmail</code> 事件被存储。</p><p><img src="2.png" alt="事件溯源系统中的用例事件"></p><center>事件溯源系统中的用例事件</center><h2 id="为什么使用事件溯源"><a href="#为什么使用事件溯源" class="headerlink" title="为什么使用事件溯源"></a>为什么使用事件溯源</h2><h3 id="代表我们的思考方式"><a href="#代表我们的思考方式" class="headerlink" title="代表我们的思考方式"></a>代表我们的思考方式</h3><p>在现实世界里，人们思考的是事件：当其他人询问你今天过得怎么样时，你会告诉他们一些发生过的有趣事件，不大可能描述你在某一时刻的确切状态。</p><p>同样，一个领域专家在描述业务流程时谈论的也是一系列事件。通过事件溯源让我们在系统中对其建模变得更加容易。</p><h3 id="容易生成报告"><a href="#容易生成报告" class="headerlink" title="容易生成报告"></a>容易生成报告</h3><p>想知道用户修改了多少次电子邮箱？通过事件溯源，你已经将这些数据详尽记录在案了。</p><p>想知道一个商品在购物车中被用户移除了多少次？只需简单的对 <code>EventRemovedFromCart</code> 事件进行计算就可以了。</p><p>通过事件溯源，你可以对你的数据进行全方位的洞察，同时使生成的报告可以追溯。</p><h3 id="你拥有了可靠的审计日志"><a href="#你拥有了可靠的审计日志" class="headerlink" title="你拥有了可靠的审计日志"></a>你拥有了可靠的审计日志</h3><p>你可以生成审计日志用来准确记录系统如何进入了某个状态。</p><p>比如，考虑一下你的银行账户，事件溯源生成了交易事件的日志，这样就可以清楚的说明为什么你每个月的工资都不够花了。</p><h2 id="为什么不使用事件溯源"><a href="#为什么不使用事件溯源" class="headerlink" title="为什么不使用事件溯源"></a>为什么不使用事件溯源</h2><p>听起来不错，但事件溯源有什么要注意的呢？</p><p>事件溯源增加了系统额外的复杂性，更多的复杂性意味着难以让新进入的开发人员上手，花更多的时间添加新功能并且也让系统更难以维护。</p><p>如果你要构建的是一个规模较小的系统，不需要安全审计日志，此时使用事件溯源方法带来的麻烦可能比它的价值更大。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;事件溯源也许很不错，但这确实增加了系统的复杂性。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;h2 id=&quot;什么是事件溯源&quot;&gt;&lt;a href=&quot;#什么是事件溯源&quot; class=&quot;headerlin
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>停止在你的循环中使用 i++</title>
    <link href="https://jpanj.com/2019/stop-using-post-increment-in-your-loops/"/>
    <id>https://jpanj.com/2019/stop-using-post-increment-in-your-loops/</id>
    <published>2019-11-12T07:28:59.000Z</published>
    <updated>2019-11-13T05:14:07.297Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>为什么 ++i 通常比 i++ 更好？</p></blockquote><p><img src="1.jpg" alt></p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>如果你之前写过 for 循环，那么你一定使用过 <code>i++</code> 来增加你的循环变量。</p><p>然而，你是否考虑过为什么要选择这种做法呢？</p><p>我们在执行完 <code>i++</code> 后，<code>i</code> 的值会比它先前大 1，这是我们想要的结果。与此同时还有很多方法可以做到，比如：<code>++i</code> 甚至 <code>i = i + 1</code>。</p><p>接下来，我会对比介绍两种实现变量加 1 的方法：<code>++i</code> 和 <code>i++</code>，并解释为什么大多数情况下 <code>++i</code> 可能好于 <code>i++</code>。</p><h2 id="后递增（i-）"><a href="#后递增（i-）" class="headerlink" title="后递增（i++）"></a>后递增（i++）</h2><p><code>i++</code> 方法（或者叫后递增）是最常见的使用方式。</p><p>在伪代码中，后递增操作符对变量 <code>i</code> 的操作大致如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">int j = i;</span><br><span class="line">i = i + 1;</span><br><span class="line">return j;</span><br></pre></td></tr></table></figure><p>由于后递增需要返回 <code>i</code> 的原值而不是返回 <code>i + 1</code> 后的增量值，所以需要将 <code>i</code> 的旧值进行存储。</p><p>这意味着 <code>i++</code> 需要额外的内存来存储这个值，但这是不必要的。因为在大多数情况下，我们并不会使用 <code>i</code> 的旧值，而是直接将其丢弃。</p><h2 id="前递增（-i）"><a href="#前递增（-i）" class="headerlink" title="前递增（++i）"></a>前递增（++i）</h2><p><code>++i</code> 方法（或者叫前递增）比较少见，通常是使用 <code>C</code> 和 <code>C++</code> 的老程序员在用。</p><p>在伪代码中，前递增操作符对变量 <code>i</code> 的操作大致如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">i = i + 1;</span><br><span class="line">return i;</span><br></pre></td></tr></table></figure><p>需要注意的是，在前递增中，我们不必保存 <code>i</code> 的旧值，我们只需简单的对它加 1 并返回。这与 <code>for</code> 循环中的经典用例更加匹配：正如上文所说，我们很少需要 <code>i</code> 的旧值。</p><h2 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h2><p>看过后递增和前递增之间的区别后，你可能会想到：由于 <code>i</code> 的旧值在后递增中未被使用，因此在编译阶段，编译器将会优化掉这一行，使两个操作符等价。</p><p>对于基本类型来说（如整形）确实如此。</p><p>但是对于复杂类型，例如（在 <code>C++</code> 中）用户自定义类型或带有 <code>+</code> 操作重载的迭代器，编译器就无法对此进行优化了。</p><p>所以如果说在你用不到所要递增变量旧值的情况下，使用前递增运算符要好过（或等价于）后递增。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;为什么 ++i 通常比 i++ 更好？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/p&gt;
&lt;h2 id=&quot;介绍&quot;&gt;&lt;a href=&quot;#介绍&quot; class=&quot;headerlink&quot; title=&quot;介绍&quot;&gt;&lt;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>RAID 介绍，为什么不推荐为 HDFS 配置 RAID？</title>
    <link href="https://jpanj.com/2019/raid-and-hdfs/"/>
    <id>https://jpanj.com/2019/raid-and-hdfs/</id>
    <published>2019-11-09T08:48:57.000Z</published>
    <updated>2019-11-09T10:53:04.937Z</updated>
    
    <content type="html"><![CDATA[<h2 id="RAID"><a href="#RAID" class="headerlink" title="RAID"></a>RAID</h2><p>开始前先来思考一个问题，如果一个文件的大小超过了一块磁盘的大小，该如何存储？</p><p><strong>独立硬盘冗余阵列（RAID, Redundant Array of Independent Disks）</strong>，简称磁盘阵列，利用虚拟化存储技术把多个磁盘组合起来，成为一个或多个磁盘阵列组，目的为提升性能或数据冗余，或是两者同时提升。</p><p>简单来说，<code>RAID</code> 把多个磁盘组合成为一个逻辑磁盘，因此，操作系统只会把它当作一个实体磁盘。</p><h2 id="常见-RAID-等级"><a href="#常见-RAID-等级" class="headerlink" title="常见 RAID 等级"></a>常见 RAID 等级</h2><h3 id="RAID-0"><a href="#RAID-0" class="headerlink" title="RAID 0"></a><code>RAID 0</code></h3><p><img src="raid0.png" alt></p><p>假设服务器有 N 块磁盘，<code>RAID 0</code> 是数据在从内存缓冲区写入磁盘时，根据磁盘数量将数据分成 N 份，这些数据同时并发写入 N 块磁盘，使得数据整体写入速度是一块磁盘的 N 倍；读取的时候也一样，所以在所有的级别中，<code>RAID 0</code> 的速度是最快的</p><p>但是 <code>RAID 0</code> 不做数据备份，N 块磁盘中只要有一块损坏，数据完整性就被破坏，其他磁盘的数据也都无法使用了。</p><h3 id="RAID-1"><a href="#RAID-1" class="headerlink" title="RAID 1"></a><code>RAID 1</code></h3><p><img src="raid1.png" alt></p><p><code>RAID 1</code> 是数据在写入磁盘时，将一份数据同时写入两块磁盘，这样任何一块磁盘损坏都不会导致数据丢失，插入一块新磁盘就可以通过复制数据的方式自动修复，具有极高的可靠性，<code>RAID 1</code> 的数据安全性在所有的 <code>RAID</code> 级别上来说是最好的。但无论用多少磁盘做 <code>RAID 1</code>，仅算一个磁盘的容量，是所有 <code>RAID</code> 中磁盘利用率最低的一个级别。</p><p><code>RAID 1</code> 在一些多线程操作系统中能有很好的读取速度，理论上<strong>读取速度</strong>等于磁盘数量的倍数，与 <code>RAID 0</code> 相同。写入速度有微小的降低。</p><h3 id="RAID-10"><a href="#RAID-10" class="headerlink" title="RAID 10"></a><code>RAID 10</code></h3><p><img src="raid10.png" alt></p><p>结合 <code>RAID 0</code> 和 <code>RAID 1</code> 两种方案构成了 <code>RAID 10</code>，它是将所有磁盘 N 平均分成两份，数据同时在两份磁盘写入，相当于 <code>RAID 1</code>；但是平分成两份，在每一份磁盘（也就是 N/2 块磁盘）里面，利用 <code>RAID 0</code> 技术并发读写，这样既提高可靠性又改善性能。不过 <code>RAID 10</code> 的磁盘利用率较低，有一半的磁盘用来写备份数据。</p><h3 id="RAID-3"><a href="#RAID-3" class="headerlink" title="RAID 3"></a><code>RAID 3</code></h3><p><img src="raid3.png" alt></p><p>RAID 3 可以在数据写入磁盘的时候，将数据分成 N-1 份，并发写入 N-1 块磁盘，并在第 N 块磁盘记录校验数据，这样任何一块磁盘损坏（包括校验数据磁盘），都可以利用其他 N-1 块磁盘的数据修复。</p><p>由于数据内的比特分散在不同的磁盘上，因此就算要读取一小段数据资料都可能需要所有的磁盘进行工作，所以这种规格比较适于读取大量数据时使用。</p><p>在数据修改较多的场景中，任何磁盘数据的修改，都会导致第 N 块磁盘重写校验数据。频繁写入的后果是第 N 块磁盘比其他磁盘更容易损坏，需要频繁更换，所以 <code>RAID 3</code> 很少在实践中使用。</p><h3 id="RAID-5"><a href="#RAID-5" class="headerlink" title="RAID 5"></a><code>RAID 5</code></h3><p><img src="raid5.png" alt></p><p>相比 <code>RAID 3</code>，<code>RAID 5</code> 是使用更多的方案。<code>RAID 5</code> 和 <code>RAID 3</code> 很相似，但是校验数据不是写入第 N 块磁盘，而是螺旋式地写入所有磁盘中。这样校验数据的修改也被平均到所有磁盘上，避免 <code>RAID 3</code> 频繁写坏一块磁盘的情况。</p><p><code>RAID 5</code> 至少需要三块磁盘，<code>RAID 5</code> 不是对存储的数据进行备份，而是把数据和相对应的奇偶校验信息存储到组成 <code>RAID 5</code> 的各个磁盘上，并且奇偶校验信息和相对应的数据分别存储于不同的磁盘上。当 <code>RAID 5</code> 的一个磁盘数据发生损坏后，可以利用剩下的数据和相应的奇偶校验信息去恢复被损坏的数据。<code>RAID 5</code> 可以理解为是 <code>RAID 0</code> 和 <code>RAID 1</code> 的折衷方案。</p><h3 id="RAID-6"><a href="#RAID-6" class="headerlink" title="RAID 6"></a><code>RAID 6</code></h3><p><img src="raid6.png" alt></p><p>如果数据需要很高的可靠性，在出现同时损坏两块磁盘的情况下（或者运维管理水平比较落后，坏了一块磁盘但是迟迟没有更换，导致又坏了一块磁盘），仍然需要修复数据，这时候可以使用 <code>RAID 6</code>。</p><p>与 <code>RAID 5</code> 相比 <code>RAID 6</code> 增加第二个独立的奇偶校验信息块。两个独立的奇偶系统使用不同的算法，数据的可靠性非常高，任意两块磁盘同时失效时不会影响数据完整性。</p><h2 id="各种-RAID-技术比较"><a href="#各种-RAID-技术比较" class="headerlink" title="各种 RAID 技术比较"></a>各种 RAID 技术比较</h2><table><thead><tr><th style="text-align:center">RAID类型</th><th style="text-align:center">访问速度</th><th style="text-align:center">数据可靠性</th><th style="text-align:center">磁盘利用率</th><th style="text-align:center">目的</th></tr></thead><tbody><tr><td style="text-align:center">RAID 0</td><td style="text-align:center">很快</td><td style="text-align:center">很低</td><td style="text-align:center">100%</td><td style="text-align:center">追求最大容量、速度</td></tr><tr><td style="text-align:center">RAID 1</td><td style="text-align:center">很慢</td><td style="text-align:center">很高</td><td style="text-align:center">50%</td><td style="text-align:center">追求最大安全性</td></tr><tr><td style="text-align:center">RAID 10</td><td style="text-align:center">中等</td><td style="text-align:center">很高</td><td style="text-align:center">50%</td><td style="text-align:center">总和 RAID 0/1 优点，理论速度较快</td></tr><tr><td style="text-align:center">RAID 5</td><td style="text-align:center">较快</td><td style="text-align:center">很高</td><td style="text-align:center">(N-1)/N</td><td style="text-align:center">追求最大容量、最小预算</td></tr><tr><td style="text-align:center">RAID 6</td><td style="text-align:center">较快</td><td style="text-align:center">较 RAID 5 高</td><td style="text-align:center">(N-2)/N</td><td style="text-align:center">同 RAID 5，但更安全</td></tr></tbody></table><h2 id="HDFS"><a href="#HDFS" class="headerlink" title="HDFS"></a>HDFS</h2><p><code>RAID</code> 可以看作是一种垂直伸缩，一台计算机集成更多的磁盘实现数据更大规模、更安全可靠的存储以及更快的访问速度。而 <code>HDFS</code> 则是水平伸缩，通过添加更多的服务器实现数据更大、更快、更安全存储与访问。</p><p><code>Hadoop</code> 分布式文件系统 <code>HDFS</code> 的设计目标是管理数以千计的服务器、数以万计的磁盘，将这么大规模的服务器计算资源当作一个单一的存储系统进行管理，对应用程序提供数以 <code>PB</code> 计的存储容量，让应用程序像使用普通文件系统一样存储大规模的文件数据。</p><h3 id="为什么不推荐为-HDFS-配置-RAID？"><a href="#为什么不推荐为-HDFS-配置-RAID？" class="headerlink" title="为什么不推荐为 HDFS 配置 RAID？"></a>为什么不推荐为 HDFS 配置 RAID？</h3><p><code>HDFS</code> 已经为同一个文件保留了多个副本，如果磁盘发生故障 <code>HDFS</code> 可以将其恢复。<code>HDFS</code> 同样可以一次从多个节点（DataNode）读取数据，如果使用 <code>RAID 1</code>，将浪费更多的存储空间，如果使用 <code>RAID 0</code>，产生失败的可能性会提升 N 倍（N = 磁盘数量）。使用 <code>RAID 5/6</code> 的话，读写速度将受到影响，也更昂贵。</p><p>不过由于 <code>NameNode</code> 在 <code>HDFS</code> 中容易出现单点故障，因此需要更可靠的硬件配置，所以建议在 <code>NameNode</code> 上使用 <code>RAID</code>。</p><h3 id="其他说明"><a href="#其他说明" class="headerlink" title="其他说明"></a>其他说明</h3><p>在一些 Master 处理节点上，如：Hive 的 <code>MetaStore</code>，也推荐使用 <code>RAID</code>。同时建议为所有的系统盘配置 <code>RAID</code>，你不会希望仅仅因为系统盘故障而导致节点故障。</p><p>对于 ElasticSearch，其本身也提供了很好的 <code>HA</code> 机制，同样无需使用 <code>RAID</code>。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>回到开头的那个问题，我的回答是：</p><ul><li>单机时代：<code>RAID</code></li><li>分布式时代：分布式文件系统</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;RAID&quot;&gt;&lt;a href=&quot;#RAID&quot; class=&quot;headerlink&quot; title=&quot;RAID&quot;&gt;&lt;/a&gt;RAID&lt;/h2&gt;&lt;p&gt;开始前先来思考一个问题，如果一个文件的大小超过了一块磁盘的大小，该如何存储？&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;独立硬盘冗余阵列
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>8 条荒谬的分布式假设</title>
    <link href="https://jpanj.com/2019/8-ridiculous-distributed-assumptions/"/>
    <id>https://jpanj.com/2019/8-ridiculous-distributed-assumptions/</id>
    <published>2019-11-04T02:29:13.000Z</published>
    <updated>2019-11-04T02:37:03.936Z</updated>
    
    <content type="html"><![CDATA[<ol><li>网络是稳定的。</li><li>网络传输的延迟是零。</li><li>网络的带宽是无穷大。</li><li>网络是安全的。</li><li>网络的拓扑不会改变。</li><li>只有一个系统管理员。</li><li>传输数据的成本为零。</li><li>整个网络是同构的。</li></ol><p>在分布式系统中错误是不可能避免的，我们在分布式系统中，能做的不是避免错误，而是要把错误的处理当成功能写在代码中。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;ol&gt;
&lt;li&gt;网络是稳定的。&lt;/li&gt;
&lt;li&gt;网络传输的延迟是零。&lt;/li&gt;
&lt;li&gt;网络的带宽是无穷大。&lt;/li&gt;
&lt;li&gt;网络是安全的。&lt;/li&gt;
&lt;li&gt;网络的拓扑不会改变。&lt;/li&gt;
&lt;li&gt;只有一个系统管理员。&lt;/li&gt;
&lt;li&gt;传输数据的成本为零。&lt;/li&gt;
&lt;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Tmux 使用笔记</title>
    <link href="https://jpanj.com/2019/Tmux-Note/"/>
    <id>https://jpanj.com/2019/Tmux-Note/</id>
    <published>2019-10-29T04:11:25.000Z</published>
    <updated>2019-10-29T04:42:01.194Z</updated>
    
    <content type="html"><![CDATA[<p>Tmux 是一个用于在终端窗口中运行多个终端会话的工具，即终端复用软件（terminal multiplexer）。</p><p>在 Tmux 中可以根据不同的工作任务创建不同的会话，每个会话又可以创建多个窗口来完成不同的工作，每个窗口又可以分割成很多小窗口。这些功能都是非常实用的。</p><h3 id="在-Mac-OS-中安装："><a href="#在-Mac-OS-中安装：" class="headerlink" title="在 Mac OS 中安装："></a>在 Mac OS 中安装：</h3><p><code>$ brew install tmux</code></p><h3 id="开启-oh-my-zsh-tmux-插件："><a href="#开启-oh-my-zsh-tmux-插件：" class="headerlink" title="开启 oh-my-zsh tmux 插件："></a>开启 oh-my-zsh tmux 插件：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">plugins=(</span><br><span class="line">  tmux</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>开启后提供以下快捷命令：</p><ol><li><code>ta &lt;session-name&gt;</code>：接入某个已存在的会话</li><li><code>ts &lt;session-name&gt;</code>：新建一个指定名称的会话</li><li><code>tl</code>：查看当前所有的 Tmux 会话</li><li><code>tksv</code>：用于杀死全部会话</li><li><code>tkss &lt;session-name&gt;</code>：用于杀死某个会话</li></ol><h3 id="最简操作流程"><a href="#最简操作流程" class="headerlink" title="最简操作流程"></a>最简操作流程</h3><ol><li>新建会话 <code>tmux new -s my_sessio</code>n。<ul><li>安装 zsh tmux 插件后可使用 <code>ts my_session</code></li></ul></li><li>在 Tmux 窗口运行所需的程序。</li><li>按下快捷键 <code>Ctrl+b d</code> 将会话分离。</li><li>下次使用时，重新连接到会话 <code>tmux attach-session -t my_session</code>。<ul><li>安装 zsh tmux 插件后可使用 <code>ta my_session</code></li></ul></li></ol><h3 id="常用快捷键："><a href="#常用快捷键：" class="headerlink" title="常用快捷键："></a>常用快捷键：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"># session</span><br><span class="line">Ctrl+b d：分离当前会话。</span><br><span class="line">Ctrl+b s：列出所有会话（切换回话）。</span><br><span class="line">Ctrl+b $：重命名当前会话。</span><br><span class="line"></span><br><span class="line"># pane</span><br><span class="line">Ctrl+b %：划分左右两个窗格。</span><br><span class="line">Ctrl+b &quot;：划分上下两个窗格。</span><br><span class="line">Ctrl+b &lt;arrow key&gt;：光标切换到其他窗格。&lt;arrow key&gt;是指向要切换到的窗格的方向键，比如切换到下方窗格，就按方向键↓。</span><br><span class="line">Ctrl+b ;：光标切换到上一个窗格。</span><br><span class="line">Ctrl+b o：光标切换到下一个窗格。</span><br><span class="line">Ctrl+b &#123;：当前窗格左移。</span><br><span class="line">Ctrl+b &#125;：当前窗格右移。</span><br><span class="line">Ctrl+b Ctrl+o：当前窗格上移。</span><br><span class="line">Ctrl+b Alt+o：当前窗格下移。</span><br><span class="line">Ctrl+b x：关闭当前窗格。</span><br><span class="line">Ctrl+b !：将当前窗格拆分为一个独立窗口。</span><br><span class="line">Ctrl+b z：当前窗格全屏显示，再使用一次会变回原来大小。</span><br><span class="line">Ctrl+b Ctrl+&lt;arrow key&gt;：按箭头方向调整窗格大小。</span><br><span class="line">Ctrl+b q：显示窗格编号。</span><br><span class="line"></span><br><span class="line"># window</span><br><span class="line">Ctrl+b c：创建一个新窗口，状态栏会显示多个窗口的信息。</span><br><span class="line">Ctrl+b p：切换到上一个窗口（按照状态栏上的顺序）。</span><br><span class="line">Ctrl+b n：切换到下一个窗口。</span><br><span class="line">Ctrl+b &lt;number&gt;：切换到指定编号的窗口，其中的&lt;number&gt;是状态栏上的窗口编号。</span><br><span class="line">Ctrl+b w：从列表中选择窗口。</span><br><span class="line">Ctrl+b ,：窗口重命名。</span><br></pre></td></tr></table></figure><h3 id="解决滚轮无效的问题"><a href="#解决滚轮无效的问题" class="headerlink" title="解决滚轮无效的问题"></a>解决滚轮无效的问题</h3><p>新建 <code>~/.tmux.conf</code> 配置文件后写入如下内容：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">set-option -g mouse on</span><br><span class="line"></span><br><span class="line">bind -n WheelUpPane if-shell -F -t = &quot;#&#123;mouse_any_flag&#125;&quot; &quot;send-keys -M&quot; &quot;if -Ft= &apos;#&#123;pane_in_mode&#125;&apos; &apos;send-keys -M&apos; &apos;select-pane -t=; copy-mode -e; send-keys -M&apos;&quot;</span><br><span class="line">bind -n WheelDownPane select-pane -t= \; send-keys -M</span><br></pre></td></tr></table></figure><p><strong>需杀掉全部会话（tksv），重新启动新的会话后以上配置才能生效。</strong></p><h3 id="修改历史记录上限"><a href="#修改历史记录上限" class="headerlink" title="修改历史记录上限"></a>修改历史记录上限</h3><p>历史记录默认上限为 2000 行，可通过在配置文件中加入如下配置来进行修改：</p><p><code>set-option -g history-limit 10000</code></p><h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><p><a href="http://www.ruanyifeng.com/blog/2019/10/tmux.html" target="_blank" rel="noopener">http://www.ruanyifeng.com/blog/2019/10/tmux.html</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Tmux 是一个用于在终端窗口中运行多个终端会话的工具，即终端复用软件（terminal multiplexer）。&lt;/p&gt;
&lt;p&gt;在 Tmux 中可以根据不同的工作任务创建不同的会话，每个会话又可以创建多个窗口来完成不同的工作，每个窗口又可以分割成很多小窗口。这些功能都是
      
    
    </summary>
    
    
  </entry>
  
</feed>
