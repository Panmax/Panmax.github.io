<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>贾攀家</title>
  
  <subtitle>Panmax&#39;s Blog</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://jpanj.com/"/>
  <updated>2021-04-22T07:42:58.983Z</updated>
  <id>https://jpanj.com/</id>
  
  <author>
    <name>Panmax</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>关于 UDP 的 10 个问题</title>
    <link href="https://jpanj.com/2021/udp-questions/"/>
    <id>https://jpanj.com/2021/udp-questions/</id>
    <published>2021-04-22T07:37:32.000Z</published>
    <updated>2021-04-22T07:42:58.983Z</updated>
    
    <content type="html"><![CDATA[<h2 id="是否可以向-UDP-的-90000-端口发送数据包？"><a href="#是否可以向-UDP-的-90000-端口发送数据包？" class="headerlink" title="是否可以向 UDP 的 90000 端口发送数据包？"></a>是否可以向 UDP 的 90000 端口发送数据包？</h2><p><strong>不可以</strong></p><p>TCP 或 UDP 数据包中的端口字段为 16 位，2^16 是 65536，所以最大的端口号是 65535。</p><h2 id="每个UDP数据包都有一个目的端口吗？"><a href="#每个UDP数据包都有一个目的端口吗？" class="headerlink" title="每个UDP数据包都有一个目的端口吗？"></a>每个UDP数据包都有一个目的端口吗？</h2><p><strong>是的</strong></p><p>UDP 报头为 8 个字节。</p><p>根据 RFC，源端口是可选的，但目的端口是必须的。以下是 UDP 的报头结构：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"> &lt;-   16 bits  -&gt;</span><br><span class="line">+-+-+-+-+-+-+-+-+--+-+-+-+-+-+-+-+-</span><br><span class="line">|   source port  |    dest port   |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span><br><span class="line">|     length     |    checksum    |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span><br></pre></td></tr></table></figure><h2 id="可以发送一个长度为-100万字节的-UDP-数据包吗？"><a href="#可以发送一个长度为-100万字节的-UDP-数据包吗？" class="headerlink" title="可以发送一个长度为 100万字节的 UDP 数据包吗？"></a>可以发送一个长度为 100万字节的 UDP 数据包吗？</h2><p><strong>不可以</strong></p><p>UDP 数据包的长度字段同样为 16 位，所以单个包的最大长度是 65535。</p><h2 id="可以把把-JSON-放在-UDP-数据包里吗？"><a href="#可以把把-JSON-放在-UDP-数据包里吗？" class="headerlink" title="可以把把 JSON 放在 UDP 数据包里吗？"></a>可以把把 JSON 放在 UDP 数据包里吗？</h2><p><strong>可以</strong></p><p>UDP 数据包中可以放入任何字节，甚至可以放一个很短的 MP3 文件。</p><h2 id="能否保证-UDP-数据包的到达顺序与发送顺序一致？"><a href="#能否保证-UDP-数据包的到达顺序与发送顺序一致？" class="headerlink" title="能否保证 UDP 数据包的到达顺序与发送顺序一致？"></a>能否保证 UDP 数据包的到达顺序与发送顺序一致？</h2><p><strong>不能</strong></p><h2 id="发送一个UDP数据包后，有没有方法办法判断它是否到达？"><a href="#发送一个UDP数据包后，有没有方法办法判断它是否到达？" class="headerlink" title="发送一个UDP数据包后，有没有方法办法判断它是否到达？"></a>发送一个UDP数据包后，有没有方法办法判断它是否到达？</h2><p><strong>没有</strong></p><p>协议没有提供。</p><h2 id="如果把-UDP-数据包发送到同一数据中心的另一台服务器上，是否能保证到达？"><a href="#如果把-UDP-数据包发送到同一数据中心的另一台服务器上，是否能保证到达？" class="headerlink" title="如果把 UDP 数据包发送到同一数据中心的另一台服务器上，是否能保证到达？"></a>如果把 UDP 数据包发送到同一数据中心的另一台服务器上，是否能保证到达？</h2><p><strong>不能</strong></p><p>即使在同一台计算机内发送，数据包仍然可能被丢弃（例如：缓冲区满了）。</p><h2 id="当你发送一个-UDP-数据包时，如果发生丢失会怎样？"><a href="#当你发送一个-UDP-数据包时，如果发生丢失会怎样？" class="headerlink" title="当你发送一个 UDP 数据包时，如果发生丢失会怎样？"></a>当你发送一个 UDP 数据包时，如果发生丢失会怎样？</h2><p><strong>那就真的就丢了</strong></p><p>如果想在 UDP 之上实现重试，只能自己去实现。</p><p>操作系统的 TCP 协议实现了 TCP 包的重试。</p><h2 id="UDP-的-80-端口与-TCP-的-80-端口一样吗？"><a href="#UDP-的-80-端口与-TCP-的-80-端口一样吗？" class="headerlink" title="UDP 的 80 端口与 TCP 的 80 端口一样吗？"></a>UDP 的 80 端口与 TCP 的 80 端口一样吗？</h2><p><strong>不一样</strong></p><p>UDP 和 TCP 都支持相同的端口号（1-65535），但它们是不同的协议。</p><p>你可以同时在 UDP 的 80 端口和 TPC 的 80 端口运行 2 个不同的服务。</p><h2 id="建立在-UDP-之上的协议有哪些？"><a href="#建立在-UDP-之上的协议有哪些？" class="headerlink" title="建立在 UDP 之上的协议有哪些？"></a>建立在 UDP 之上的协议有哪些？</h2><p><strong>DNS、DHCP, QUIC, NTP, statsd</strong> 和各种视频会议协议。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;是否可以向-UDP-的-90000-端口发送数据包？&quot;&gt;&lt;a href=&quot;#是否可以向-UDP-的-90000-端口发送数据包？&quot; class=&quot;headerlink&quot; title=&quot;是否可以向 UDP 的 90000 端口发送数据包？&quot;&gt;&lt;/a&gt;是否可以向 UD
      
    
    </summary>
    
    
      <category term="udp" scheme="https://jpanj.com/tags/udp/"/>
    
  </entry>
  
  <entry>
    <title>关于 TCP 的 14 个问题</title>
    <link href="https://jpanj.com/2021/tcp-questions/"/>
    <id>https://jpanj.com/2021/tcp-questions/</id>
    <published>2021-04-19T02:39:44.000Z</published>
    <updated>2021-04-19T02:42:18.964Z</updated>
    
    <content type="html"><![CDATA[<h2 id="每个TCP数据包都有一个源-目的IP地址吗？"><a href="#每个TCP数据包都有一个源-目的IP地址吗？" class="headerlink" title="每个TCP数据包都有一个源/目的IP地址吗？"></a>每个TCP数据包都有一个源/目的IP地址吗？</h2><p><strong>是的</strong></p><p>下边是一个TCP数据包的结构</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+-+-+-+-+-+-+-+-+--</span><br><span class="line">| Ethernet header |</span><br><span class="line">+-+-+-+-+-+-+-+-+--</span><br><span class="line">| IP header       |</span><br><span class="line">+-+-+-+-+-+-+-+-+--</span><br><span class="line">| TCP header      |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+</span><br><span class="line">| packet contents |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+</span><br></pre></td></tr></table></figure><p>这就是为什么我们称它为 「TCP/IP」– TCP数据包总是有一个 IP 头。</p><h2 id="每个TCP数据包都有一个端口号吗？"><a href="#每个TCP数据包都有一个端口号吗？" class="headerlink" title="每个TCP数据包都有一个端口号吗？"></a>每个TCP数据包都有一个端口号吗？</h2><p><strong>是的</strong></p><p>TCP 头中的端口字段为16位。</p><h2 id="TCP连接中发送的第一个数据包是什么？"><a href="#TCP连接中发送的第一个数据包是什么？" class="headerlink" title="TCP连接中发送的第一个数据包是什么？"></a>TCP连接中发送的第一个数据包是什么？</h2><p><strong>SYN</strong></p><p>每个TCP连接都是以三次向握手开始的：</p><ul><li>client: SYN</li><li>server: SYN + ACK</li><li>client: ACK</li></ul><h2 id="如果防火墙拦截了连接，TCP握手能否完成？"><a href="#如果防火墙拦截了连接，TCP握手能否完成？" class="headerlink" title="如果防火墙拦截了连接，TCP握手能否完成？"></a>如果防火墙拦截了连接，TCP握手能否完成？</h2><p><strong>不能</strong></p><p>被防火墙拦截的常见症状是 SYN 数据包发出后无 ACK 返回。</p><h2 id="电子邮件使用TCP吗？"><a href="#电子邮件使用TCP吗？" class="headerlink" title="电子邮件使用TCP吗？"></a>电子邮件使用TCP吗？</h2><p><strong>是的</strong></p><p>电子邮件是通过SMTP发送的，它使用了 TCP。</p><p>FTP、POP3、HTTP/1、HTTP/2、websockets 等很逗互联网协议也都使用了 TCP。</p><h2 id="TCP连接是双向的吗？-客户端和服务器都能发送消息吗？"><a href="#TCP连接是双向的吗？-客户端和服务器都能发送消息吗？" class="headerlink" title="TCP连接是双向的吗？(客户端和服务器都能发送消息吗？)"></a>TCP连接是双向的吗？(客户端和服务器都能发送消息吗？)</h2><p><strong>是的</strong></p><p>一个HTTP 请求/响应 是个 TCP 连接 —— 客户端发送 HTTP 请求，然后服务器发送响应。</p><h2 id="如果服务器发送响应给客户端，客户端能否发送更多数据？"><a href="#如果服务器发送响应给客户端，客户端能否发送更多数据？" class="headerlink" title="如果服务器发送响应给客户端，客户端能否发送更多数据？"></a>如果服务器发送响应给客户端，客户端能否发送更多数据？</h2><p><strong>是的</strong></p><p>例如，websockets 使用 TCP 使客户端和服务器根据需要来回发送数据。</p><h2 id="如果你通过TCP发送一个长消息，是否会被分成多个数据包？"><a href="#如果你通过TCP发送一个长消息，是否会被分成多个数据包？" class="headerlink" title="如果你通过TCP发送一个长消息，是否会被分成多个数据包？"></a>如果你通过TCP发送一个长消息，是否会被分成多个数据包？</h2><p><strong>是的</strong></p><p>数据包有最大限制（通常是1500字节），所以需要将一个 TCP 消息分割成许多数据包。</p><h2 id="当你发送-TCP-数据包时，数据包是否可以按照你发送的顺序到达目的地？"><a href="#当你发送-TCP-数据包时，数据包是否可以按照你发送的顺序到达目的地？" class="headerlink" title="当你发送 TCP 数据包时，数据包是否可以按照你发送的顺序到达目的地？"></a>当你发送 TCP 数据包时，数据包是否可以按照你发送的顺序到达目的地？</h2><p><strong>不是</strong></p><p>无法确保网络数据包会按照你发送的顺序到达。</p><h2 id="负责理解TCP协议的代码在哪？"><a href="#负责理解TCP协议的代码在哪？" class="headerlink" title="负责理解TCP协议的代码在哪？"></a>负责理解TCP协议的代码在哪？</h2><p><strong>通常是在操作系统中</strong></p><p>Linux、Mac、Windows等都有 TCP 实现。如果你愿意，也可以编写自己的TCP实现。</p><h2 id="你的操作系统使用-TCP-数据包中的哪个字段将数据包按正确顺序排列？"><a href="#你的操作系统使用-TCP-数据包中的哪个字段将数据包按正确顺序排列？" class="headerlink" title="你的操作系统使用 TCP 数据包中的哪个字段将数据包按正确顺序排列？"></a>你的操作系统使用 TCP 数据包中的哪个字段将数据包按正确顺序排列？</h2><p><strong>序列号(sequence number)</strong></p><p>数据包的序列号告诉你它在整个数据流中的位置。序列号计算的是字节数，而不是包数。</p><p>下面是一组数据包内容和序列号的例子（每个字符是1个字节）：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+-+-+-+-+-+-+-+-+-+-+-</span><br><span class="line">| contents   | seq # |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-</span><br><span class="line">| hello I    |   0   |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-</span><br><span class="line">| x!         |   15  |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-</span><br><span class="line">| &apos;m panma   |   7   |</span><br><span class="line">+-+-+-+-+-+-+-+-+-+-+-</span><br></pre></td></tr></table></figure><p>如果按照正确的顺序排列，这些数据包将重新排列为“hello I’m  panmax!”</p><h2 id="顺序号总是从0开始吗？"><a href="#顺序号总是从0开始吗？" class="headerlink" title="顺序号总是从0开始吗？"></a>顺序号总是从0开始吗？</h2><p><strong>不是</strong></p><p>通常连接中的第一个序列号是一个很大的数字，比如：1737132723，其他序列号都是与这个数字相对的。所以在计算时需要减去第一个序列号。</p><p>如果你在 Wireshark/tcpdump 中查看TCP数据包，它们会进行减法处理，使其看起来像序列号从0开始，让人类更易阅读。</p><h2 id="如果你发送一个-TCP-数据包，如何知道它被接收？"><a href="#如果你发送一个-TCP-数据包，如何知道它被接收？" class="headerlink" title="如果你发送一个 TCP 数据包，如何知道它被接收？"></a>如果你发送一个 TCP 数据包，如何知道它被接收？</h2><p><strong>你会收到一个ACK</strong></p><p>例如，服务器收到了一个序列号为1200的数据包，并且也已收到所有在它之前的数据包，服务器会发送一个序列号为1200的ACK数据包。</p><h2 id="被丢弃的TCP数据包是否会被重试发送？"><a href="#被丢弃的TCP数据包是否会被重试发送？" class="headerlink" title="被丢弃的TCP数据包是否会被重试发送？"></a>被丢弃的TCP数据包是否会被重试发送？</h2><p><strong>是的</strong></p><p>如果客户端(或服务器)在一定时间内没有收到其发送数据包的ACK，它将会重试发送该数据包。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;每个TCP数据包都有一个源-目的IP地址吗？&quot;&gt;&lt;a href=&quot;#每个TCP数据包都有一个源-目的IP地址吗？&quot; class=&quot;headerlink&quot; title=&quot;每个TCP数据包都有一个源/目的IP地址吗？&quot;&gt;&lt;/a&gt;每个TCP数据包都有一个源/目的IP地址
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>关于事件循环的 15 个问题</title>
    <link href="https://jpanj.com/2021/event-loop-questions/"/>
    <id>https://jpanj.com/2021/event-loop-questions/</id>
    <published>2021-04-13T03:50:05.000Z</published>
    <updated>2021-04-13T03:50:51.518Z</updated>
    
    <content type="html"><![CDATA[<h2 id="如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？"><a href="#如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？" class="headerlink" title="如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？"></a>如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？</h2><p>不可能。</p><p>一个事件循环中的所有代码都在一个操作系统线程中运行，所以任何时刻只能有一段代码在运行。</p><h2 id="在一个有事件循环的程序中，是否可以有其他线程？"><a href="#在一个有事件循环的程序中，是否可以有其他线程？" class="headerlink" title="在一个有事件循环的程序中，是否可以有其他线程？"></a>在一个有事件循环的程序中，是否可以有其他线程？</h2><p>是的。</p><p>例如，在 <code>node.js</code> 中，所有的 Javascript 代码都在一个线程中运行，但还有其他工作线程来处理网络请求和其他 I/O。</p><h2 id="在一个事件循环中，是否由操作系统来负责调度函数执行的顺序？"><a href="#在一个事件循环中，是否由操作系统来负责调度函数执行的顺序？" class="headerlink" title="在一个事件循环中，是否由操作系统来负责调度函数执行的顺序？"></a>在一个事件循环中，是否由操作系统来负责调度函数执行的顺序？</h2><p>不是。</p><p>例如，用Python的 <code>asyncio</code>，做调度的代码是一个 Python 程序。</p><h2 id="事件循环真的是一个循环吗？（就像像-for-循环或-while-循环？）"><a href="#事件循环真的是一个循环吗？（就像像-for-循环或-while-循环？）" class="headerlink" title="事件循环真的是一个循环吗？（就像像 for 循环或 while 循环？）"></a>事件循环真的是一个循环吗？（就像像 <code>for</code> 循环或 <code>while</code> 循环？）</h2><p>是的。</p><p>通常事件循环都是以<code>while</code>循环的形式实现的，它看起来像这样。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">while True:</span><br><span class="line">    self._run_once()</span><br><span class="line">    if self._stopping:</span><br><span class="line">        break</span><br></pre></td></tr></table></figure><p>（以上是 Python 的 <code>asyncio</code> 事件循环的实际代码）</p><h2 id="事件循环如何决定下一个函数的运行？"><a href="#事件循环如何决定下一个函数的运行？" class="headerlink" title="事件循环如何决定下一个函数的运行？"></a>事件循环如何决定下一个函数的运行？</h2><p>通过队列。</p><p>当函数准备好运行时，它们会被推到队列中，然后事件循环按顺序执行队列中的函数。</p><h2 id="如果一个网络请求返回，并且它有一个附加的回调，该回调是否会被推送到事件循环的队列中？"><a href="#如果一个网络请求返回，并且它有一个附加的回调，该回调是否会被推送到事件循环的队列中？" class="headerlink" title="如果一个网络请求返回，并且它有一个附加的回调，该回调是否会被推送到事件循环的队列中？"></a>如果一个网络请求返回，并且它有一个附加的回调，该回调是否会被推送到事件循环的队列中？</h2><p>是的。</p><p>当网络请求或其他 I/O 完成后，或者用户点击了某些东西，或者因为该函数计划在那时运行等，函数可能会被推入事件循环的队列中。</p><h2 id="常规函数和异步函数一样吗？"><a href="#常规函数和异步函数一样吗？" class="headerlink" title="常规函数和异步函数一样吗？"></a>常规函数和异步函数一样吗？</h2><p>不一样。</p><p>异步函数特殊之处在于，它们可以被“暂停”并在稍后的事件循环中重新启动。</p><p>例如，在下边这段 Javascript 代码中</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">async function panda() &#123;</span><br><span class="line">    let x = 3;</span><br><span class="line">    await elephant();</span><br><span class="line">    let y = 4;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>事件循环调度 <code>elephant()</code>，暂停 <code>panda</code>，并在 <code>elephant()</code> 运行完毕后调度 <code>panda()</code> 重新启动。普通的非 <code>async</code> 函数不能像这样暂停和重启。这些可以暂停和重启的异步函数的另一个名字是协程。</p><h2 id="如果你要求事件循环在某个时间运行一个函数（比如-Javascript-中的setTimeout），它能保证在那个时间运行吗？"><a href="#如果你要求事件循环在某个时间运行一个函数（比如-Javascript-中的setTimeout），它能保证在那个时间运行吗？" class="headerlink" title="如果你要求事件循环在某个时间运行一个函数（比如 Javascript 中的setTimeout），它能保证在那个时间运行吗？"></a>如果你要求事件循环在某个时间运行一个函数（比如 Javascript 中的<code>setTimeout</code>），它能保证在那个时间运行吗？</h2><p>不能。</p><p>事件循环会尽力而为，但有时会延迟。</p><h2 id="在-Javascript-中，promises、setTimeout、async-await-和回调是否都使用相同的事件循环？"><a href="#在-Javascript-中，promises、setTimeout、async-await-和回调是否都使用相同的事件循环？" class="headerlink" title="在 Javascript 中，promises、setTimeout、async/await 和回调是否都使用相同的事件循环？"></a>在 Javascript 中，promises、setTimeout、async/await 和回调是否都使用相同的事件循环？</h2><p>是的。</p><p>虽然它们的语法不同，但它们是安排代码稍后运行的不同方式。</p><h2 id="在下边这段代码中，是否可以让事件循环在x-3后中断，然后运行别的东西？"><a href="#在下边这段代码中，是否可以让事件循环在x-3后中断，然后运行别的东西？" class="headerlink" title="在下边这段代码中，是否可以让事件循环在x=3后中断，然后运行别的东西？"></a>在下边这段代码中，是否可以让事件循环在x=3后中断，然后运行别的东西？</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">x = 3;</span><br><span class="line">y = 4;</span><br></pre></td></tr></table></figure><p>不能。</p><p>你需要显式让步给事件循环，让它运行一个不同的函数，例如使用 await</p><h2 id="如果你运行一些CPU密集型的代码，如下，事件循环最终会中断代码吗？"><a href="#如果你运行一些CPU密集型的代码，如下，事件循环最终会中断代码吗？" class="headerlink" title="如果你运行一些CPU密集型的代码，如下，事件循环最终会中断代码吗？"></a>如果你运行一些CPU密集型的代码，如下，事件循环最终会中断代码吗？</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">while(true) &#123; </span><br><span class="line">   i = i * 3</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不会。</p><p>通常，你可以通过运行一些 CPU 密集型操作来长时间阻塞事件循环。</p><h2 id="如果你的Web-服务器事件循环中-CPU-的使用率到达100-，当新的-HTTP-请求进来时，是否能够立即响应？"><a href="#如果你的Web-服务器事件循环中-CPU-的使用率到达100-，当新的-HTTP-请求进来时，是否能够立即响应？" class="headerlink" title="如果你的Web 服务器事件循环中 CPU 的使用率到达100%，当新的 HTTP 请求进来时，是否能够立即响应？"></a>如果你的Web 服务器事件循环中 CPU 的使用率到达100%，当新的 HTTP 请求进来时，是否能够立即响应？</h2><p>不能。</p><p>如果你的事件循环的 CPU 总是很忙，那么新进来的事件就不会得到及时处理。</p><h2 id="Javascript代码总是有一个事件循环吗？"><a href="#Javascript代码总是有一个事件循环吗？" class="headerlink" title="Javascript代码总是有一个事件循环吗？"></a>Javascript代码总是有一个事件循环吗？</h2><p>是的。</p><p>至少在 <code>node.js</code> 和浏览器中是这样的，Javascript 代码总是有一个事件循环运行。</p><h2 id="是否存在一个所有的事件循环都在使用标准的事件循环库？"><a href="#是否存在一个所有的事件循环都在使用标准的事件循环库？" class="headerlink" title="是否存在一个所有的事件循环都在使用标准的事件循环库？"></a>是否存在一个所有的事件循环都在使用标准的事件循环库？</h2><p>没有。</p><p>在不同的编程语言中，有很多不同的事件循环实现。</p><h2 id="你可以在任何编程语言中使用事件循环吗？"><a href="#你可以在任何编程语言中使用事件循环吗？" class="headerlink" title="你可以在任何编程语言中使用事件循环吗？"></a>你可以在任何编程语言中使用事件循环吗？</h2><p>可以。</p><p>大多数编程语言并没有像 Javascript 那样的“一切都在事件循环中运行”的模式，但许多语言都有事件循环库。而且从理论上讲，即使还没有自己语言的事件循环库，你也可以编写一个。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？&quot;&gt;&lt;a href=&quot;#如果你的程序中只有一个事件循环，没有其他代码，那么是否有可能在同一时间运行两行代码？&quot; class=&quot;headerlink&quot; title=&quot;如果你的程序中只
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>面向程序员的数据挖掘指南-知识点</title>
    <link href="https://jpanj.com/2021/guidetodatamining-points/"/>
    <id>https://jpanj.com/2021/guidetodatamining-points/</id>
    <published>2021-04-10T08:00:30.000Z</published>
    <updated>2021-04-10T10:49:10.719Z</updated>
    
    <content type="html"><![CDATA[<p>前段时间读了《面向程序员的数据挖掘指南》，原文链接：<a href="https://dataminingguide.books.yourtion.com/" target="_blank" rel="noopener">https://dataminingguide.books.yourtion.com/</a>，把里边的知识点做下整理。</p><h2 id="曼哈顿距离"><a href="#曼哈顿距离" class="headerlink" title="曼哈顿距离"></a>曼哈顿距离</h2><p>x之差的绝对值加上y之差的绝对值<br><img src="16180502800725.jpg" alt></p><h2 id="欧几里得距离"><a href="#欧几里得距离" class="headerlink" title="欧几里得距离"></a>欧几里得距离</h2><p><img src="16180503062224.jpg" alt></p><p>勾股定理<br><img src="16180503540478.jpg" alt></p><h2 id="闵可夫斯基距离"><a href="#闵可夫斯基距离" class="headerlink" title="闵可夫斯基距离"></a>闵可夫斯基距离</h2><p><img src="16180503743318.jpg" alt></p><ul><li>r = 1 该公式即曼哈顿距离</li><li>r = 2 该公式即欧几里得距离</li><li>r = ∞ 极大距离</li></ul><p>r值越大，单个维度的差值大小会对整体距离有更大的影响。</p><h2 id="协同过滤"><a href="#协同过滤" class="headerlink" title="协同过滤"></a>协同过滤</h2><p>利用他人的喜好来进行推荐，也就是说，是大家一起产生的推荐。</p><h2 id="皮尔逊相关系数"><a href="#皮尔逊相关系数" class="headerlink" title="皮尔逊相关系数"></a>皮尔逊相关系数</h2><p>用于衡量两个变量之间的相关性，它的值在-1至1之间，1表示完全吻合，-1表示完全相悖。</p><p>皮尔逊相关系数的计算公式是：<br><img src="16180504120770.jpg" alt></p><p>皮尔逊相关系数的近似值：<br><img src="16180504256020.jpg" alt></p><h2 id="余弦相似度"><a href="#余弦相似度" class="headerlink" title="余弦相似度"></a>余弦相似度</h2><p><img src="16180504363132.jpg" alt></p><p>“·”号表示数量积。</p><p>“||x||”表示向量x的模，计算公式是：<br><img src="16180504450729.jpg" alt></p><p>如：<br><img src="16180504509371.jpg" alt></p><p>它们的模是：<br><img src="16180504562002.jpg" alt></p><p>数量积的计算：<br><img src="16180504675659.jpg" alt></p><p>因此余弦相似度是：<br><img src="16180504735687.jpg" alt></p><p>余弦相似度的范围从1到-1，1表示完全匹配，-1表示完全相悖。</p><h2 id="应该使用哪种相似度？"><a href="#应该使用哪种相似度？" class="headerlink" title="应该使用哪种相似度？"></a>应该使用哪种相似度？</h2><ul><li>如果数据存在“分数膨胀”问题，就使用皮尔逊相关系数。</li><li>如果数据比较“密集”，变量之间基本都存在公有值，且这些距离数据是非常重要的，那就使用欧几里得或曼哈顿距离。</li><li>如果数据是稀疏的，则使用余弦相似度。</li></ul><h3 id="K最邻近算法"><a href="#K最邻近算法" class="headerlink" title="K最邻近算法"></a>K最邻近算法</h3><p><img src="16180505034865.jpg" alt></p><h2 id="用户的评价类型可以分为显式评价和隐式评价"><a href="#用户的评价类型可以分为显式评价和隐式评价" class="headerlink" title="用户的评价类型可以分为显式评价和隐式评价"></a>用户的评价类型可以分为显式评价和隐式评价</h2><ul><li>显式评价指的是用户明确地给出对物品的评价</li><li>所谓隐式评价，就是我们不让用户明确给出对物品的评价，而是通过观察他们的行为来获得偏好信息。<ul><li>另一种隐式评价是用户的实际购买记录</li></ul></li></ul><h2 id="显式评价的问题"><a href="#显式评价的问题" class="headerlink" title="显式评价的问题"></a>显式评价的问题</h2><ul><li>问题1：人们很懒，不愿评价物品</li><li>问题2：人们会撒谎，或存有偏见</li><li>问题3：人们不会更新他们的评论</li></ul><h3 id="基于用户的协同过滤弊端"><a href="#基于用户的协同过滤弊端" class="headerlink" title="基于用户的协同过滤弊端"></a>基于用户的协同过滤弊端</h3><ol><li><strong>扩展性</strong> 上文已经提到，随着用户数量的增加，其计算量也会增加。这种算法在只有几千个用户的情况下能够工作得很好，但达到一百万个用户时就会出现瓶颈。</li><li><strong>稀疏性</strong> 大多数推荐系统中，物品的数量要远大于用户的数量，因此用户仅仅对一小部分物品进行了评价，这就造成了数据的稀疏性。</li></ol><h2 id="基于用户的协同过滤和基于物品的协同过滤区别"><a href="#基于用户的协同过滤和基于物品的协同过滤区别" class="headerlink" title="基于用户的协同过滤和基于物品的协同过滤区别"></a>基于用户的协同过滤和基于物品的协同过滤区别</h2><ul><li>基于用户的协同过滤是通过计算用户之间的距离找出最相似的用户，并将他评价过的物品推荐给目标用户；</li><li><p>而基于物品的协同过滤则是找出最相似的物品，再结合用户的评价来给出推荐结果。</p></li><li><p>基于用户的协同过滤又称为内存型协同过滤，因为我们需要将所有的评价数据都保存在内存中来进行推荐。</p></li><li>基于物品的协同过滤也称为基于模型的协同过滤，因为我们不需要保存所有的评价数据，而是通过构建一个物品相似度模型来做推荐。</li></ul><h2 id="修正的余弦相似度"><a href="#修正的余弦相似度" class="headerlink" title="修正的余弦相似度"></a>修正的余弦相似度</h2><p>修正的余弦相似度是一种基于模型的协同过滤算法。这种算法的优势之一是扩展性好，对于大数据量而言，运算速度快、占用内存少。</p><p>用户的评价标准是不同的，比如喜欢一个歌手时有些人会打4分，有些打5分；不喜欢时有人会打3分，有些则会只给1分。修正的余弦相似度计算时会将用户对物品的评分减去用户所有评分的均值，从而解决这个问题。</p><p><img src="16180506173984.jpg" alt></p><p>U表示同时评价过物品i和j的用户集合</p><p><img src="16180506253823.jpg" alt></p><p>表示将用户u对物品i的评价值减去用户u对所有物品的评价均值，从而得到修正后的评分。</p><p>s(i,j)表示物品i和j的相似度，分子表示将同时评价过物品i和j的用户的修正评分相乘并求和，分母则是对所有的物品的修正评分做一些汇总处理。</p><h2 id="修正的余弦相似度示例"><a href="#修正的余弦相似度示例" class="headerlink" title="修正的余弦相似度示例"></a>修正的余弦相似度示例</h2><p>计算Kacey Musgraves和Imagine Dragons的相似度<br><img src="16180506513378.jpg" alt></p><p>我已经标出了同时评价过这两个歌手的用户，代入到公式中：<br><img src="16180506567485.jpg" alt></p><p>所以这两个歌手之间的修正余弦相似度为0.5260</p><h2 id="使用修正余弦相似度进行预测"><a href="#使用修正余弦相似度进行预测" class="headerlink" title="使用修正余弦相似度进行预测"></a>使用修正余弦相似度进行预测</h2><p>比如我想知道David有多喜欢Kacey Musgraves？<br><img src="16180506750399.jpg" alt></p><p>p(u,i)表示我们会来预测用户u对物品i的评分，所以p(David, Kacey Musgraves)就表示我们将预测David会给Kacey打多少分。<br>N是一个物品的集合，有如下特性：</p><ul><li>用户u对集合中的物品打过分</li><li>物品i和集合中的物品有相似度数据（即上文中的矩阵）</li></ul><p>Si,N表示物品i和N的相似度，Ru,N表示用户u对物品N的评分。</p><p>为了让公式的计算效果更佳，对物品的评价分值最好介于-1和1之间。<br><img src="16180506961757.jpg" alt></p><p>MaxR表示评分系统中的最高分（这里是5），MinR为最低分（这里是1），Ru,N是用户u对物品N的评分，NRu,N则表示修正后的评分（即范围在-1和1之间）。</p><p>若已知NRu,N，求解Ru,N的公式为：<br><img src="16180507023401.jpg" alt></p><p>比如一位用户打了2分，那修正后的评分为：<br><img src="16180507112641.jpg" alt></p><p>反过来则是：<br><img src="16180507150889.jpg" alt></p><p>修正David对各个物品的评分：<br><img src="16180507222041.jpg" alt></p><p>结合物品相似度矩阵，代入公式：<br><img src="16180507271022.jpg" alt></p><p>将其转换到5星评价体系中：<br><img src="16180507321601.jpg" alt></p><h2 id="Slope-One算法"><a href="#Slope-One算法" class="headerlink" title="Slope One算法"></a>Slope One算法</h2><p>一种比较流行的基于物品的协同过滤算法</p><p>分为两个步骤：</p><ul><li>首先需要计算出两两物品之间的差值（可以在夜间批量计算）。</li><li>第二步则是进行预测</li></ul><h2 id="Slope-One算法计算差值"><a href="#Slope-One算法计算差值" class="headerlink" title="Slope One算法计算差值"></a>Slope One算法计算差值</h2><p>计算物品之间差异的公式是：<br><img src="16180507539281.jpg" alt></p><p>card(S)表示S中有多少个元素；X表示所有评分值的集合；card(Sj,i(X))则表示同时评价过物品j和i的用户数。</p><p>计算Taylor Swift 和 PSY之间的差值<br><img src="16180507585991.jpg" alt></p><p>card(Sj,i(X))的值是2——因为有两个用户（Amy和Ben）同时对PSY和Taylor Swift打过分。</p><p>分子uj-ui表示用户对j的评分减去对i的评分，代入公式得：<br><img src="16180507641583.jpg" alt></p><p>即用户们给Taylor Swift的评分比PSY要平均高出两分。</p><h2 id="Slope-One算法更新"><a href="#Slope-One算法更新" class="headerlink" title="Slope One算法更新"></a>Slope One算法更新</h2><p>比如说Taylor Swift和PSY的差值是2，是根据9位用户的评价计算的。当有一个新用户对Taylor Swift打了5分，PSY打了1分时，更新后的差值为：<br><img src="16180507784873.jpg" alt></p><p>使用加权的Slope One算法进行预测<br>公式为：<br><img src="16180507824668.jpg" alt><br><img src="16180507871219.jpg" alt></p><p>PWS1(u)j表示我们将预测用户u对物品i的评分。<br><img src="16180507950656.jpg" alt></p><p>表示遍历Ben评价过的所有歌手，除了Whitney Houston以外（也就是-{j}的意思）。</p><p>整个分子的意思是：对于Ben评价过的所有歌手（Whitney Houston除外），找出Whitney Houston和这些歌手之间的差值，并将差值加上Ben对这个歌手的评分。同时，我们要将这个结果乘以同时评价过两位歌手的用户数。</p><p>Ben的评分情况和两两歌手之间的差异值展示如下：<br><img src="16180508068666.jpg" alt></p><ol><li>Ben对Taylor Swift打了5分，也就是ui</li><li>Whitney Houston和Taylor Swift的差异是-1，即devj,i</li><li>devj,i + ui = 4</li><li>共有两个用户（Amy和Daisy）同时对Taylor Swift和Whitney Houston做了评价，即cj,i = 2</li><li>那么(devj,i + ui) cj,i = 4 × 2 = 8</li><li>Ben对PSY打了2分</li><li>Whitney Houston和PSY的差异是0.75</li><li>devj,i + ui = 2.75</li><li>有两个用户同时评价了这两位歌手，因此(devj,i + ui) cj,i = 2.75 × 2 = 5.5</li><li>分子：8 + 5.5 = 13.5</li><li>分母：2 + 2 = 4</li><li>预测评分：13.5 ÷ 4 = 3.375</li></ol><h2 id="向量"><a href="#向量" class="headerlink" title="向量"></a>向量</h2><p>在线性代数中，向量（vector）指的是具有大小和方向的几何对象。向量支持多重运算，包括相加、相减及数乘等。</p><ul><li>当我们用这种方式定义特征后，就可以运用线性代数中的向量运算法则了。</li></ul><p>在数据挖掘中，向量则可简单认为是物品的一组特征，比如音乐乐曲的特征。做文本挖掘时，会将一篇文章也用向量来表示——每个元素的位置表示一个特定的单词，这个位置上的值表示单词出现的次数。</p><ul><li>用「向量」一词比用「物品的一组特征」要来的专业</li></ul><h2 id="分类器"><a href="#分类器" class="headerlink" title="分类器"></a>分类器</h2><p>分类器是指通过物品特征来判断它应该属于哪个组或类别的程序。</p><p>分类器程序会基于一组已经做过分类的物品进行学习，从而判断新物品的所属类别。</p><h2 id="标准化"><a href="#标准化" class="headerlink" title="标准化"></a>标准化</h2><p>要让数据变得可用我们可以对其进行标准化，最常用的方法是将所有数据都转化为0到1之间的值。</p><p>标准分计算公式：<br><img src="16180508898266.jpg" alt></p><p>mean：平均值<br>standard deviation：标准差<br>标准差的计算公式是：<br><img src="16180508947636.jpg" alt></p><p>card(x)表示集合x中的元素个数。</p><h3 id="修正的标准分"><a href="#修正的标准分" class="headerlink" title="修正的标准分"></a>修正的标准分</h3><p>计算方法：将标准分公式中的均值改为中位数，将标准差改为绝对偏差。<br><img src="16180509129002.jpg" alt></p><p>中位数指的是将所有数据进行排序，取中间的那个值。如果数据量是偶数，则取中间两个数值的均值。</p><p>计算工资的对偏差：<br>首先将所有人按薪水排序，找到中位数，然后计算绝对偏差：<br><img src="16180509679074.jpg" alt></p><p>可以计算得出Yun的修正标准分：<br><img src="16180509735438.jpg" alt></p><h2 id="是否需要标准化？"><a href="#是否需要标准化？" class="headerlink" title="是否需要标准化？"></a>是否需要标准化？</h2><p>当物品的特征数值尺度不一时，就有必要进行标准化。</p><p>需要进行标准化的情形：</p><ol><li>我们需要通过物品特性来计算距离；</li><li>不同特性之间的尺度相差很大。</li></ol><h2 id="十折交叉验证"><a href="#十折交叉验证" class="headerlink" title="十折交叉验证"></a>十折交叉验证</h2><p>将数据集随机分割成十个等份，每次用9份数据做训练集，1份数据做测试集，如此迭代10次。</p><h2 id="留一法"><a href="#留一法" class="headerlink" title="留一法"></a>留一法</h2><p>在数据挖掘领域，N折交叉验证又称为留一法。</p><p>上面已经提到了留一法的优点之一：我们用几乎所有的数据进行训练，然后用一个数据进行测试。</p><h3 id="留一法的另一个优点是：确定性。"><a href="#留一法的另一个优点是：确定性。" class="headerlink" title="留一法的另一个优点是：确定性。"></a>留一法的另一个优点是：确定性。</h3><p>十折交叉验证是一种不确定的验证。相反，留一法得到的结果总是相同的，这是它的一个优点。</p><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>最大的缺点是计算时间很长。</p><p>留一法的另一个缺点是分层问题。</p><p>在留一法中，所有的测试集都只包含一个数据。所以说，留一法对小数据集是合适的，但大多数情况下我们会选择十折交叉验证。</p><h2 id="混淆矩阵"><a href="#混淆矩阵" class="headerlink" title="混淆矩阵"></a>混淆矩阵</h2><p>表格的行表示测试用例实际所属的类别，列则表示分类器的判断结果。</p><p>混淆矩阵可以帮助我们快速识别出分类器到底在哪些类别上发生了混淆，因此得名。</p><p>这个数据集中有300人，使用十折交叉验证，其混淆矩阵如下：<br><img src="16180510428253.jpg" alt></p><p>可以看到，100个体操运动员中有83人分类正确，17人被错误地分到了马拉松一列；92个篮球运动员分类正确，8人被分到了马拉松；85个马拉松运动员分类正确，9人被分到了体操，16人被分到了篮球。</p><p>混淆矩阵的对角线（绿色字体）表示分类正确的人数，因此求得的准确率是：<br><img src="16180510502089.jpg" alt></p><p>从混淆矩阵中可以看出分类器的主要问题。</p><p>在这个示例中，我们的分类器可以很好地区分体操运动员和篮球运动员，而马拉松运动员则比较容易和其他两个类别发生混淆。</p><h2 id="Kappa指标"><a href="#Kappa指标" class="headerlink" title="Kappa指标"></a>Kappa指标</h2><p>Kappa指标可以用来评价分类器的效果比随机分类要好多少。</p><p>Kappa指标可以用来衡量我们之前构造的分类器和随机分类器的差异，公式为：<br><img src="16180510688805.jpg" alt></p><p>P(c)表示分类器的准确率，P(r)表示随机分类器的准确率。<br><img src="16180510730702.jpg" alt></p><h3 id="动手实践"><a href="#动手实践" class="headerlink" title="动手实践"></a>动手实践</h3><p>以下是该分类器的混淆矩阵，尝试计算出它的Kappa指标并予以解释。<br><img src="16180510916565.jpg" alt></p><p>准确率 = （50+75+123+170）/600= 0.697</p><p>计算列合计和百分比：<br><img src="16180510979756.jpg" alt></p><p>然后根据百分比来填充随机分类器的混淆矩阵：<br><img src="16180511012204.jpg" alt></p><p>随机分类器准确率 = (8 + 24 + 51 + 92) / 600 = (175 / 600) = 0.292</p><p>最后，计算Kappa指标：<br><img src="16180511094255.jpg" alt></p><p>这说明分类器的效果还是要好过预期的。</p><h2 id="kNN算法"><a href="#kNN算法" class="headerlink" title="kNN算法"></a>kNN算法</h2><p>考察这条新记录周围距离最近的k条记录，而不是只看一条，因此这种方法称为k近邻算法（kNN）。</p><p>每个近邻都有投票权，程序会将新记录判定为得票数最多的分类。比如说，我们使用三个近邻（k = 3），其中两条记录属于体操，一条记录属于马拉松，那我们会判定x为体操。</p><h2 id="KNN-算法预测举例"><a href="#KNN-算法预测举例" class="headerlink" title="KNN 算法预测举例"></a>KNN 算法预测举例</h2><p>我们需要预测Ben对Funky Meters的喜好程度，他的三个近邻分别是Sally、Tara、和Jade。</p><p>下表是这三个人离Ben的距离，以及他们对Funky Meters的评分：<br><img src="16180511236348.jpg" alt></p><p>在计算平均值的时候，我希望距离越近的用户影响越大，因此可以对距离取倒数，从而得到下表：<br><img src="16180511296710.jpg" alt></p><p>下面，我们把所有的距离倒数除以距离倒数的和（0.2 + 0.1 + 0.067 = 0.367），从而得到评分的权重：<br><img src="16180511348946.jpg" alt></p><p>我们可以注意到两件事情：权重之和是1；原始数据中，Sally的距离是Tara的二分之一，这点在权重中体现出来了。</p><p>最后，我们求得平均值，也即预测Ben对Funky Meters的评分：<br><img src="16180511443995.jpg" alt></p><h2 id="近邻算法-vs-贝叶斯算法"><a href="#近邻算法-vs-贝叶斯算法" class="headerlink" title="近邻算法 vs 贝叶斯算法"></a>近邻算法 vs 贝叶斯算法</h2><p>近邻算法又称为被动学习算法。这种算法只是将训练集的数据保存起来，在收到测试数据时才会进行计算。</p><p>贝叶斯算法则是一种主动学习算法。它会根据训练集构建起一个模型，并用这个模型来对新的记录进行分类，因此速度会快很多。</p><h2 id="贝叶斯算法的两个优点"><a href="#贝叶斯算法的两个优点" class="headerlink" title="贝叶斯算法的两个优点"></a>贝叶斯算法的两个优点</h2><p>能够给出分类结果的置信度</p><p>它是一种主动学习算法</p><h2 id="概率"><a href="#概率" class="headerlink" title="概率"></a>概率</h2><p>我们用符号P(h)来表示，即事件h发生的概率：</p><ul><li>投掷硬币：P(正面) = 0.5</li><li>掷骰子：P(1) = 1/6</li><li>青少年：P(女生) = 0.5</li></ul><p>P(h|D)来表示D条件下事件h发生的概率。比如：P(女生|弗兰克学院的学生) = 0.86</p><p>计算的公式是：<br><img src="16180511856276.jpg" alt></p><h2 id="概率计算"><a href="#概率计算" class="headerlink" title="概率计算"></a>概率计算</h2><p>下表是一些人使用笔记本电脑和手机的品牌：<br><img src="16180511997961.jpg" alt></p><p>使用iPhone的概率是多少？<br><img src="16180512064821.jpg" alt></p><p>如果已知这个人使用的是Mac笔记本，那他使用iPhone的概率是？<br><img src="16180512109821.jpg" alt></p><p>首先计算出同时使用Mac和iPhone的概率：<br><img src="16180512214247.jpg" alt></p><p>使用Mac的概率则是：<br><img src="16180512279040.jpg" alt></p><p>从而计算得到Mac用户中使用iPhone的概率：<br><img src="16180512332435.jpg" alt></p><p>为了简单起见，我们可以直接通过计数得到：<br><img src="16180512368972.jpg" alt></p><h2 id="贝叶斯法则"><a href="#贝叶斯法则" class="headerlink" title="贝叶斯法则"></a>贝叶斯法则</h2><p>贝叶斯法则描述了P(h)、P(h|D)、P(D)、以及P(D|h)这四个概率之间的关系：<br><img src="16180512440972.jpg" alt></p><p>现实问题中要计算P(h|D)往往是很困难的</p><h2 id="朴素贝叶斯"><a href="#朴素贝叶斯" class="headerlink" title="朴素贝叶斯"></a>朴素贝叶斯</h2><p>朴素贝叶斯计算得到的概率其实是真实概率的一种估计，而真实概率是对全量数据做统计得到的。</p><p>在朴素贝叶斯中，概率为0的影响是很大的，甚至会不顾其他概率的大小。此外，抽样统计的另一个问题是会低估真实概率。</p><h3 id="如何解决概率为0的影响？"><a href="#如何解决概率为0的影响？" class="headerlink" title="如何解决概率为0的影响？"></a>如何解决概率为0的影响？</h3><p>解决方法是将公式变为以下形式：<br><img src="16180512581633.jpg" alt></p><p>n表示训练集中y类别的记录数；nc表示y类别中值为x的记录数。</p><p>m是一个常数，表示等效样本大小。</p><p>决定常数m的方法有很多，我们这里使用值的类别来作为m，比如投票有赞成和否决两种类别，所以m就为2。</p><p>p则是相应的先验概率，比如说赞成和否决的概率分别是0.5，那p就是0.5。</p><h3 id="标准差"><a href="#标准差" class="headerlink" title="标准差"></a>标准差</h3><p><img src="16180512917768.jpg" alt></p><p>标准差是用来衡量数据的离散程度的，如果所有数据都接近于平均值，那标准差也会比较小。</p><p>样本标准差的公式是：<br><img src="16180512974529.jpg" alt></p><p>我们把有限集合A的元素个数记为card(A)。例如A={a,b,c}，则card(A)=3</p><h2 id="高斯分布"><a href="#高斯分布" class="headerlink" title="高斯分布"></a>高斯分布</h2><p>正态分布、钟型曲线、高斯分布等术语，他们指的是同一件事：68%的数据会落在标准差为1的范围内，95%的数据会落在标准差为2的范围内：<br><img src="16180513070896.jpg" alt></p><p>概率计算公式：<br><img src="16180513116255.jpg" alt></p><p>假设我们要计算P(100k|i500)的概率，即购买i500的用户中收入是100,000美元的概率。之前我们计算过购买i500的用户平均收入（106.111）以及样本标准差（21.327），我们用希腊字母μ（读“谬”）来表示平均值，σ（读“西格玛”）来表示标准差。<br><img src="16180513192590.jpg" alt></p><p>xi = 100 指的是收入100k<br><img src="16180513291608.jpg" alt><br><img src="16180513358428.jpg" alt><br><img src="16180513402506.jpg" alt></p><p>e是自然常数，约等于2.718。<br><img src="16180513439380.jpg" alt></p><h2 id="监督式和非监督式学习"><a href="#监督式和非监督式学习" class="headerlink" title="监督式和非监督式学习"></a>监督式和非监督式学习</h2><p>当我们使用已经标记好分类的数据集进行训练时，这种类型的机器学习称为“监督式学习”。文本分类就是监督式学习的一种。</p><p>如果训练集没有标好分类，那就称为“非监督式学习”，聚类就是一种非监督式学习</p><h2 id="聚类"><a href="#聚类" class="headerlink" title="聚类"></a>聚类</h2><p>通过物品特征来计算距离，并自动分类到不同的群集或组中。</p><h2 id="k-means算法可概括为"><a href="#k-means算法可概括为" class="headerlink" title="k-means算法可概括为"></a>k-means算法可概括为</h2><ol><li>随机选取k个元素作为中心点；</li><li>根据距离将各个点分配给中心点；</li><li>计算新的中心点；</li><li>重复2、3，直至满足条件。</li></ol><h2 id="评判聚类结果的好坏"><a href="#评判聚类结果的好坏" class="headerlink" title="评判聚类结果的好坏"></a>评判聚类结果的好坏</h2><p>我们可以使用误差平方和（或称离散程度）来评判聚类结果的好坏，它的计算方法是：计算每个点到中心点的距离平方和。<br><img src="16180513694801.jpg" alt></p><p>上面的公式中，第一个求和符号是遍历所有的分类，比如i=1时计算第一个分类，i=2时计算第二个分类，直到计算第k个分类；第二个求和符号是遍历分类中所有的点；Dist指代距离计算公式（如曼哈顿距离、欧几里得距离）；计算数据点x和中心点ci之间的距离，平方后相加。</p><h2 id="k-means"><a href="#k-means" class="headerlink" title="k-means++"></a>k-means++</h2><p>前面我们提到k-means是50年代发明的算法，它的实现并不复杂，但仍是现今最流行的聚类算法。不过它也有一个明显的缺点。在算法一开始需要随机选取k个起始点，正是这个随机会有问题。<br>有时选取的点能产生最佳结果，而有时会让结果变得很差。k-means++则改进了起始点的选取过程，其余的和k-means一致。</p><p>以下是k-means++选取起始点的过程：</p><ol><li>随机选取一个点；</li><li>重复以下步骤，直到选完k个点：<ol><li>计算每个数据点（dp）到各个中心点的距离（D），选取最小的值，记为D(dp)；</li><li>根据D(dp)的概率来随机选取一个点作为中心点。</li></ol></li></ol><p>k-means++选取起始点的方法总结下来就是：第一个点还是随机的，但后续的点就会尽量选择离现有中心点更远的点。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;前段时间读了《面向程序员的数据挖掘指南》，原文链接：&lt;a href=&quot;https://dataminingguide.books.yourtion.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://dataminingguide.bo
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>利用 docker-compose，搭建本地 HBase 集群</title>
    <link href="https://jpanj.com/2021/docker-compose-hbase/"/>
    <id>https://jpanj.com/2021/docker-compose-hbase/</id>
    <published>2021-04-06T03:11:36.000Z</published>
    <updated>2021-04-06T03:14:22.607Z</updated>
    
    <content type="html"><![CDATA[<p>最近在重构公司的直播推荐服务，特征数据的存储使用的是 <a href="https://hbase.apache.org/" target="_blank" rel="noopener">Hbase</a>，但有个问题是我们的开发环境并没有搭建 HBase 集群，开发环境和生产环境网络又不通，这样本地调试就很不方便，所以我需要在本地搭一个 HBase 集群。</p><p>第一时间想到的就是使用 <code>docker-compose</code></p><p>牛顿老师说过，轮子还是别人的圆，于是我在 gayhub 上找到了这个轮子：<a href="https://github.com/big-data-europe/docker-hbase" target="_blank" rel="noopener">https://github.com/big-data-europe/docker-hbase</a></p><p>但是在搭建过程中发现了它的问题，有两个重要的端口没有在 <code>docker-compose.yml</code> 文件中开放，并且说明中没有提示要修改 hosts，再看了下这个仓库的更新时间，已经有 3 年没有更新，所以我也就不提 pr 了，而是将仓库进行了 fork 并修改了源码，可以直接 clone 我的仓库来搭建，流程如下：</p><h2 id="1-将代码-clone-到本地"><a href="#1-将代码-clone-到本地" class="headerlink" title="1. 将代码 clone 到本地"></a>1. 将代码 clone 到本地</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git clone git@github.com:Panmax/docker-hbase.git</span><br><span class="line">cd docker-hbase</span><br></pre></td></tr></table></figure><h2 id="2-etc-hosts-中添加以下两项"><a href="#2-etc-hosts-中添加以下两项" class="headerlink" title="2. /etc/hosts 中添加以下两项"></a>2. /etc/hosts 中添加以下两项</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0.0.0.0 hbase-master</span><br><span class="line">0.0.0.0 hbase-region</span><br></pre></td></tr></table></figure><h2 id="3-启动"><a href="#3-启动" class="headerlink" title="3. 启动"></a>3. 启动</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker-compose -f docker-compose-distributed-local.yml up</span><br></pre></td></tr></table></figure><p>等待所有服务完全启动后，就可以让我们的程序通过监听在本地 2181 端口的 zookeeper 去发现并访问 hbase 了。</p><p>Hbase 数据录入</p><p>为了验证代码逻辑，我还需要写一些数据到 hbase 中，操作如下：</p><h2 id="1-进入容器"><a href="#1-进入容器" class="headerlink" title="1. 进入容器"></a>1. 进入容器</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker exec -it hbase-master bash</span><br></pre></td></tr></table></figure><h2 id="2-进入-hbase-安装目录"><a href="#2-进入-hbase-安装目录" class="headerlink" title="2. 进入 hbase 安装目录"></a>2. 进入 hbase 安装目录</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cd /opt/hbase-1.2.6/</span><br></pre></td></tr></table></figure><h2 id="3-运行-hbase-shell"><a href="#3-运行-hbase-shell" class="headerlink" title="3. 运行 hbase shell"></a>3. 运行 hbase shell</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bin/hbase shell</span><br></pre></td></tr></table></figure><p>然后就可以使用SQL语句进行操作了，例如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">&gt; create &apos;mods_model_storage&apos;, &apos;f&apos;</span><br><span class="line">&gt; put &apos;mods_model_storage&apos;,&apos;model1&apos;,&apos;f:model&apos;, &apos;model content1&apos;</span><br><span class="line">&gt; put &apos;mods_model_storage&apos;,&apos;model2&apos;,&apos;f:model&apos;, &apos;model content2&apos;</span><br><span class="line"></span><br><span class="line">&gt; scan &apos;mods_model_storage&apos;</span><br><span class="line">ROW                     COLUMN+CELL</span><br><span class="line"> model1                 column=f:model, timestamp=1617605399565, value=model content1</span><br><span class="line"> model2                 column=f:model, timestamp=1617605400576, value=model content2</span><br><span class="line">2 row(s) in 0.0330 seconds</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;最近在重构公司的直播推荐服务，特征数据的存储使用的是 &lt;a href=&quot;https://hbase.apache.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Hbase&lt;/a&gt;，但有个问题是我们的开发环境并没有搭建 HBase 集群，开发环境和
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>关于 CDN 的灵魂 16 问</title>
    <link href="https://jpanj.com/2021/cdn-question/"/>
    <id>https://jpanj.com/2021/cdn-question/</id>
    <published>2021-03-18T02:00:04.000Z</published>
    <updated>2021-03-29T02:58:44.852Z</updated>
    
    <content type="html"><![CDATA[<h3 id="如果你把你的站点部署在-CDN-的后面，网站的-IP-地址是-CDN-还是后端服务器？"><a href="#如果你把你的站点部署在-CDN-的后面，网站的-IP-地址是-CDN-还是后端服务器？" class="headerlink" title="如果你把你的站点部署在 CDN 的后面，网站的 IP 地址是 CDN 还是后端服务器？"></a>如果你把你的站点部署在 CDN 的后面，网站的 IP 地址是 CDN 还是后端服务器？</h3><p> <strong>是 CDN 的 IP</strong></p><p> 所有请求都会发到 CDN，然后 CDN 在需要的情况下向后端服务器发出请求。</p><h3 id="CDN-是否只有一个数据中心用于缓存内容？"><a href="#CDN-是否只有一个数据中心用于缓存内容？" class="headerlink" title="CDN 是否只有一个数据中心用于缓存内容？"></a>CDN 是否只有一个数据中心用于缓存内容？</h3><p> <strong>不是</strong></p><p>CDN 会将内容缓存在不同地区的多个服务器上，这样你的用户无论在哪里都能迅速得到响应。</p><h3 id="CDN-只会缓存-HTTP-响应的-body（比如图片）吗？"><a href="#CDN-只会缓存-HTTP-响应的-body（比如图片）吗？" class="headerlink" title="CDN 只会缓存 HTTP 响应的 body（比如图片）吗？"></a>CDN 只会缓存 HTTP 响应的 body（比如图片）吗？</h3><p><strong>不是</strong></p><p>它可以缓存整个 HTTP 响应，包括状态码和响应头。</p><p>所以，举例来说，如果你的服务器不小心返回了 404，CDN 将这个响应进行了缓存，那么即使服务器已经恢复，你的网站仍然可能是 404。</p><h3 id="如果你不小心缓存了错误的内容，是否能清理掉？"><a href="#如果你不小心缓存了错误的内容，是否能清理掉？" class="headerlink" title="如果你不小心缓存了错误的内容，是否能清理掉？"></a>如果你不小心缓存了错误的内容，是否能清理掉？</h3><p><strong>是的</strong></p><p>CDN 提供方通常有清除缓存的方法。不过有时需要几分钟才能完成（CDN 可能要去告诉世界各地的数百台服务器来清理它们的缓存）。</p><p>你可以选择从缓存中只删除特定文件或者删除所有缓存的文件。</p><h3 id="CDN-如何知道它应该把-HTTP-响应放在缓存中？"><a href="#CDN-如何知道它应该把-HTTP-响应放在缓存中？" class="headerlink" title="CDN 如何知道它应该把 HTTP 响应放在缓存中？"></a>CDN 如何知道它应该把 HTTP 响应放在缓存中？</h3><p><strong>依赖于客户端的请求</strong></p><p>当 CDN 收到一个资源请求时，它会从你的服务器上请求资源，然后会把资源放在它的缓存中，这样下次就不用再到你的服务器上请求了。</p><h3 id="CDN-可以缓存任何类型的-HTTP-的响应吗？"><a href="#CDN-可以缓存任何类型的-HTTP-的响应吗？" class="headerlink" title="CDN 可以缓存任何类型的 HTTP 的响应吗？"></a>CDN 可以缓存任何类型的 HTTP 的响应吗？</h3><p><strong>是的</strong></p><p>如果你要求 CDN 进行缓存，它通常可以缓存任何你想要的 HTTP 响应，比如可以将响应头设置为： <code>Cache-Control: public; max-age=3600</code>。</p><p>不过大多数 CDN 会对缓存内容的大小进行限制，所以你可能无法缓存一个电影。</p><h3 id="CDN-是否可以在你的服务器宕机的情况下继续为你的站点提供服务？"><a href="#CDN-是否可以在你的服务器宕机的情况下继续为你的站点提供服务？" class="headerlink" title="CDN 是否可以在你的服务器宕机的情况下继续为你的站点提供服务？"></a>CDN 是否可以在你的服务器宕机的情况下继续为你的站点提供服务？</h3><p><strong>也许</strong></p><p>即使你的服务器没有运行，CDN 也可以继续提供缓存页面。</p><p>但是如果你告诉它只缓存一定时间（比如 1 小时），内容可能会在一段时间后过期，无法访问。而如果内容根本没有被缓存，CDN 也帮不了你。</p><h3 id="如果你在-CDN-后面的网站使用-TLS，CDN-能读取你未加密的网站流量吗？"><a href="#如果你在-CDN-后面的网站使用-TLS，CDN-能读取你未加密的网站流量吗？" class="headerlink" title="如果你在 CDN 后面的网站使用 TLS，CDN 能读取你未加密的网站流量吗？"></a>如果你在 CDN 后面的网站使用 TLS，CDN 能读取你未加密的网站流量吗？</h3><p><strong>是的</strong></p><p>如果你想让CDN缓存内容，它需要能够解密和读取。</p><p>通常人们处理这个问题的方法是，只把静态内容（如 CSS/JS/图片）放在 CDN 后面的域名上，而使用一个单独的域名来处理带有用户数据的请求。例如，<a href="https://github.githubassets.com/" target="_blank" rel="noopener">https://github.githubassets.com/</a> 在 CDN 后面，但<a href="https://github.com" target="_blank" rel="noopener">https://github.com</a> 不是。</p><h3 id="CDN-总是对资源进行缓存吗？"><a href="#CDN-总是对资源进行缓存吗？" class="headerlink" title="CDN 总是对资源进行缓存吗？"></a>CDN 总是对资源进行缓存吗？</h3><p><strong>不是</strong></p><p>如果你想，可以配置你的 CDN 不进行缓存，只是代理每个请求到你的后端服务器。</p><h3 id="是否可以判断出某个网站使用了-CDN？"><a href="#是否可以判断出某个网站使用了-CDN？" class="headerlink" title="是否可以判断出某个网站使用了 CDN？"></a>是否可以判断出某个网站使用了 CDN？</h3><p><strong>是的</strong></p><p>你通常可以从 <code>header</code> 中找出答案：运行curl -I <a href="https://jiapan.me，看看我使用的是什么" target="_blank" rel="noopener">https://jiapan.me，看看我使用的是什么</a> CDN。</p><h3 id="是否能判断出你收到的是一个被缓存的响应？"><a href="#是否能判断出你收到的是一个被缓存的响应？" class="headerlink" title="是否能判断出你收到的是一个被缓存的响应？"></a>是否能判断出你收到的是一个被缓存的响应？</h3><p><strong>是的</strong></p><p>CDN 通常会设置一个响应头，比如 <code>x-cache: HIT</code>，你可以用它来判断是缓存命中还是缓存失效。</p><h3 id="及时没有缓存，CDN-是否可以使请求更快？"><a href="#及时没有缓存，CDN-是否可以使请求更快？" class="headerlink" title="及时没有缓存，CDN 是否可以使请求更快？"></a>及时没有缓存，CDN 是否可以使请求更快？</h3><p><strong>是的</strong></p><ol><li>CDN通常可以在离客户端更近的地方终止 TLS，这意味着 TLS 握手可以快很多。如果你的后端服务器离客户端很远，这可以节省一秒左右的时间。这样做的原因是，它经常会与后端服务器保持一个开放的 TLS 连接，所以它不必每次都重新建立一个新的连接。</li><li>它可能比客户机有更快的路由连接到你的后端服务器。</li><li>CDN还可以通过更多的方式来提高性能!</li></ol><h3 id="如果你的站点只支持-HTTP-1-1，CDN-可以接收-HTTP-2-0-的请求吗？"><a href="#如果你的站点只支持-HTTP-1-1，CDN-可以接收-HTTP-2-0-的请求吗？" class="headerlink" title="如果你的站点只支持 HTTP/1.1，CDN 可以接收 HTTP/2.0 的请求吗？"></a>如果你的站点只支持 HTTP/1.1，CDN 可以接收 HTTP/2.0 的请求吗？</h3><p><strong>大部分情况下可以</strong></p><p>许多CDN可以透明地将 <code>HTTP/2</code> 请求翻译成 <code>HTTP/1</code> 请求到你的后端服务器，所以你可以在不做任何工作的情况下获得 <code>HTTP/2</code> 的很多性能优势。</p><h3 id="是否可以让-CDN-对响应只缓存一段时间（如10分钟）？"><a href="#是否可以让-CDN-对响应只缓存一段时间（如10分钟）？" class="headerlink" title="是否可以让 CDN 对响应只缓存一段时间（如10分钟）？"></a>是否可以让 CDN 对响应只缓存一段时间（如10分钟）？</h3><p><strong>是的</strong></p><p>您可以通过设置 <code>Cache-Control</code> 响应头来实现，比如 <code>Cache-Control: max-age=600</code>。</p><h3 id="是否允许资源只被浏览器缓存而不被-CDN-缓存？"><a href="#是否允许资源只被浏览器缓存而不被-CDN-缓存？" class="headerlink" title="是否允许资源只被浏览器缓存而不被 CDN 缓存？"></a>是否允许资源只被浏览器缓存而不被 CDN 缓存？</h3><p><strong>是的</strong></p><p>你可以通过设置 <code>Cache-Control: private, max-age=3600</code> 来实现。<code>private</code> 意味着内容只能存储在浏览器的缓存中，而不是 CDN 的缓存中。</p><h3 id="如果你用同一个-URL-请求-CDN，但-header-不同，是否会得到相同的缓存响应？"><a href="#如果你用同一个-URL-请求-CDN，但-header-不同，是否会得到相同的缓存响应？" class="headerlink" title="如果你用同一个 URL 请求 CDN，但 header 不同，是否会得到相同的缓存响应？"></a>如果你用同一个 URL 请求 CDN，但 header 不同，是否会得到相同的缓存响应？</h3><p><strong>视情况而定</strong></p><p>默认情况下，会得到相同的响应。但如果服务器设置了 <code>Vary:</code> 头，那么 CDN 将为该头的每个值存储不同的缓存值。</p><p>例如，<code>Vary: Accept-Encoding</code> 将使 CDN 同时存储压缩和非压缩版本。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3 id=&quot;如果你把你的站点部署在-CDN-的后面，网站的-IP-地址是-CDN-还是后端服务器？&quot;&gt;&lt;a href=&quot;#如果你把你的站点部署在-CDN-的后面，网站的-IP-地址是-CDN-还是后端服务器？&quot; class=&quot;headerlink&quot; title=&quot;如果你把你的
      
    
    </summary>
    
    
      <category term="cdn" scheme="https://jpanj.com/tags/cdn/"/>
    
  </entry>
  
  <entry>
    <title>git 子模块使用</title>
    <link href="https://jpanj.com/2021/git-submodule/"/>
    <id>https://jpanj.com/2021/git-submodule/</id>
    <published>2021-02-25T06:00:12.000Z</published>
    <updated>2021-02-25T06:38:53.752Z</updated>
    
    <content type="html"><![CDATA[<p>前几天调研了一下 <a href="https://gohugo.io/" target="_blank" rel="noopener">hugo</a>，准备后边把自己的博客迁移过去。hugo 教程中新增主题推荐通过 git 子模块的方式（前提是原始文件就已经在一个 git 项目下），比如我要新增一个名字叫 zen 的主题，可以通过以下命令进行安装：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule add https://github.com/frjo/hugo-theme-zen.git themes/zen</span><br></pre></td></tr></table></figure><p>这个命令会将主题仓库中的文件 clone 到 themes/zen 路径下，同时会在我的的仓库根路径下新建一个 <code>.gitmodules</code> 文件，之后 add 其他子模块时，会往这个文件中追加数据，格式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">[submodule &quot;themes/zen&quot;]</span><br><span class="line">path = themes/zen</span><br><span class="line">url = https://github.com/frjo/hugo-theme-zen.git themes/zen</span><br><span class="line">[submodule &quot;themes/cleanwhite&quot;]</span><br><span class="line">path = themes/cleanwhite</span><br><span class="line">url = https://github.com/zhaohuabing/hugo-theme-cleanwhite.git</span><br><span class="line">[submodule &quot;themes/anatole&quot;]</span><br><span class="line">path = themes/anatole</span><br><span class="line">url = https://github.com/lxndrblz/anatole</span><br><span class="line">[submodule &quot;themes/jane&quot;]</span><br><span class="line">path = themes/jane</span><br><span class="line">url = https://github.com/xianmin/hugo-theme-jane.git</span><br></pre></td></tr></table></figure><p>我们需要把 <code>.gitmodules</code> 文件加入到 git 的版本控制中。</p><h2 id="子模块常用管理命令"><a href="#子模块常用管理命令" class="headerlink" title="子模块常用管理命令"></a>子模块常用管理命令</h2><h3 id="更新子模块："><a href="#更新子模块：" class="headerlink" title="更新子模块："></a>更新子模块：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update --recursive --remote</span><br></pre></td></tr></table></figure><h3 id="在新环境中拉取所有子模块代码"><a href="#在新环境中拉取所有子模块代码" class="headerlink" title="在新环境中拉取所有子模块代码"></a>在新环境中拉取所有子模块代码</h3><p>首次在一个新的环境中 clone 我们的仓库后是不带子模块代码的，可以通过下边这个命令来把所有子模块代码拉下来：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update --init --recursive</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;前几天调研了一下 &lt;a href=&quot;https://gohugo.io/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;hugo&lt;/a&gt;，准备后边把自己的博客迁移过去。hugo 教程中新增主题推荐通过 git 子模块的方式（前提是原始文件就已经在一个 gi
      
    
    </summary>
    
    
      <category term="git" scheme="https://jpanj.com/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>Go 中 5 个常见错误</title>
    <link href="https://jpanj.com/2021/go-5-normally-mistakes/"/>
    <id>https://jpanj.com/2021/go-5-normally-mistakes/</id>
    <published>2021-02-20T09:49:13.000Z</published>
    <updated>2021-02-22T05:14:15.641Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-循环中"><a href="#1-循环中" class="headerlink" title="1. 循环中"></a>1. 循环中</h2><p>使用循环时下边几个容易尝试混乱的编码方式我们要尽量避免。</p><h3 id="1-1-对循环的变量进行引用"><a href="#1-1-对循环的变量进行引用" class="headerlink" title="1.1 对循环的变量进行引用"></a>1.1 对循环的变量进行引用</h3><p>考虑到效率，在进行循环遍历过程中，迭代出的变量会赋值到同一个地址。这可能会导致无意识的错误。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">in := []int&#123;1, 2, 3&#125;</span><br><span class="line"></span><br><span class="line">var out []*int</span><br><span class="line">for  _, v := range in &#123;</span><br><span class="line">out = append(out, &amp;v)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">fmt.Println(&quot;Values:&quot;, *out[0], *out[1], *out[2])</span><br><span class="line">fmt.Println(&quot;Addresses:&quot;, out[0], out[1], out[2])</span><br></pre></td></tr></table></figure><p>以上代码得到的结果是：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Values: 3 3 3</span><br><span class="line">Addresses: 0xc000014188 0xc000014188 0xc000014188</span><br></pre></td></tr></table></figure><p>原因很容易解释：每次迭代时我们将 <code>v</code> 的地址追加到 <code>out</code> 切片中，前边提到，<code>v</code> 在每次遍历时为同一个变量，在输出的第二行可以看到打印出了相同的地址。</p><p>简单的修复方法是，将每一次的迭代出的变量复制给一个新的变量：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">in := []int&#123;1, 2, 3&#125;</span><br><span class="line"></span><br><span class="line">var out []*int</span><br><span class="line">for  _, v := range in &#123;</span><br><span class="line">v := v</span><br><span class="line">out = append(out, &amp;v)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">fmt.Println(&quot;Values:&quot;, *out[0], *out[1], *out[2])</span><br><span class="line">fmt.Println(&quot;Addresses:&quot;, out[0], out[1], out[2])</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Values: 1 2 3</span><br><span class="line">Addresses: 0xc0000b6010 0xc0000b6018 0xc0000b6020</span><br></pre></td></tr></table></figure><p>同样的问题会出现在将迭代出的变量用在 Goroutine 中：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">list := []int&#123;1, 2, 3&#125;</span><br><span class="line"></span><br><span class="line">for _, v := range list &#123;</span><br><span class="line">go func() &#123;</span><br><span class="line">fmt.Printf(&quot;%d &quot;, v)</span><br><span class="line">&#125;()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">3 3 3</span><br></pre></td></tr></table></figure><p>这个 bug 也可以使用上边提到的方法解决。（注：如果不在 Goroutine 中执行，上边的代码是没有问题的）</p><h3 id="1-2-在循环中调用-WaitGroup-Wait"><a href="#1-2-在循环中调用-WaitGroup-Wait" class="headerlink" title="1.2 在循环中调用 WaitGroup.Wait"></a>1.2 在循环中调用 WaitGroup.Wait</h3><p>下边代码循环中的 <code>group.Wait()</code> 会被阻塞，导致无法执行后边的循环。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">var wg sync.WaitGroup</span><br><span class="line">wg.Add(len(tasks))</span><br><span class="line">for _, t := range tasks &#123;</span><br><span class="line">go func(t *task) &#123; </span><br><span class="line">defer group.Done()</span><br><span class="line">&#125;(t)</span><br><span class="line">group.Wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>正确的写法是把 <code>Wait()</code> 放在循环外：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">var wg sync.WaitGroup</span><br><span class="line">wg.Add(len(tasks))</span><br><span class="line">for _, t := range tasks &#123;</span><br><span class="line">go func(t *task) &#123; </span><br><span class="line">defer group.Done()</span><br><span class="line">&#125;(t)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">group.Wait()</span><br></pre></td></tr></table></figure><h3 id="1-3-在循环中使用-defer"><a href="#1-3-在循环中使用-defer" class="headerlink" title="1.3 在循环中使用 defer"></a>1.3 在循环中使用 defer</h3><p>只有当函数返回时，<code>defer</code> 才会被执行。除非你知道你在做什么，否则不应该将 <code>defer</code> 用在循环中。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">var mutex sync.Mutex</span><br><span class="line">type Person struct &#123;</span><br><span class="line">Age int</span><br><span class="line">&#125;</span><br><span class="line">persons := make([]Person, 10)</span><br><span class="line">for _, p := range persons &#123;</span><br><span class="line">mutex.Lock()</span><br><span class="line">defer mutex.Unlock()</span><br><span class="line">p.Age = 13</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上边的例子中，在完成第一次循环后，之后的循环无法获得互斥锁从而被阻塞。应该改成下边的显性释放锁的方式：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">var mutex sync.Mutex</span><br><span class="line">type Person struct &#123;</span><br><span class="line">Age int</span><br><span class="line">&#125;</span><br><span class="line">persons := make([]Person, 10)</span><br><span class="line">for _, p := range persons &#123;</span><br><span class="line">mutex.Lock()</span><br><span class="line"></span><br><span class="line">p.Age = 13</span><br><span class="line">mutex.Unlock()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果你确实需要在循环中使用 <code>defer</code>，可以考虑将工作委托给另一个函数：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">var mutex sync.Mutex</span><br><span class="line">type Person struct &#123;</span><br><span class="line">Age int</span><br><span class="line">&#125;</span><br><span class="line">persons := make([]Person, 10)</span><br><span class="line">for _, p := range persons &#123;</span><br><span class="line">func() &#123;</span><br><span class="line">mutex.Lock()</span><br><span class="line">defer mutex.Unlock()</span><br><span class="line">p.Age = 13</span><br><span class="line">&#125;()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-往-unbuffered-channel-中发送数据"><a href="#2-往-unbuffered-channel-中发送数据" class="headerlink" title="2. 往 unbuffered channel 中发送数据"></a>2. 往 unbuffered channel 中发送数据</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">func doReq(timeout time.Duration) obj &#123;</span><br><span class="line">ch :=make(chan obj)</span><br><span class="line">go func() &#123;</span><br><span class="line">obj := do()</span><br><span class="line">ch &lt;- obj</span><br><span class="line">&#125; ()</span><br><span class="line">select &#123;</span><br><span class="line">case result = &lt;- ch :</span><br><span class="line">return result</span><br><span class="line">case&lt;- time.After(timeout):</span><br><span class="line">return nil </span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上边的代码模拟这样一个行为：超时前获得到结果将结果返回，若超时则返回 nil。</p><p>我们通过一个 Goroutine 异步获取结果，并通过一个 <code>channel</code> 配合 <code>select</code> 来阻塞代码往后执行。</p><p>上边代码使用了 <code>unbuffered channel</code>，这会导致的问题是，如果代码因超时提前返回了，Goroutine 在获取到结果后，会阻塞在 <code>ch &lt;- obj</code> 这一行（因为没有其他的 Goroutine 来读取这个 <code>channle</code>），从而这个 Goroutine 无法退出，进而会发生 Goroutine 泄露。</p><p>解决方法是使用一个长度为 1 的 <code>buffered channel</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">func doReq(timeout time.Duration) obj &#123;</span><br><span class="line">ch := make(chan obj, 1)</span><br><span class="line">go func() &#123;</span><br><span class="line">obj := do()</span><br><span class="line">ch &lt;- result</span><br><span class="line">&#125; ()</span><br><span class="line">select &#123;</span><br><span class="line">case result = &lt;- ch :</span><br><span class="line">return result</span><br><span class="line">case&lt;- time.After(timeout):</span><br><span class="line">return nil </span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>还有一种修复方式是在 Goroutine 中使用一个 <code>select</code> 配合一个空的 <code>default</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">select &#123; </span><br><span class="line">case ch &lt;- result: </span><br><span class="line">default:</span><br><span class="line">&#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>当没有其他 Goroutine 来读取这个 <code>channel</code> 时，会走到 <code>default</code> 行为，这个 Goroutine 也就可以正常退出了。</p><h2 id="3-不使用接口"><a href="#3-不使用接口" class="headerlink" title="3. 不使用接口"></a>3. 不使用接口</h2><p>接口可以使代码更具灵活性，是在代码中引入多态的一种方法。接口允许我们关注一组行为而非特定类型。不使用接口不会有错误产生，但会让我们的代码看起来不那么优雅、不具有可扩展性。</p><p>在众多接口中，<code>io.Reader</code> 和 <code>io.Writer</code> 可能是最受欢迎的一对。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">type Reader interface &#123;</span><br><span class="line">    Read(p []byte) (n int, err error)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">type Writer interface &#123;</span><br><span class="line">    Write(p []byte) (n int, err error)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这些接口非常强大， 假设我们需要将一个对象写入一个文件，可以这样定义一个 <code>Save</code> 方法：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">func (o *obj) Save(file os.File) error</span><br></pre></td></tr></table></figure><p>如果明天我们有需要将这个文件写入 <code>http.ResponseWriter</code> 呢？我们可不想重新定义一个新的方法，这时 <code>io.Writer</code> 就派上用场了：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">func (o *obj) Save(w io.Writer) error</span><br></pre></td></tr></table></figure><p>还需明白的一点是：我们应该只关心我们要使用的行为。在上边的例子中，使用 <code>io.ReadWriteCloser</code> 虽然也行得通，但如果我们只用到了 <code>Write</code> 方法，就不是特别好的实践了。<strong>接口面积越大，抽象能力越弱。</strong></p><p>因此，在大部分情况下，我们应关注行为而不是具体类型。</p><h2 id="4-struct-中未考虑字段声明顺序"><a href="#4-struct-中未考虑字段声明顺序" class="headerlink" title="4. struct 中未考虑字段声明顺序"></a>4. struct 中未考虑字段声明顺序</h2><p>下边的代码不会出现错误，但会有使用更多的内存：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">type BadOrderedPerson struct &#123;</span><br><span class="line">Veteran bool   // 1 byte</span><br><span class="line">Name    string // 16 byte</span><br><span class="line">Age     int32  // 4 byte</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上边的 <code>struct</code> 看起来会分配 21 bytes 的内存，但实际上分配的是 32 bytes。出现这个情况原因是<a href="https://en.wikipedia.org/wiki/Data_structure_alignment" target="_blank" rel="noopener">数据结构对齐</a>。在 64 位架构中，内存以 8 bytes 为一个连续单元，改成下边的声明顺序可以优化到分配 24 bytes：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">type OrderedPerson struct &#123;</span><br><span class="line">Name    string</span><br><span class="line">Age     int32</span><br><span class="line">Veteran bool</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在频繁使用不合理字段顺序的类型时，会导致额外的内存开销。</p><p>不过，我们也不必手动计算和优化结构体内存，可以使用 <code>go tool</code> 提供的 <strong><a href="https://github.com/golang/tools" target="_blank" rel="noopener">fieldalignment</a></strong> 工具来检测并修复不合理的声明顺序。</p><h3 id="fieldalignment-安装："><a href="#fieldalignment-安装：" class="headerlink" title="fieldalignment 安装："></a>fieldalignment 安装：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cd $GOPATH</span><br><span class="line">git clone git@github.com:golang/tools.git src/golang.org/x/tools</span><br><span class="line">src/golang.org/x/tools/go/analysis/passes/fieldalignment/cmd/fieldalignment</span><br><span class="line">go install</span><br></pre></td></tr></table></figure><h3 id="fieldalignment-使用"><a href="#fieldalignment-使用" class="headerlink" title="fieldalignment 使用"></a>fieldalignment 使用</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">➜ fieldalignment .</span><br><span class="line">/Users/jiapan/Projects/tantan-live-distribution/app/domain/recommend_service.go:179:30: struct of size 88 could be 80</span><br><span class="line">/Users/jiapan/Projects/tantan-live-distribution/app/domain/voice_recommend_service.go:63:35: struct with 40 pointer bytes could be 24</span><br><span class="line"></span><br><span class="line">// 修复字段顺序</span><br><span class="line">➜ fieldalignment -fix .</span><br></pre></td></tr></table></figure><h2 id="5-test-时未使用-race-检测器"><a href="#5-test-时未使用-race-检测器" class="headerlink" title="5. test 时未使用 race 检测器"></a>5. test 时未使用 race 检测器</h2><p>数据竞争会导致一些很迷的问题，而且通常是在部署一段时间后才会发生。所以此类问题在并发系统中是最常见而且最难排查的 bug。为了更方便找出此类 bug，Go 1.1 中引入了一个内置的数据竞争检测器，只需加上 <code>-race</code> 标识就可以了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ go test -race pkg    // to test the package</span><br><span class="line">$ go run -race pkg.go  // to run the source file</span><br><span class="line">$ go build -race       // to build the package</span><br><span class="line">$ go install -race pkg // to install the package</span><br></pre></td></tr></table></figure><p>当开启竞争检测器时，编译器会记录代码对内存进行了何时、何种方式的访问，同时 <code>runtime</code> 监控共享变量的非同步访问。</p><p>发现数据竞争时，竞争检测器会打印包含访问冲突的调用栈记录，如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">WARNING: DATA RACE</span><br><span class="line">Read by goroutine 185:</span><br><span class="line">  net.(*pollServer).AddFD()</span><br><span class="line">      src/net/fd_unix.go:89 +0x398</span><br><span class="line">  net.(*pollServer).WaitWrite()</span><br><span class="line">      src/net/fd_unix.go:247 +0x45</span><br><span class="line">  net.(*netFD).Write()</span><br><span class="line">      src/net/fd_unix.go:540 +0x4d4</span><br><span class="line">  net.(*conn).Write()</span><br><span class="line">      src/net/net.go:129 +0x101</span><br><span class="line">  net.func·060()</span><br><span class="line">      src/net/timeout_test.go:603 +0xaf</span><br><span class="line">Previous write by goroutine 184:</span><br><span class="line">  net.setWriteDeadline()</span><br><span class="line">      src/net/sockopt_posix.go:135 +0xdf</span><br><span class="line">  net.setDeadline()</span><br><span class="line">      src/net/sockopt_posix.go:144 +0x9c</span><br><span class="line">  net.(*conn).SetDeadline()</span><br><span class="line">      src/net/net.go:161 +0xe3</span><br><span class="line">  net.func·061()</span><br><span class="line">      src/net/timeout_test.go:616 +0x3ed</span><br><span class="line">Goroutine 185 (running) created at:</span><br><span class="line">  net.func·061()</span><br><span class="line">      src/net/timeout_test.go:609 +0x288</span><br><span class="line">Goroutine 184 (running) created at:</span><br><span class="line">  net.TestProlongTimeout()</span><br><span class="line">      src/net/timeout_test.go:618 +0x298</span><br><span class="line">  testing.tRunner()</span><br><span class="line">      src/testing/testing.go:301 +0xe8</span><br></pre></td></tr></table></figure><hr><blockquote><p>写在最后：人类从历史中学到的唯一教训，就是人类无法从历史中学到任何教训。</p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;1-循环中&quot;&gt;&lt;a href=&quot;#1-循环中&quot; class=&quot;headerlink&quot; title=&quot;1. 循环中&quot;&gt;&lt;/a&gt;1. 循环中&lt;/h2&gt;&lt;p&gt;使用循环时下边几个容易尝试混乱的编码方式我们要尽量避免。&lt;/p&gt;
&lt;h3 id=&quot;1-1-对循环的变量进行引用&quot;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Go 结构体方法值传递与引用传递区别</title>
    <link href="https://jpanj.com/2021/struct-method-value-vs-pointer/"/>
    <id>https://jpanj.com/2021/struct-method-value-vs-pointer/</id>
    <published>2021-02-17T11:33:47.000Z</published>
    <updated>2021-02-17T12:03:06.737Z</updated>
    
    <content type="html"><![CDATA[<p>Go 结构体方法中，有一个很重要的点就是值传递和引用传递，我们通过一个例子来看下什么是值传递、什么是引用传递，二者有什么区别。</p><p>我们声明 <code>person</code> 结构体，里边有一个 <code>name</code> 字段，用两种方式实现 <code>SetName</code> 方法，分别是值传递和引用传递。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">type person struct &#123;</span><br><span class="line">    name string</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (p person) SetName1(name string) &#123;</span><br><span class="line">    p.name = name</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (p *person) SetName2(name string) &#123;</span><br><span class="line">    p.name = name</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上，<code>SetName1</code> 就是值传递，<code>SetName2</code> 为引用传递。</p><p>在 <code>main</code> 方法中，我们分别调用两个 <code>SetName</code> 方法对 <code>name</code> 进行赋值，并打印每次赋值后的 <code>name</code> 值。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    p := &amp;person&#123;&#125;</span><br><span class="line"></span><br><span class="line">    p.SetName1(&quot;张三&quot;)</span><br><span class="line">    fmt.Println(p.name)</span><br><span class="line"></span><br><span class="line">    p.SetName2(&quot;李四&quot;)</span><br><span class="line">    fmt.Println(p.name)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行这个程序得到如下结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">李四</span><br></pre></td></tr></table></figure><p>可以看到 <code>张三</code> 并没有打印出来，而 <code>李四</code> 打印了出来。</p><p>在调用 <code>SetName1</code> 时，实际上是复制了一个新的 <code>person</code>，方法内操作的也是那个新 <code>person</code> 把复制 <code>person</code> 的 <code>name</code> 改为了张三，而我们在 main 方法中打印的确是原始 <code>person</code> 的 <code>name</code> 字段。因为我们在初始化 <code>person</code> 时并没有指定 <code>name</code> 的值，所以第一次打印出来的是个空串。</p><p>对程序稍作调整，先调用 <code>SetName2</code> 再调用 <code>SetName1</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    p := &amp;person&#123;&#125;</span><br><span class="line"></span><br><span class="line">    p.SetName2(&quot;李四&quot;)</span><br><span class="line">    fmt.Println(p.name)</span><br><span class="line"></span><br><span class="line">    p.SetName1(&quot;张三&quot;)</span><br><span class="line">    fmt.Println(p.name)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这时输出的结果为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">李四</span><br><span class="line">李四</span><br></pre></td></tr></table></figure><p>第一次调用后，<code>p</code> 结构体指针中 <code>name</code> 的值已经被改为了 <code>李四</code>，接下来我们调用 <code>SetName1</code> 时，因为是复制了一个新的 <code>person</code>，并没有影响之前的 <code>person</code>，所以打印结果还是 <code>李四</code>。</p><p>**我们日常开发中，编写结构体方法时大部分情况都是用引用传递。</p><p>值传递的问题是，如果我们结构体的成员数量非常多时，每次调用方法都会进行一次拷贝，会有额外的内存开销。</p><p>验证一下值传递有没有分配新的内存：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">type person struct&#123;</span><br><span class="line">    name string</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (p person) SetName1(name string) &#123;</span><br><span class="line">    fmt.Printf(&quot;SetName1: %p\n&quot;, &amp;p)</span><br><span class="line">    p.name = name</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (p *person) SetName2(name string) &#123;</span><br><span class="line">    fmt.Printf(&quot;SetName2: %p\n&quot;, p)</span><br><span class="line">    p.name = name</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">    p := &amp;person&#123;&#125;</span><br><span class="line">    fmt.Printf(&quot;Origin: %p\n&quot;, p)</span><br><span class="line"></span><br><span class="line">    p.SetName1(&quot;张三&quot;)</span><br><span class="line">    p.SetName2(&quot;李四&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Origin: 0xc000010200</span><br><span class="line">SetName1: 0xc000010210</span><br><span class="line">SetName2: 0xc000010200</span><br></pre></td></tr></table></figure><p>可以看到，原始的 <code>person</code> 地址为 0xc000010200，在通过值传递时位置发生了改变，变为了0xc000010210，这也就意味着系统为这个新的 <code>person</code> 分配了新的内存地址，而用引用传递的方式地址是不会变的。</p><h3 id="引用传递容易犯的错误"><a href="#引用传递容易犯的错误" class="headerlink" title="引用传递容易犯的错误"></a>引用传递容易犯的错误</h3><p>我们假设要实现一个发送邮件的功能，定义一个 <code>email</code> 结构体，里边有两个成员 <code>from</code> 和 <code>to</code>，实现两个方法用来更新这两个成员变量。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">type email struct &#123;</span><br><span class="line">    from string</span><br><span class="line">    to   string</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (e *email) SetFrom(from string) &#123;</span><br><span class="line">    e.from = from</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (e *email) SetTo(to string) &#123;</span><br><span class="line">    e.to=to</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再实现一个发送邮件的方法，这里简单将 <code>from</code> 和 <code>to</code> 打印出来即可：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">func (e *email) Send() &#123;</span><br><span class="line">    fmt.Printf(&quot;from: %s, to: %s\n&quot;, e.from, e.to)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>main</code> 方法中，我们写一个循环，实现发送 10 次邮件，0 发送给 1，1 发送个 2，一次以此类推：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    e: = &amp;email&#123;&#125;</span><br><span class="line"></span><br><span class="line">    for i:=0; i&lt;10; i++ &#123;</span><br><span class="line">        e.SetFrom(fmt.Sprintf(&quot;%d&quot;, i))</span><br><span class="line">        e.SetTo(fmt.Sprintf(&quot;%d&quot;, i+1))</span><br><span class="line">        e.Send()</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">from: 0, to: 1</span><br><span class="line">from: 1, to: 2</span><br><span class="line">from: 2, to: 3</span><br><span class="line">from: 3, to: 4</span><br><span class="line">from: 4, to: 5</span><br><span class="line">from: 5, to: 6</span><br><span class="line">from: 6, to: 7</span><br><span class="line">from: 7, to: 8</span><br><span class="line">from: 8, to: 9</span><br><span class="line">from: 9, to: 10</span><br></pre></td></tr></table></figure><p>这时候，如果我们改成并发发送这些邮件，同时发给10个人，很容易就会把上边的代码改写如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    e := &amp;email&#123;&#125;</span><br><span class="line"></span><br><span class="line">    for i:=0; i&lt;10; i++ &#123;</span><br><span class="line">        go func(i int) &#123;</span><br><span class="line">            e.SetFrom(fmt.Sprintf(&quot;%d&quot;, i))</span><br><span class="line">            e.SetTo(fmt.Sprintf(&quot;%d&quot;, i+1))</span><br><span class="line">            e.Send()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    time.Sleep(1 * time.Second)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再次运行，结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">from: 2, to: 3</span><br><span class="line">from: 1, to: 2</span><br><span class="line">from: 3, to: 4</span><br><span class="line">from: 4, to: 5</span><br><span class="line">from: 5, to: 6</span><br><span class="line">from: 6, to: 7</span><br><span class="line">from: 0, to: 1</span><br><span class="line">from: 7, to: 8</span><br><span class="line">from: 9, to: 1</span><br><span class="line">from: 8, to: 9</span><br></pre></td></tr></table></figure><p>没有按照递增的顺序发送是在我们意料之中的，但是我们可以看到其中有一行输出为：<code>from: 9, to: 1</code>，这个并不是我们想要的结果。</p><p>出现这个问题的原因是在每个 <code>go routine</code> 中都是对原始 <code>email</code> 进行的修改，再并发操作的过程中，<code>from</code> 和 <code>to</code> 有可能被其他的 <code>go routine</code> 改掉，这是个非常严重的 bug。</p><h3 id="两种修改方法"><a href="#两种修改方法" class="headerlink" title="两种修改方法"></a>两种修改方法</h3><h4 id="每一次都初始化一个新的-email-结构体："><a href="#每一次都初始化一个新的-email-结构体：" class="headerlink" title="每一次都初始化一个新的 email 结构体："></a>每一次都初始化一个新的 email 结构体：</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    for i:=0; i&lt;10; i++ &#123;</span><br><span class="line">        e := &amp;email&#123;&#125;</span><br><span class="line">        go func(i int) &#123;</span><br><span class="line">            e.SetFrom(fmt.Sprintf(&quot;%d&quot;, i))</span><br><span class="line">            e.SetTo(fmt.Sprintf(&quot;%d&quot;, i+1))</span><br><span class="line">            e.Send()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    time.Sleep(1 * time.Second)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="改用值传递，并返回修改后的结构体"><a href="#改用值传递，并返回修改后的结构体" class="headerlink" title="改用值传递，并返回修改后的结构体"></a>改用值传递，并返回修改后的结构体</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import(</span><br><span class="line">    &quot;fmt&quot;</span><br><span class="line">    &quot;time&quot;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">type email struct &#123;</span><br><span class="line">    from string</span><br><span class="line">    to   string</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (e email) SetFrom(from string) email &#123;</span><br><span class="line">    e.from = from</span><br><span class="line">    return e</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (e email) SetTo(to string) email &#123;</span><br><span class="line">    e.to = to</span><br><span class="line">    return e</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func (e email) Send() &#123;</span><br><span class="line">    fmt.Printf(&quot;from: %s, to: %s\n&quot;, e.from, e.to)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">    e := &amp;email&#123;&#125;</span><br><span class="line">    </span><br><span class="line">    for i:=0; i&lt;10; i++ &#123;</span><br><span class="line">        go func(i int) &#123;</span><br><span class="line">            e.SetFrom(fmt.Sprintf(&quot;%d&quot;, i)).</span><br><span class="line">            SetTo(fmt.Sprintf(&quot;%d&quot;, i+1)).</span><br><span class="line">            Send()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    time.Sleep(1 * time.Second)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>读者可以自己想一想，为什么这种写法可以解决并发赋值出现的问题。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Go 结构体方法中，有一个很重要的点就是值传递和引用传递，我们通过一个例子来看下什么是值传递、什么是引用传递，二者有什么区别。&lt;/p&gt;
&lt;p&gt;我们声明 &lt;code&gt;person&lt;/code&gt; 结构体，里边有一个 &lt;code&gt;name&lt;/code&gt; 字段，用两种方式实现 &lt;c
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>快速搭建一个静态文件服务</title>
    <link href="https://jpanj.com/2021/quick-start-static-file-service/"/>
    <id>https://jpanj.com/2021/quick-start-static-file-service/</id>
    <published>2021-02-14T13:20:44.000Z</published>
    <updated>2021-02-14T13:30:15.640Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="1.png" alt></div><p>前几天朋友问我如何在没有 root 权限且无法编辑 Nginx 配置的条件下，搭建一个静态文件服务。</p><p>我最快想到的是用 Go 写一个程序，直接在目标机器上执行 Go 编译好的二进制文件，应该不会超过 10 行代码。</p><p>本着不重复造轮子的想法（另一个主要原因是朋友当时非常着急），尝试找了找前人造好的轮子，于是找到了这个项目：<a href="https://github.com/philippgille/serve" target="_blank" rel="noopener">https://github.com/philippgille/serve</a>。</p><p>看了一下介绍，和我的想法相同，同时提供了便于扩展的参数，而且提供了各个平台编译好的可执行文件。</p><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><h3 id="Windowns-安装"><a href="#Windowns-安装" class="headerlink" title="Windowns 安装"></a>Windowns 安装</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">scoop install serve</span><br></pre></td></tr></table></figure><h3 id="Mac-安装"><a href="#Mac-安装" class="headerlink" title="Mac 安装"></a>Mac 安装</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install philippgille/tap/serve</span><br></pre></td></tr></table></figure><h3 id="Linux-安装"><a href="#Linux-安装" class="headerlink" title="Linux 安装"></a>Linux 安装</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">wget https://download.jpanj.com/serve_v0.3.2_Linux_x64.zip</span><br><span class="line">unzip serve_v0.3.2_Linux_x64.zip .</span><br></pre></td></tr></table></figure><h2 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h2><h3 id="检查-serve-版本"><a href="#检查-serve-版本" class="headerlink" title="检查 serve 版本"></a>检查 serve 版本</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ serve -v</span><br><span class="line">serve version: v0.3.2</span><br></pre></td></tr></table></figure><h3 id="服务监听-8100-端口并将当前目录作为静态文件根目录"><a href="#服务监听-8100-端口并将当前目录作为静态文件根目录" class="headerlink" title="服务监听 8100 端口并将当前目录作为静态文件根目录"></a>服务监听 8100 端口并将当前目录作为静态文件根目录</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">$ serve -p 8100</span><br><span class="line"></span><br><span class="line">Serving &quot;.&quot; on all network interfaces (0.0.0.0) on HTTP port: 8100</span><br><span class="line"></span><br><span class="line">Local network interfaces and their IP addresses so you can pass one to your colleagues:</span><br><span class="line">      Interface      |  IPv4 Address   |              IPv6 Address</span><br><span class="line">---------------------|-----------------|----------------------------------------</span><br><span class="line">lo                   | 127.0.0.1       | ::1</span><br><span class="line">eth0                 | xxx.xxx.xx.xxx  | fe80::a8aa:ff:fe11:fb4b</span><br><span class="line">docker0              | 172.17.0.1      | fe80::42:1dff:fe8b:c5fe</span><br><span class="line">br-556c0122836a      | 172.24.0.1      | fe80::42:92ff:fe47:2965</span><br><span class="line">vethf6d94b9          |                 | fe80::dcdf:59ff:fe90:c6e3</span><br><span class="line">vethf64d185          |                 | fe80::286e:3eff:fe12:eaf9</span><br><span class="line">veth7259f52          |                 | fe80::34d3:83ff:fe97:612c</span><br><span class="line">veth0e57d69          |                 | fe80::c007:bff:fe1b:2b7b</span><br><span class="line"></span><br><span class="line">You probably want to share:</span><br><span class="line">http://xxx.xxx.xx.xxx:8100</span><br></pre></td></tr></table></figure><h3 id="d-指定文件根目录"><a href="#d-指定文件根目录" class="headerlink" title="-d 指定文件根目录"></a>-d 指定文件根目录</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serve -p 8100 -d &quot;/opt&quot;</span><br></pre></td></tr></table></figure><h3 id="a-启用-Basic-认证"><a href="#a-启用-Basic-认证" class="headerlink" title="-a 启用 Basic 认证"></a>-a 启用 Basic 认证</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serve -p 8100 -d &quot;/opt&quot; -a &quot;test:test&quot;</span><br></pre></td></tr></table></figure><div style="width:70%;margin:auto"><img src="2.png" alt></div><h3 id="s-生成一个自签名证书（7天有效期），开启-https-协议"><a href="#s-生成一个自签名证书（7天有效期），开启-https-协议" class="headerlink" title="-s 生成一个自签名证书（7天有效期），开启 https 协议"></a>-s 生成一个自签名证书（7天有效期），开启 https 协议</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serve -p 8100 -d &quot;/opt&quot; -a &quot;test:test&quot; -s</span><br></pre></td></tr></table></figure><h3 id="b-指定监听的网络接口，默认为-0-0-0-0"><a href="#b-指定监听的网络接口，默认为-0-0-0-0" class="headerlink" title="-b 指定监听的网络接口，默认为 0.0.0.0"></a>-b 指定监听的网络接口，默认为 0.0.0.0</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ serve -p 8100 -d &quot;/opt&quot; -a &quot;test:test&quot; -b &quot;0.0.0.0&quot;</span><br></pre></td></tr></table></figure><h3 id="h-查看帮助文档"><a href="#h-查看帮助文档" class="headerlink" title="-h 查看帮助文档"></a>-h 查看帮助文档</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">$ serve -h</span><br><span class="line">Usage of serve:</span><br><span class="line">  -a string</span><br><span class="line">    Require basic authentication with the given credentials (e.g. -a &quot;alice:secret&quot;)</span><br><span class="line">  -b string</span><br><span class="line">    Bind to (listen on) a specific interface. &quot;0.0.0.0&quot; is for ALL interfaces. &quot;localhost&quot; disables access from other devices. (default &quot;0.0.0.0&quot;)</span><br><span class="line">  -d string</span><br><span class="line">    The directory of static files to host (default &quot;.&quot;)</span><br><span class="line">  -hPrint the usage</span><br><span class="line">  -p string</span><br><span class="line">    Port to serve on. 8080 by default for HTTP, 8443 for HTTPS (when using the -s flag) (default &quot;8080&quot;)</span><br><span class="line">  -sServe via HTTPS instead of HTTP. Creates a temporary self-signed certificate for localhost, 127.0.0.1, &lt;hostname&gt;.local, &lt;hostname&gt;.lan, &lt;hostname&gt;.home and the determined LAN IP address</span><br><span class="line">  -tTest / dry run (just prints the interface table)</span><br><span class="line">  -vPrint the version</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;1.png&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;前几天朋友问我如何在没有 root 权限且无法编辑 Nginx 配置的条件下，搭建一个静态文件服务。&lt;/p&gt;
&lt;p&gt;我最快想到的是用 Go 写一个
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>国内服务器访问 github 加速</title>
    <link href="https://jpanj.com/2021/domestic-server-speed-github/"/>
    <id>https://jpanj.com/2021/domestic-server-speed-github/</id>
    <published>2021-02-08T10:27:17.000Z</published>
    <updated>2021-02-08T10:43:37.556Z</updated>
    
    <content type="html"><![CDATA[<p>众所周知的原因，国内访问 github 的速度非常受限，在个人电脑上还可以挂个代理之类的来提速，但是如果是在服务器上操作的话，配代理就没那么方便了。</p><p>前几天帮朋友在他的服务器上部署一个 github 上的开源项目，clone 的速度真的感人。</p><div style="width:70%;margin:auto"><img src="slow.png" alt></div><p>4k 左右的下载速度。</p><p>可以通过阿里提供的代理来解决，只需把 rep 地址中的 <code>github.com</code> 替换为 <code>github.com.cnpmjs.org/</code> 就可以了。</p><p>比如，之前的地址是：<code>https://github.com/gin-gonic/gin</code>，替换后为：<code>https://github.com.cnpmjs.org/gin-gonic/gin</code>。</p><p>效果如下：</p><div style="width:70%;margin:auto"><img src="quick.png" alt></div><p>虽然没有快的飞起，但是已经相当不错了。</p><hr><p>P.S. 域名后边的 <a href="cnpmjs.org">cnpmjs.org</a> 这个地址是提供 npm 加速的，前端童鞋可以通过 cnpm 来实现前端构建的加速。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;众所周知的原因，国内访问 github 的速度非常受限，在个人电脑上还可以挂个代理之类的来提速，但是如果是在服务器上操作的话，配代理就没那么方便了。&lt;/p&gt;
&lt;p&gt;前几天帮朋友在他的服务器上部署一个 github 上的开源项目，clone 的速度真的感人。&lt;/p&gt;
&lt;div
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>自动备份数据库并上传到 S3</title>
    <link href="https://jpanj.com/2020/auto-backup-database/"/>
    <id>https://jpanj.com/2020/auto-backup-database/</id>
    <published>2020-11-30T02:25:45.000Z</published>
    <updated>2020-11-30T02:27:21.628Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpg" alt></div><p>我开发的<a href="https://bossku.cn/" target="_blank" rel="noopener">老板管库</a>虽然没太多收入，但是还是有不少的用户量，为了节约成本，我并没有使用厂商提供的云数据库，而是在服务器本地搭了一个 MariaDB 实例。考虑到用户数据安全还是第一位的，所以我每天会通过定时任务的方式进行全量备份，并上传到我的七牛云，脚本如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span>!/bin/bash</span><br><span class="line"></span><br><span class="line">dir=$(dirname $(readlink -f "$0"))</span><br><span class="line"></span><br><span class="line">filename=bossku_$(date +%Y%m%d%H%M).sql</span><br><span class="line">echo $&#123;filename&#125;</span><br><span class="line">cd $&#123;dir&#125;</span><br><span class="line">mysqldump -h&#123;ipaddress&#125; -P&#123;port&#125; -uroot -p&#123;password&#125; bossku &gt; $&#123;filename&#125;</span><br><span class="line"></span><br><span class="line">qshell rput bosskudb $&#123;filename&#125; $&#123;filename&#125;</span><br></pre></td></tr></table></figure><p>上边的命令会生成一个以执行时间为后缀的 <code>.sql</code> 文件并上传到我的七牛云中名为 bosskudb 的bucket中，同时我还会配置这个 bucket 的生命周期，只保留近7天的数据。这实际上是套比较通用的流程，昨天恰好看到一个 repo：<a href="https://github.com/appleboy/docker-backup-database" target="_blank" rel="noopener">https://github.com/appleboy/docker-backup-database</a> 就是用来提供这套流程的封装的，看到作者又是个自己比较崇拜的开发者，于是准备上手用一用。</p><p>（P.S. appleboy 大神是个非常活跃的 golang开发者，在去年学习 go 的时候就 fo 了他）</p><p>这个工具目前支持备份 PG 和 MySQL 数据库，并上传到 S3（包括支持S3协议的 minio） 或者本地路径下，启用方式也非常方便，写个 docker-compose 文件就可以了。</p><p>以下是我的操作记录：</p><h3 id="准备环境"><a href="#准备环境" class="headerlink" title="准备环境"></a>准备环境</h3><p>首先在我的 AWS 中新建了一个 bucket，我所选择的区域为亚太地区(香港) ap-east-1，bucket 名为 bossku-db-backup。</p><div style="width:70%;margin:auto"><img src="1.png" alt></div><h3 id="准备-docker-compose-yml-脚本"><a href="#准备-docker-compose-yml-脚本" class="headerlink" title="准备 docker-compose.yml 脚本"></a>准备 docker-compose.yml 脚本</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">'3'</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">backup_mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">appleboy/docker-backup-database:mysql-5.7</span></span><br><span class="line">    <span class="attr">logging:</span></span><br><span class="line">      <span class="attr">options:</span></span><br><span class="line">        <span class="attr">max-size:</span> <span class="string">"100k"</span></span><br><span class="line">        <span class="attr">max-file:</span> <span class="string">"3"</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">STORAGE_DRIVER:</span> <span class="string">s3</span></span><br><span class="line">      <span class="attr">STORAGE_ENDPOINT:</span> <span class="string">s3.amazonaws.com</span></span><br><span class="line">      <span class="attr">STORAGE_BUCKET:</span> <span class="string">bossku-db-backup</span></span><br><span class="line">      <span class="attr">STORAGE_REGION:</span> <span class="string">ap-east-1</span></span><br><span class="line">      <span class="attr">STORAGE_PATH:</span> <span class="string">backup</span></span><br><span class="line">      <span class="attr">STORAGE_SSL:</span> <span class="string">"false"</span></span><br><span class="line">      <span class="attr">STORAGE_INSECURE_SKIP_VERIFY:</span> <span class="string">"false"</span></span><br><span class="line">      <span class="attr">ACCESS_KEY_ID:</span> <span class="string">AKI*******UFT</span></span><br><span class="line">      <span class="attr">SECRET_ACCESS_KEY:</span> <span class="string">4u********************NU</span></span><br><span class="line"></span><br><span class="line">      <span class="attr">DATABASE_DRIVER:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">DATABASE_HOST:</span> <span class="string">&#123;ip&#125;:&#123;port&#125;</span></span><br><span class="line">      <span class="attr">DATABASE_USERNAME:</span> <span class="string">root</span></span><br><span class="line">      <span class="attr">DATABASE_PASSWORD:</span> <span class="string">&#123;password&#125;</span></span><br><span class="line">      <span class="attr">DATABASE_NAME:</span> <span class="string">bossku</span></span><br><span class="line">      <span class="attr">DATABASE_OPTS:</span></span><br><span class="line"></span><br><span class="line">      <span class="attr">TIME_SCHEDULE:</span> <span class="string">"0 0 * * *"</span></span><br><span class="line">      <span class="attr">TIME_LOCATION:</span> <span class="string">Asia/Shanghai</span></span><br></pre></td></tr></table></figure><p><code>ACCESS_KEY_ID</code> 和 <code>SECRET_ACCESS_KEY</code> 获取方式可以查看：<a href="https://aws.amazon.com/cn/blogs/security/wheres-my-secret-access-key/" target="_blank" rel="noopener">Where’s My Secret Access Key?</a>。</p><p>因为我所启动的数据库实例为 MariaDB:10.2，根据<a href="https://mariadb.com/kb/en/system-variable-differences-between-mariadb-and-mysql/" target="_blank" rel="noopener">官方介绍</a>，其所对应的 MySQL 版本为 5.7，所以上边命令中的 image 我指定的是  <code>appleboy/docker-backup-database:mysql-5.7</code>。</p><div style="width:70%;margin:auto"><img src="2.png" alt></div><h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><p>测试的时候，为了方便查看效果，可以将 <code>TIME_SCHEDULE</code> 删掉，这样会立即执行，且执行一次后退出。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">docker-compose up -d</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 然后观察日志</span><br><span class="line">docker-compose logs -f</span><br><span class="line">Attaching to bossku-db-backup_backup_mysql_1_6cc39ab97f2c</span><br><span class="line">backup_mysql_1_6cc39ab97f2c | $ mysqldump --version</span><br><span class="line">backup_mysql_1_6cc39ab97f2c | mysqldump  Ver 10.13 Distrib 5.7.32, for Linux (x86_64)</span><br><span class="line">backup_mysql_1_6cc39ab97f2c | $ bash -c mysqldump -h &#123;ip&#125; -P &#123;port&#125; -u root bossku | gzip &gt; dump.sql.gz</span><br><span class="line"></span><br><span class="line">bossku-db-backup_backup_mysql_1_6cc39ab97f2c exited with code 0</span><br></pre></td></tr></table></figure><p>可以看到成功了，再到 S3 中验证一下文件有没有上传上来：</p><div style="width:70%;margin:auto"><img src="3.png" alt></div><p>文件也传成功了！</p><p>如果在最后的上传步骤遇到无权限的错误，可以通过尝试调整 bucket 权限来解决。</p><h3 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h3><p>通过日志和上传上来的文件名可以看出，其实他也是通过 <code>mysqldump</code> 先生成备份文件，然后通过 S3 的 SDK 进行上传，同时也是使用了日期最为文件名的命名方式。我也大致看了下代码，所使用的 SDK 为 minio 提供的，这样又可以同时支持上传到 minio 了。</p><p>创新就是将一些已有的东西进行重新组合，比如这里只是将 <code>docker</code>、<code>mysqldump</code>和<code>S3</code> 进行了组合，就创造出了这么一个好用且通用的工具，非常值得学习。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;我开发的&lt;a href=&quot;https://bossku.cn/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;老板管库&lt;/a&gt;虽
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>使用 Go 语言时没有关注值传递和误用 for 循环导致的 bug</title>
    <link href="https://jpanj.com/2020/go-value-for-mistake/"/>
    <id>https://jpanj.com/2020/go-value-for-mistake/</id>
    <published>2020-11-26T02:56:39.000Z</published>
    <updated>2020-11-26T03:42:48.723Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="1.jpg" alt></div><p>我们的业务代码中习惯使用 <code>Map</code> 维护一些 <code>LocalCache</code>，前两天发现自己维护的一个 <code>LocalCache</code> 数据有些不对：Cache 的 <code>Key</code> 为某个对象的ID，值为这个ID对应的 <code>PO</code>（即数据库中的对象），调试时发现所有的 <code>Key</code> 对应的值都是一样的，这是因为自己对一些细节没有关注到，还把 Java 那套东西搬来用导致的问题。</p><p>为了简化，我就不把业务代码搬上来了，写个简单的示例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">&quot;encoding/json&quot;</span><br><span class="line">&quot;fmt&quot;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">type Student struct &#123;</span><br><span class="line">ID   int</span><br><span class="line">Name string</span><br><span class="line">Age  int</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line"></span><br><span class="line">var students []Student</span><br><span class="line">students = append(students, Student&#123;</span><br><span class="line">ID: 1,</span><br><span class="line">Name: &quot;张三&quot;,</span><br><span class="line">Age: 18,</span><br><span class="line">&#125;)</span><br><span class="line">students = append(students, Student&#123;</span><br><span class="line">ID: 2,</span><br><span class="line">Name: &quot;李四&quot;,</span><br><span class="line">Age: 19,</span><br><span class="line">&#125;)</span><br><span class="line">students = append(students, Student&#123;</span><br><span class="line">ID: 3,</span><br><span class="line">Name: &quot;王五&quot;,</span><br><span class="line">Age: 20,</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">studentMap := make(map[int]*Student, len(students))</span><br><span class="line">for _, student := range students &#123;</span><br><span class="line">studentMap[student.ID] = &amp;student</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bs, _ := json.Marshal(studentMap)</span><br><span class="line">fmt.Println(string(bs))</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上边代码输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;&quot;1&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;,&quot;2&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;,&quot;3&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;&#125;</span><br></pre></td></tr></table></figure><p>可以看到所有的 <code>value</code> 是同一个 <code>Student</code>，为什么会出现这样的问题呢？因为 <code>students</code> 存储的是 <code>Student</code> 的值，在给 <code>for</code> 循环中的 <code>student</code> 赋值时，是复制了一个新的值给它，而 <code>for</code> 循环中的 <code>student</code> 变量所指向的地址是不变的。</p><p>可以打印 student 的地址看一下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">for _, student := range students &#123;</span><br><span class="line">fmt.Printf(&quot;%p \n&quot;, &amp;student)</span><br><span class="line">studentMap[student.ID] = &amp;student</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0xc0000a6040 </span><br><span class="line">0xc0000a6040 </span><br><span class="line">0xc0000a6040 </span><br><span class="line">&#123;&quot;1&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;,&quot;2&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;,&quot;3&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;&#125;</span><br></pre></td></tr></table></figure><p>这种情况下我们应该用 <code>students</code> 中索引对应数据的指针，上边 for 循环修改如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">for i, student := range students &#123;</span><br><span class="line">fmt.Printf(&quot;%p \n&quot;, &amp;students[i])</span><br><span class="line">studentMap[student.ID] = &amp;students[i]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0xc0000b8000 </span><br><span class="line">0xc0000b8020 </span><br><span class="line">0xc0000b8040 </span><br><span class="line">&#123;&quot;1&quot;:&#123;&quot;ID&quot;:1,&quot;Name&quot;:&quot;张三&quot;,&quot;Age&quot;:18&#125;,&quot;2&quot;:&#123;&quot;ID&quot;:2,&quot;Name&quot;:&quot;李四&quot;,&quot;Age&quot;:19&#125;,&quot;3&quot;:&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;&#125;</span><br></pre></td></tr></table></figure><p>上边的情况给 <code>student</code> 赋值也是有问题的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">for _, student := range students &#123;</span><br><span class="line">student.Name = &quot;test&quot;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bs, _ := json.Marshal(students)</span><br><span class="line">fmt.Println(string(bs))</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[&#123;&quot;ID&quot;:1,&quot;Name&quot;:&quot;张三&quot;,&quot;Age&quot;:18&#125;,&#123;&quot;ID&quot;:2,&quot;Name&quot;:&quot;李四&quot;,&quot;Age&quot;:19&#125;,&#123;&quot;ID&quot;:3,&quot;Name&quot;:&quot;王五&quot;,&quot;Age&quot;:20&#125;]</span><br></pre></td></tr></table></figure><hr><p>Java 写习惯了就以为迭代时的 <code>student</code> 指向的是 <code>students</code> 中的地址。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;我们的业务代码中习惯使用 &lt;code&gt;Map&lt;/code&gt; 维护一些 &lt;code&gt;LocalCache&lt;/code&gt;，前两天发现自己维护的一个 &lt;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>make slice 后 append 产生的问题</title>
    <link href="https://jpanj.com/2020/go-make-slice-question/"/>
    <id>https://jpanj.com/2020/go-make-slice-question/</id>
    <published>2020-11-23T10:49:42.000Z</published>
    <updated>2020-11-30T09:57:59.888Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="1.jpg" alt></div><p>今天又踩到了一个 go 语言的坑，其实也不算坑，本质上还是自己对这门语言的不熟悉。</p><p>来看一下我犯的错误，直接上代码：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">func Int64ToStrings(ids []int64) []string &#123;</span><br><span class="line">strs := make([]string, len(ids))</span><br><span class="line">for i, id := range ids &#123;</span><br><span class="line">strs = append(strs, strconv.FormatInt(id, 10))</span><br><span class="line">&#125;</span><br><span class="line">return strs</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我的需求很简单，将一个 <code>int64</code> 的切片转为 <code>string</code> 类型的切片，写这段代码的时候想到可以预先分配 <code>slice</code> 的大小，所以写了开头的 <code>make</code>，后边就直接逐个将转为 <code>string</code> 的元素 <code>append</code> 进去了。</p><p>但是，<code>make</code> 时实际已经帮我完成了里边指定数量元素的初始化，即：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">s = make([]string, 5) // s == []string&#123;&quot;&quot;, &quot;&quot;, &quot;&quot;, &quot;&quot;, &quot;&quot;&#125;</span><br></pre></td></tr></table></figure><p>所以我之后再往里边 <code>append</code> 时是往最后一个空字符串后边追加元素。</p><p>修复后的代码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">func Int64ToStrings(ids []int64) []string &#123;</span><br><span class="line">strs := make([]string, len(ids))</span><br><span class="line">for i, id := range ids &#123;</span><br><span class="line">strs[i] = strconv.FormatInt(id, 10)</span><br><span class="line">&#125;</span><br><span class="line">return strs</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>或者在初始化时指定 <code>slice</code> 长度为0，容量为我们需要的长度：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">func Int64ToStrings(ids []int64) []string &#123;</span><br><span class="line">strs := make([]string, 0, len(ids))</span><br><span class="line">for i, id := range ids &#123;</span><br><span class="line">strs = append(strs, strconv.FormatInt(id, 10))</span><br><span class="line">&#125;</span><br><span class="line">return strs</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;1.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;今天又踩到了一个 go 语言的坑，其实也不算坑，本质上还是自己对这门语言的不熟悉。&lt;/p&gt;
&lt;p&gt;来看一下我犯的错误，直接上代码：&lt;/p&gt;
&lt;fi
      
    
    </summary>
    
    
      <category term="go" scheme="https://jpanj.com/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>Big Sur 开启HiDPI</title>
    <link href="https://jpanj.com/2020/big-sur-turn-on-HiDPI/"/>
    <id>https://jpanj.com/2020/big-sur-turn-on-HiDPI/</id>
    <published>2020-11-23T02:34:05.000Z</published>
    <updated>2020-11-23T02:42:05.157Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpeg" alt></div><p>上周我的电脑升级了 Big Sur，果不其然之前配置的 HiDPI 失效了。而且这次苹果做了更严格的限制，即便禁用 SIP 也无法对系统目录进行修改了。</p><p>网上找了很多 Big Sur 开启 HiDPI 的方法，最后找到一种有效的方式，记录在这里，为了方便自己查阅，也希望能帮助到其他人。</p><h3 id="运行下边这条命令："><a href="#运行下边这条命令：" class="headerlink" title="运行下边这条命令："></a>运行下边这条命令：</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bash -c &quot;$(curl -fsSL https://raw.githubusercontent.com/xzhih/one-key-hidpi/master/hidpi.sh)&quot;</span><br></pre></td></tr></table></figure><h3 id="执行路径为："><a href="#执行路径为：" class="headerlink" title="执行路径为："></a>执行路径为：</h3><ol><li>选择自己的外接显示器</li><li>开启HIDPI(同时注入EDID)</li><li>保持原样</li><li>手动输入分辨率</li></ol><p>最后手动输入我需要的分辨率：<code>1920x1080 2560x1440</code>，重启后就可以通过 RDM（<a href="https://github.com/avibrazil/RDM" target="_blank" rel="noopener">https://github.com/avibrazil/RDM</a>） 来开启 HiDPI 了。</p><div style="width:70%;margin:auto"><img src="1.png" alt></div><h3 id="如果无效的话"><a href="#如果无效的话" class="headerlink" title="如果无效的话"></a>如果无效的话</h3><p>尝试删除 <code>/Library/Displays/Contents/Resources/Overrides/DisplayVendorID-xxx</code> 目录后再试一次（xxx 为你的 VendorID）。</p><blockquote><p>我之前一直失败就是用这个方式才成功的，估计是用其他方法写入了脏数据，<code>hidpi.sh</code> 脚本的数据一直写入失败，需要手动删除一下脏数据。</p></blockquote><h3 id="参考："><a href="#参考：" class="headerlink" title="参考："></a>参考：</h3><p><a href="https://github.com/xzhih/one-key-hidpi/issues/136" target="_blank" rel="noopener">https://github.com/xzhih/one-key-hidpi/issues/136</a><br><a href="https://blog.chajian110.com/macOS/32.html" target="_blank" rel="noopener">https://blog.chajian110.com/macOS/32.html</a><br><a href="https://blog.csdn.net/ymyz1229/article/details/109676446" target="_blank" rel="noopener">https://blog.csdn.net/ymyz1229/article/details/109676446</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpeg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;上周我的电脑升级了 Big Sur，果不其然之前配置的 HiDPI 失效了。而且这次苹果做了更严格的限制，即便禁用 SIP 也无法对系统目录进行
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>由于粗心，Go 变量作用域导致的问题</title>
    <link href="https://jpanj.com/2020/go-field-scope-question/"/>
    <id>https://jpanj.com/2020/go-field-scope-question/</id>
    <published>2020-11-19T08:53:04.000Z</published>
    <updated>2020-11-19T09:03:52.317Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpg" alt></div><p>昨天写代码的时候因为变量作用域的问题被坑了好久，在这里记录一下，避免今后再犯。</p><p>先看下面这段代码，大致功能是为传进来的引用填充一个带有自增的 <code>ID</code> 的对象，同时这个 <code>ID</code> 中不能包含 4，自增 <code>ID</code> 是使用 Redis 的 <code>incr</code> 来维护的。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">func fillNewUserLiveRightAttribute(ctx context.Context, userLiveRight *po.UserLiveRight, right *po.LiveRight) error &#123;</span><br><span class="line">if right.Type == util.LiveRightTypeMystery &#123;</span><br><span class="line">var incrId int64</span><br><span class="line">// 跳过 ID 中包含 4 的情况</span><br><span class="line">for &#123;</span><br><span class="line">incrId, err := cache.GetUserLiveRightCacheRepository().GetUserLiveRightIncrID(ctx, right.ID)</span><br><span class="line">if err != nil &#123;</span><br><span class="line">return err</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">incrIdStr := strconv.FormatInt(incrId, 10)</span><br><span class="line"></span><br><span class="line">if !strings.Contains(incrIdStr, &quot;4&quot;) &#123;</span><br><span class="line">break</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">userLiveRight.Attribute = &amp;po.UserLiveRightAttribute&#123;ID: incrId&#125;</span><br><span class="line">&#125;</span><br><span class="line">return nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是之后发现填充进去的 <code>ID</code> 永远是 0，检查了一下 Redis 中 那个自增 ID 也确实存在。</p><p>这里当时还饶了一下远路，因为那个 <code>Attribute</code>  字段在数据库中使用 jsonb 存储的，所以我前期先检查了插入时执行的 <code>SQL</code> 语句，发现每次 <code>Attribute</code> 都是打印的 <code>{}</code>，就以为是我自己没有赋上值。真实的原因是我指定了 <code>Attribute</code> 中的 <code>ID</code> 字段在转 Json 时启用 <code>omitempty</code> ，即：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">type UserLiveRightAttribute struct &#123;</span><br><span class="line">    ID int64 `json:&quot;id,omitempty&quot;`</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 <code>omitempty</code> 可以告诉 <code>Marshal</code> 函数如果 <code>field</code> 的值是对应类型的 <code>zero-value</code>，那么序列化之后的 JSON object 中不包含此 字段，所以 <code>ID=0</code> 转 Json 后自然就没有这个字段了。</p><p>回到为啥上边的代码拿到的 <code>ID</code> 总是 0 的问题：因为 <code>for</code> 中赋值的 <code>incrId</code> 是在一个新的作用域内，只在 for 的花括号内有效，退出 for  后拿到的是最开始初始化 <code>incrId</code> 的  0 值，这里使用的 := 进行的赋值，因为 err 是个新字段，所以并没有提示错误。</p><p>修复方法很简单，err 也在作用域外声明，里边使用 <code>=</code> 来赋值。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">func fillNewUserLiveRightAttribute(ctx context.Context, userLiveRight *po.UserLiveRight, right *po.LiveRight) error &#123;</span><br><span class="line">if right.Type == util.LiveRightTypeMystery &#123;</span><br><span class="line">var incrId int64</span><br><span class="line">var err error</span><br><span class="line">// 跳过 ID 中包含 4 的情况</span><br><span class="line">for &#123;</span><br><span class="line">incrId, err = cache.GetUserLiveRightCacheRepository().GetUserLiveRightIncrID(ctx, right.ID)</span><br><span class="line">if err != nil &#123;</span><br><span class="line">return err</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">incrIdStr := strconv.FormatInt(incrId, 10)</span><br><span class="line"></span><br><span class="line">if !strings.Contains(incrIdStr, &quot;4&quot;) &#123;</span><br><span class="line">break</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">userLiveRight.Attribute = &amp;po.UserLiveRightAttribute&#123;ID: incrId&#125;</span><br><span class="line">&#125;</span><br><span class="line">return nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;昨天写代码的时候因为变量作用域的问题被坑了好久，在这里记录一下，避免今后再犯。&lt;/p&gt;
&lt;p&gt;先看下面这段代码，大致功能是为传进来的引用填充一个带
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>非常好用的代码速查工具 cheat.sh</title>
    <link href="https://jpanj.com/2020/code-serch-cheat-sh/"/>
    <id>https://jpanj.com/2020/code-serch-cheat-sh/</id>
    <published>2020-11-09T06:11:26.000Z</published>
    <updated>2020-11-09T06:39:36.282Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpg" alt></div><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>cheat.sh 号称自己提供了世界优质技术社区中代码速查表的统一访问。</p><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl https://cht.sh/:cht.sh | sudo tee /usr/local/bin/cht.sh</span><br><span class="line">chmod +x /usr/local/bin/cht.sh</span><br></pre></td></tr></table></figure><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>基本的查询命令为：<code>cht.sh 语言 问题关键词</code></p><h3 id="例1：查命令"><a href="#例1：查命令" class="headerlink" title="例1：查命令"></a>例1：查命令</h3><p>比如，你不知道上边安装命令中 <code>tee</code> 是什么意思，可以尝试用下边的命令查看提示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cht.sh linux tee</span><br></pre></td></tr></table></figure><div style="width:70%;margin:auto"><img src="1.png" alt></div><p>由此可以看出 <code>cht.sh</code> 不限于查代码，还可以查命令的用法。</p><h3 id="例2：查函数"><a href="#例2：查函数" class="headerlink" title="例2：查函数"></a>例2：查函数</h3><p>再举个例子，假如我不知道 <code>go</code> 中有没有能够判断字符串中是否包含某个字符的函数，可以使用：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cht.sh go string contain</span><br></pre></td></tr></table></figure><div style="width:70%;margin:auto"><img src="2.png" alt></div><h3 id="例3：查实现"><a href="#例3：查实现" class="headerlink" title="例3：查实现"></a>例3：查实现</h3><p>或者我想知道 <code>go</code> 中如何反转一个 <code>list</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cht.sh go reverse list</span><br></pre></td></tr></table></figure><div style="width:70%;margin:auto"><img src="3.png" alt></div><h2 id="进入交互模式"><a href="#进入交互模式" class="headerlink" title="进入交互模式"></a>进入交互模式</h2><p>想进入 <code>cht.sh</code> 的交互模式，需要先安装 <code>rlwrap</code> 这个工具。</p><p>Mac 安装方式：<code>brew install rlwrap</code></p><p>进入交互模式的命令为：<code>cht.sh --shell</code></p><p>进入交互模式后，就不用再输入 <code>cht.sh</code> 的命令了，直接问问题就可以，比如我想知道 go 中如何将 int 转为 string：</p><div style="width:70%;margin:auto"><img src="4.png" alt></div><p>更进一步，如果想在后续的查询者固定查询某个语言，可以通过 <code>cd</code> 命令，这样在后续的查询中连语言都可以省掉：</p><div style="width:70%;margin:auto"><img src="5.png" alt></div><p>也可以在进入交互界面时指定语言 <code>cht.sh --shell go</code>：</p><div style="width:70%;margin:auto"><img src="7.png" alt></div><p>在指定了语言的交互界面中，如果想在不 <code>cd</code> 到其他语言的情况下临时查询其他语言的用法，可以通过以 / 开头 临时指定语言：</p><div style="width:70%;margin:auto"><img src="6.png" alt></div><h3 id="更多用法可以参考项目的-README-md"><a href="#更多用法可以参考项目的-README-md" class="headerlink" title="更多用法可以参考项目的 README.md"></a>更多用法可以参考项目的 README.md</h3><p><a href="https://github.com/chubin/cheat.sh" target="_blank" rel="noopener">https://github.com/chubin/cheat.sh</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpg&quot; alt&gt;&lt;/div&gt;

&lt;h2 id=&quot;介绍&quot;&gt;&lt;a href=&quot;#介绍&quot; class=&quot;headerlink&quot; title=&quot;介绍&quot;&gt;&lt;/a&gt;介绍&lt;/h2&gt;&lt;p&gt;cheat.
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>GoLand Protobuf 语法检查</title>
    <link href="https://jpanj.com/2020/goland-protobuf-syntax-check/"/>
    <id>https://jpanj.com/2020/goland-protobuf-syntax-check/</id>
    <published>2020-11-04T10:50:33.000Z</published>
    <updated>2020-11-04T10:55:24.008Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpg" alt></div><p>公司的服务间调用使用的 gRPC，所以开发过程中需要写一些 <code>.proto</code> 文件来生成 <code>pb</code>，写的时候借助语法检查可以更高效一些。</p><p>GoLand 中提供了一个叫 <code>Protoco Buffer Editor</code> 的插件，但是这个插件在我的环境中是有 bug 的，无法处理 import 进来的包。</p><p>询问同事他们的都没有问题，所以我从网上查了一下这个问题，网上推荐的插件名叫 <code>Protobuf Support</code>，显然我的 IDE 的插件市场中没有这个插件，又借助搜索引擎找到了这个插件的描述页面，看到已经被打上 deprecated 的标签了。</p><div style="width:70%;margin:auto"><img src="1.png" alt></div><p>我猜测这个插件之前可以用，后来被官方下掉了，我的同事们是在下掉之前安装的，为了印证我的想法，让其中一个同事看了一下他的插件名，果然是 <code>Protobuf Support</code>。</p><p>好歹顺着官方页面找到了GitHub 的地址 <a href="https://github.com/ksprojects/protobuf-jetbrains-plugin" target="_blank" rel="noopener">https://github.com/ksprojects/protobuf-jetbrains-plugin</a> ，又在 <a href="https://github.com/ksprojects/protobuf-jetbrains-plugin/releases/tag/v0.13.0" target="_blank" rel="noopener">Releases</a> 页面中找到了插件的压缩包，通过压缩包在本地进行了安装，并 disable 了 <code>Protoco Buffer Editor</code> 插件，重启 GoLand 后问题解决。</p><hr><p>今天一下午光处理坑了，上线了一个限流功能，一直没有拿到打点数据，和治理组同事检查了所有地方都没有问题，最后治理组同事想起来，需要触发限流后才会打点，在监控中看到数据。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;公司的服务间调用使用的 gRPC，所以开发过程中需要写一些 &lt;code&gt;.proto&lt;/code&gt; 文件来生成 &lt;code&gt;pb&lt;/code&gt;，写
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>《高效能人士的七个习惯》 脑图 &amp;&amp; 好句</title>
    <link href="https://jpanj.com/2020/the-7-habits-of-highly-effective-people/"/>
    <id>https://jpanj.com/2020/the-7-habits-of-highly-effective-people/</id>
    <published>2020-10-16T12:19:09.000Z</published>
    <updated>2020-10-16T12:21:45.869Z</updated>
    
    <content type="html"><![CDATA[<p><img src="1.png" alt></p><hr><h3 id="好句"><a href="#好句" class="headerlink" title="好句"></a>好句</h3><p>对自己要有耐心，因为自我成长是神圣的，同时也是脆弱的，是人生中最大规模的投资。</p><p>人的一生包含了许多成长和进步阶段，必须循序渐进，每一步都十分重要，并且需要时间，不能挑过。</p><p>承认自己的无知往往是求知的第一步。</p><p>教育孩子应该有充分的耐心让他们体会拥有的感觉，同时用足够的智慧告诉他们付出的价值，另外还要以身作则。</p><p>人们越是依赖立竿见影的解决办法，越是加剧了问题潜在的隐患。</p><p>如果不能持续投资以增进自己的产能，眼光就会受到局限，只能在现有的职位上踏步。p45</p><p>所有积极主动的人都深谙其道，不会把自己的行为归咎于环境、外界条件或他人的影响。p57</p><p>对力不能及之事处之泰然，对能够改变的则全力以赴。p65</p><p>对于已经无法返回的错误，积极主动的人不是懊恨不已，而是承认往日错误已属关注圈的事实，那是人力无法企及的范畴，既不能从头来过，也不能改变结果。p65</p><p>学会做照亮他人的蜡烛，而不是评判对错的法官；以身作则，而不是一心挑错；解决问题，而不是制造事端。p67</p><p>如果你一直认为问题「存在于外部」，那么请马上打住，因为这种想法本身就是问题。p67</p><p>太多人成功之后，反而感到空虚；得到名利之后，却发现牺牲了更可贵的东西。因此我们务必紧盯真正重要的愿景，然后勇往直前坚持到底，使生活充满意义。p69</p><p>管理是正确地做事，领导则是做正确的事。p74</p><p>一个人的应变能力取决于他对自己的本性、人生目标以及价值观不变信念。p78</p><p>有效管理是掌握重点式的管理，它把最重要的事放在第一位。由领导决定什么是重点后，再靠自制力来掌握重点，时刻把他们放在第一位，以免被感觉、情绪或冲动所左右。p96</p><p>对人不讲效率，对事才可如此。对人应讲效用，即某一行为是否有效。p109</p><p>管理者注重建立制度，然后汇集群力共同完成工作。p111</p><p>信任是促使人们进不的最大动力，因为信任能够让人们表现出自己最好的一面。p113</p><p>责任型授权是关于授权的全新思维方式，它改变了人际关系的性质：因为分得工作的人成为自己的老板，受自己内心良知的指引，努力兑现自己的承诺，达到既定目标。p114</p><p>当孩子感觉受重视的时候，亲子之间就建起了一座爱与信任的坚实桥梁。p126</p><p>经验表明，在家族式或者建立在友谊基础上的生意启动之前，最好先就「不能双赢就好聚好散」这一点达成协议，这样的繁荣才不会导致关系的破裂。p133</p><p>成熟就是在表达自己的情感和信念的同时又能体谅他人的想法和感受的能力。p135</p><p>领导所要做的就是放手，让有责任心、积极处世以及具有自我领导能力的人独立完成任务。p140</p><p>双赢协议是管理的核心内容。有了这样的一个协议，员工就可以在协议规定的范围内进行有效的自我管理，而经理就像是赛跑中的开路车一样，待一切顺利开展后悄悄退出，做好后勤工作。p141</p><p>你应该时刻想着先理解别人，这是你力所能及的。如果你把精力放在影响圈内，就能真正且深入地了解对方。你会获得准确的信息，能迅速抓住事件的核心，建立自己的情感账户，还能给对方提供给你有效合作所必须的「心里空气」。p157</p><p>与所见略同嗯人沟通，益处不大，要有分歧才有收获。p159</p><p>不要在意别人的无理行径，避开那些消极力量，发现并利用别人的优势，提高自己的认识，扩展自己的视野。p172</p><p>工作本身并不能带来经济上的安全感，具备良好的思考、学习、创造与适应能力，才能立于不败之地。p175</p><p>我们越擅长发觉别人的潜力，越能在配偶、子女、同事或雇员身上发挥自己的想象力，而不是记忆力。p183</p><p>良知是一种天赋，帮助我们判断自己是否背离了正确的原则，然后引导我们向这些原则靠拢。p186</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;1.png&quot; alt&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&quot;好句&quot;&gt;&lt;a href=&quot;#好句&quot; class=&quot;headerlink&quot; title=&quot;好句&quot;&gt;&lt;/a&gt;好句&lt;/h3&gt;&lt;p&gt;对自己要有耐心，因为自我成长是神圣的，同时也是脆弱的，是人生中最大规模的
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>解决低版本 GoLand 启动服务报 Version of Delve is too old for this version of Go</title>
    <link href="https://jpanj.com/2020/resolve-old-goland-delve-bug/"/>
    <id>https://jpanj.com/2020/resolve-old-goland-delve-bug/</id>
    <published>2020-10-09T12:35:55.000Z</published>
    <updated>2020-10-09T12:52:26.453Z</updated>
    
    <content type="html"><![CDATA[<div style="width:70%;margin:auto"><img src="0.jpg" alt></div><p>今天算是入职新公司的第一天，配置好开发环境后，尝试用 GoLand 来启动服务，结果报了：<code>Version of Delve is too old for this version of Go (maximum supported version 1.13, suppress this error with --check-go-version=false)</code> 这个错误。</p><p>查询后发现这个是 JetBrain 在将 delve 嵌入到 他们的 IDE 时导致的 bug，按照官方的说法是升级 IDE 就可以解决了。详细讨论见这个 issue：<a href="https://github.com/go-delve/delve/issues/1710" target="_blank" rel="noopener">https://github.com/go-delve/delve/issues/1710</a></p><p>但是我的 ToolBox 在 Check for updates 时没有响应，所以需要通过其他方式进行了解决。</p><h3 id="更新-dlv，并将-GoLand-中的-dlv-路径指向更新后的路径"><a href="#更新-dlv，并将-GoLand-中的-dlv-路径指向更新后的路径" class="headerlink" title="更新 dlv，并将 GoLand 中的 dlv 路径指向更新后的路径"></a>更新 dlv，并将 GoLand 中的 dlv 路径指向更新后的路径</h3><p>1) <code>go get -u github.com/go-delve/delve/cmd/dlv</code><br>2) 执行以下命令并将打印的路径复制下来：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">➜ echo `go env | grep GOPATH | cut -d &quot;\&quot;&quot; -f 2`/bin/dlv</span><br><span class="line"></span><br><span class="line"># 以下是打印的结果，进行复制</span><br><span class="line">/Users/jiapan/go/bin/dlv`</span><br></pre></td></tr></table></figure><p>3) 在 GoLand 中 Help -&gt; Edit Custom Properties（之前没编辑过会提示新建）<br>4) 新增一项 <code>dlv.path={你复制的路径}</code>，比如我的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dlv.path=/Users/jiapan/go/bin/dlv</span><br></pre></td></tr></table></figure><p>再次启动服务，问题解决。</p><blockquote><p>delve 是 go 语言的 debug 工具，delve 的意思是：钻研、探索，用这个来命名一个 debug 工具还是非常形象的。</p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;div style=&quot;width:70%;margin:auto&quot;&gt;&lt;img src=&quot;0.jpg&quot; alt&gt;&lt;/div&gt;

&lt;p&gt;今天算是入职新公司的第一天，配置好开发环境后，尝试用 GoLand 来启动服务，结果报了：&lt;code&gt;Version of Delve is to
      
    
    </summary>
    
    
  </entry>
  
</feed>
