<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>贾攀家</title>
  <subtitle>Panmax&#39;s Blog</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://jpanj.com/"/>
  <updated>2017-07-19T02:33:41.000Z</updated>
  <id>https://jpanj.com/</id>
  
  <author>
    <name>Panmax</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>书单</title>
    <link href="https://jpanj.com/2333/book-list/"/>
    <id>https://jpanj.com/2333/book-list/</id>
    <published>2333-12-30T16:00:00.000Z</published>
    <updated>2017-07-19T02:33:41.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="2017"><a href="#2017" class="headerlink" title="2017"></a>2017</h2><h3 id="1月"><a href="#1月" class="headerlink" title="1月"></a>1月</h3><ul>
<li>摆渡人</li>
<li>了不起的盖茨比</li>
</ul>
<h3 id="2月"><a href="#2月" class="headerlink" title="2月"></a>2月</h3><ul>
<li>偷影子的人</li>
<li>Python 高手之路 (The Hacker’s Guide to Python)</li>
<li>富爸爸，穷爸爸</li>
</ul>
<h3 id="3月"><a href="#3月" class="headerlink" title="3月"></a>3月</h3><ul>
<li>小狗钱钱</li>
<li>岛上书店</li>
<li>嫌疑人X的献身</li>
</ul>
<h3 id="4月"><a href="#4月" class="headerlink" title="4月"></a>4月</h3><ul>
<li>无声告白</li>
</ul>
<hr>
<h2 id="2016"><a href="#2016" class="headerlink" title="2016"></a>2016</h2><h3 id="1月-1"><a href="#1月-1" class="headerlink" title="1月"></a>1月</h3><ul>
<li>Redis 入门指南（第2版）</li>
</ul>
<h3 id="3月-1"><a href="#3月-1" class="headerlink" title="3月"></a>3月</h3><ul>
<li>第七天（余华）</li>
<li>三体·地球往事</li>
<li>动物农场</li>
</ul>
<h3 id="4月-1"><a href="#4月-1" class="headerlink" title="4月"></a>4月</h3><ul>
<li>黄金时代</li>
<li>三体Ⅱ·黑暗森林</li>
<li>React Native 入门与实战</li>
</ul>
<h3 id="5月"><a href="#5月" class="headerlink" title="5月"></a>5月</h3><ul>
<li>兄弟（余华）</li>
</ul>
<h3 id="6月"><a href="#6月" class="headerlink" title="6月"></a>6月</h3><ul>
<li>三体Ⅲ·死神永生</li>
</ul>
<h3 id="7月"><a href="#7月" class="headerlink" title="7月"></a>7月</h3><ul>
<li>代码的未来</li>
<li>白银时代</li>
</ul>
<h3 id="8月"><a href="#8月" class="headerlink" title="8月"></a>8月</h3><ul>
<li>解忧杂货店</li>
</ul>
<h3 id="9月"><a href="#9月" class="headerlink" title="9月"></a>9月</h3><ul>
<li>没有色彩的多崎作和他的巡礼之年</li>
<li>北京折叠</li>
</ul>
<h3 id="12月"><a href="#12月" class="headerlink" title="12月"></a>12月</h3><ul>
<li>挪威的森林</li>
</ul>
<h2 id="2014-2015"><a href="#2014-2015" class="headerlink" title="2014-2015"></a>2014-2015</h2><ul>
<li>小王子</li>
<li>追风筝的人</li>
<li>围城</li>
<li>活着</li>
<li>1984</li>
<li>平凡的世界</li>
<li>看见</li>
<li>许三观卖血记</li>
<li>失控</li>
<li>当我谈跑步时我谈些什么</li>
<li>白鹿原</li>
<li>爱你就像爱生命</li>
<li>一只特立独行的猪</li>
<li>乖，摸摸头</li>
<li>滚蛋吧，肿瘤君</li>
<li>人性的弱点</li>
<li>白夜</li>
<li>孤独六讲</li>
<li>黑客与画家</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;2017&quot;&gt;&lt;a href=&quot;#2017&quot; class=&quot;headerlink&quot; title=&quot;2017&quot;&gt;&lt;/a&gt;2017&lt;/h2&gt;&lt;h3 id=&quot;1月&quot;&gt;&lt;a href=&quot;#1月&quot; class=&quot;headerlink&quot; title=&quot;1月&quot;&gt;&lt;/a&gt;1月&lt;/h
    
    </summary>
    
      <category term="小攀说" scheme="https://jpanj.com/categories/%E5%B0%8F%E6%94%80%E8%AF%B4/"/>
    
    
      <category term="读书" scheme="https://jpanj.com/tags/%E8%AF%BB%E4%B9%A6/"/>
    
  </entry>
  
  <entry>
    <title>山丘 - 李宗盛</title>
    <link href="https://jpanj.com/2333/shangqiu-video/"/>
    <id>https://jpanj.com/2333/shangqiu-video/</id>
    <published>2333-07-21T10:42:59.000Z</published>
    <updated>2017-07-21T10:46:14.000Z</updated>
    
    <content type="html"><![CDATA[<video width="720" height="480" controls><br><source src="http://otfsjju04.bkt.clouddn.com/Jonathan%20Lee%E6%9D%8E%E5%AE%97%E7%9B%9B%20%5B%20%E5%B1%B1%E4%B8%98%20%5D%20Official%20Music%20Video-rVEMTxg_LrU.mp4"><br></video>

<blockquote>
<p>想说却还没说的 还很多<br>攒着是因为想写成歌<br>让人轻轻地唱着 淡淡地记着<br>就算终于忘了 也值了<br>说不定我一生涓滴意念<br>侥幸汇成河<br>然后我俩各自一端<br>望着大河弯弯 终于敢放胆<br>嘻皮笑脸 面对 人生的难<br>也许我们从未成熟<br>还没能晓得 就快要老了<br>尽管心里活着的还是那个<br>年轻人<br>因为不安而频频回首<br>无知地索求 羞耻于求救<br>不知疲倦地翻越 每一个山丘<br>越过山丘 虽然已白了头<br>喋喋不休 时不我予的哀愁<br>还未如愿见着不朽<br>就把自己先搞丢<br>越过山丘 才发现无人等候<br>喋喋不休 再也唤不回温柔<br>为何记不得上一次是谁给的拥抱<br>在什么时候<br>我没有刻意隐藏 也无意让你感伤<br>多少次我们无醉不欢<br>咒骂人生太短 唏嘘相见恨晚<br>让女人把妆哭花了 也不管<br>遗憾我们从未成熟<br>还没能晓得 就已经老了<br>尽力却仍不明白<br>身边的年轻人<br>给自己随便找个理由<br>向情爱的挑逗 命运的左右<br>不自量力地还手 直至死方休<br>越过山丘 虽然已白了头<br>喋喋不休 时不我予的哀愁<br>还未如愿见着不朽<br>就把自己先搞丢<br>越过山丘 才发现无人等候<br>喋喋不休 再也唤不回了温柔<br>为何记不得上一次是谁给的拥抱<br>在什么时候<br>越过山丘 虽然已白了头<br>喋喋不休 时不我予的哀愁<br>还未如愿见着不朽<br>就把自己先搞丢<br>越过山丘 才发现无人等候<br>喋喋不休 再也唤不回了温柔<br>为何记不得上一次是谁给的拥抱<br>在什么时候<br>喋喋不休 时不我予的哀愁<br>向情爱的挑逗 命运的左右<br>不自量力地还手 直至死方休<br>为何记不得上一次是谁给的拥抱<br>在什么时候</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;video width=&quot;720&quot; height=&quot;480&quot; controls&gt;&lt;br&gt;&lt;source src=&quot;http://otfsjju04.bkt.clouddn.com/Jonathan%20Lee%E6%9D%8E%E5%AE%97%E7%9B%9B%20%5B%2
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>让博客重定向到 https 的方法</title>
    <link href="https://jpanj.com/2017/blog-redirect-https/"/>
    <id>https://jpanj.com/2017/blog-redirect-https/</id>
    <published>2017-07-25T02:21:24.000Z</published>
    <updated>2017-07-25T02:42:53.000Z</updated>
    
    <content type="html"><![CDATA[<p>前段时间将博客在七牛上部署了一份，并且为新的域名 <a href="https://jpanj.com">jpanj.com</a> 申请了 SSL 证书，但是发现一个问题，使用 http 请求还是可以访问的，想通过 https 的方式访问，需要手动将地址修改为 https，我想有没有什么办法能在用 http 访问时重定向到 https。</p>
<p>所以我开了个工单请教七牛的工作人员，得到的结果是他们也无法做强制跳转。</p>
<p><img src="1.png" alt=""></p>
<p>之前让 http 请求重定向到 https 的方法是通过 Nginx 的 rewrite 完成的，但是我现在的博客是一个纯静态站点，而且并没有托管在自己的服务器上，所以无法这样操作。</p>
<p>今天得到了一个解决方法，是通过修改主题源码来实现的，就我现在用的这个主题来说，<code>layout</code> 目录下所有模板都会继承 <code>_layout.swig</code>，所以我只要在 <code>&lt;head&gt;</code> 标签中加入以下代码即可：</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span><span class="javascript"></span></div><div class="line">  <span class="keyword">var</span> host = <span class="string">"jpanj.com"</span>;</div><div class="line">  <span class="keyword">if</span> ((host == <span class="built_in">window</span>.location.host) &amp;&amp; (<span class="built_in">window</span>.location.protocol != <span class="string">"https:"</span>))</div><div class="line">    <span class="built_in">window</span>.location.protocol = <span class="string">"https"</span>;</div><div class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div></pre></td></tr></table></figure>
<p>我只需要判断 <a href="https://jpanj.com">jpanj.com</a> 就可以了，之前的 <a href="http://panmax.love" target="_blank" rel="external">panmax.love</a> 不做修改。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前段时间将博客在七牛上部署了一份，并且为新的域名 &lt;a href=&quot;https://jpanj.com&quot;&gt;jpanj.com&lt;/a&gt; 申请了 SSL 证书，但是发现一个问题，使用 http 请求还是可以访问的，想通过 https 的方式访问，需要手动将地址修改为 https
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>--spring.profiles.active 穿透性</title>
    <link href="https://jpanj.com/2017/spring-profiles-active-chuan-tou-xing/"/>
    <id>https://jpanj.com/2017/spring-profiles-active-chuan-tou-xing/</id>
    <published>2017-07-20T08:00:08.000Z</published>
    <updated>2017-07-25T08:09:36.000Z</updated>
    
    <content type="html"><![CDATA[<p>今天在帮助 datamaster 接入配置中心时发现一个问题，就是如果在应用的 <code>application.yml</code> 像这样：</p>
<figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">spring:</span></div><div class="line"><span class="symbol">  application:</span></div><div class="line"><span class="symbol">    name:</span> datamaster-scheduler</div><div class="line"><span class="symbol">  profiles:</span></div><div class="line"><span class="symbol">    active:</span> lc10</div></pre></td></tr></table></figure>
<p>指定环境的话，在启动时会自动寻找 <code>datamaster-scheduler-lc10.yml</code> 这个配置文件，不论使用 <code>bootrun</code> 启动或者打成 <code>jar</code> 包都没有问题。</p>
<p>但是如果不在 <code>application.yml</code> 中指定 <code>spring.profiles.active</code>，而是打成 <code>jar</code> 包，使用 <code>--spring.profiles.active=lc10</code> 参数启动的话，就注册不上 Eureka，所以也就没办法正常拉取配置文件了。</p>
<p>出现这个问题可以理解，因为我们在封装微服务接入的 starter 时，只定义了两个 active：</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="meta">---</span></div><div class="line"><span class="attr">spring:</span></div><div class="line"><span class="attr">  profiles:</span> <span class="string">dev</span></div><div class="line"><span class="attr">  rabbitmq:</span></div><div class="line"><span class="attr">    host:</span> <span class="number">172.24</span><span class="number">.8</span><span class="number">.100</span></div><div class="line"><span class="attr">    port:</span> <span class="number">5672</span></div><div class="line"><span class="attr">    username:</span> <span class="string">admin</span></div><div class="line"><span class="attr">    password:</span> <span class="string">admin</span></div><div class="line"><span class="attr">  zipkin:</span></div><div class="line"><span class="attr">    base-url:</span> <span class="attr">http://172.24.8.100:7030</span></div><div class="line"></div><div class="line"><span class="attr">eureka:</span></div><div class="line"><span class="attr">  client:</span></div><div class="line"><span class="attr">    service-url:</span></div><div class="line"><span class="attr">      defaultZone:</span> <span class="attr">http://172.24.8.100:7011/eureka/,http://172.24.8.100:7012/eureka/,http://172.24.8.100:7013/eureka/</span></div><div class="line"></div><div class="line"><span class="meta">---</span></div><div class="line"><span class="attr">spring:</span></div><div class="line"><span class="attr">  profiles:</span> <span class="string">beta</span></div><div class="line"><span class="attr">  rabbitmq:</span></div><div class="line"><span class="attr">    host:</span> <span class="number">172.24</span><span class="number">.8</span><span class="number">.100</span></div><div class="line"><span class="attr">    port:</span> <span class="number">5672</span></div><div class="line"><span class="attr">    username:</span> <span class="string">admin</span></div><div class="line"><span class="attr">    password:</span> <span class="string">admin</span></div><div class="line"><span class="attr">  zipkin:</span></div><div class="line"><span class="attr">    base-url:</span> <span class="attr">http://172.24.8.100:7030</span></div><div class="line"><span class="attr">eureka:</span></div><div class="line"><span class="attr">  client:</span></div><div class="line"><span class="attr">    service-url:</span></div><div class="line"><span class="attr">      defaultZone:</span> <span class="attr">http://172.24.8.100:7011/eureka/,http://172.24.8.100:7012/eureka/,http://172.24.8.100:7013/eureka/</span></div></pre></td></tr></table></figure>
<p>所以使用这两种之外的环境是找不到配置中心的地址的，但是之前那种方式所得到的行为就不太理解了。</p>
<p>这个问题我得到的结论是这样的：在应用系统的 <code>application.yml</code> 中定义的 <code>active</code> 是不具有穿透性的，所以我们的 微服务 starter 是不会得到这里定义的 <code>active</code> 的，而且 微服务starter 中的 <code>bootstrap.yml</code> 中定义了：</p>
<figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">spring:</span></div><div class="line"><span class="symbol">  profiles:</span></div><div class="line"><span class="symbol">    active:</span> dev</div></pre></td></tr></table></figure>
<p>所以在没有指定时会使用 dev，没有任何问题。</p>
<p>使用 <code>--spring.profiles.active=lc10</code> 指定的 active 具有穿透性，会让这个应用系统依赖的其他组件也使用指定的 <code>active</code>，所以这个时候 微服务 starter 也会切换到 lc10 的 <code>active</code>，因为我们没有在 starter 中配 lc10 对应的 <code>active</code>，也就就相当于没有指定 eureka 的注册地址，所以接下来的所有流程就都跑不通了。</p>
<p>为了解决这个问题，我在 微服务 starter 的 <code>bootstrap.yml</code> 中加上了默认的 eureka 注册地址和其他需要用到的配置，这样即便是指定的环境不存在的话也没有任何问题。</p>
<p>之后在为客户部署时，只需要在 微服务 starter 中增加一个客户所对应的环境，然后启动应用时指定客户的 <code>active</code> 就可以了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;今天在帮助 datamaster 接入配置中心时发现一个问题，就是如果在应用的 &lt;code&gt;application.yml&lt;/code&gt; 像这样：&lt;/p&gt;
&lt;figure class=&quot;highlight dts&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>解决 CentOS 安装 MySQL-python 报错</title>
    <link href="https://jpanj.com/2017/centos-fix-python-mysql/"/>
    <id>https://jpanj.com/2017/centos-fix-python-mysql/</id>
    <published>2017-07-20T02:54:42.000Z</published>
    <updated>2017-07-20T02:56:47.000Z</updated>
    
    <content type="html"><![CDATA[<p>今天在服务器上用 pip 安装 MySQL-python 时报错，虽然之前处理过很多次，但都没有记录，这次记录一下。</p>
<p>报错如下：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div></pre></td><td class="code"><pre><div class="line">Downloading/unpacking MySQL-python</div><div class="line">  Downloading MySQL-python-1.2.5.zip (108kB): 108kB downloaded</div><div class="line">  Running setup.py egg_info for package MySQL-python</div><div class="line">    sh: mysql_config: command not found</div><div class="line">    Traceback (most recent <span class="keyword">call</span> <span class="keyword">last</span>):</div><div class="line">      <span class="keyword">File</span> <span class="string">"&lt;string&gt;"</span>, line <span class="number">16</span>, <span class="keyword">in</span> &lt;<span class="keyword">module</span>&gt;</div><div class="line">      <span class="keyword">File</span> <span class="string">"/home/jiapan/.virtualenvs/appstore-crawler/build/MySQL-python/setup.py"</span>, line <span class="number">17</span>, <span class="keyword">in</span> &lt;<span class="keyword">module</span>&gt;</div><div class="line">        metadata, options = get_config()</div><div class="line">      <span class="keyword">File</span> <span class="string">"setup_posix.py"</span>, line <span class="number">43</span>, <span class="keyword">in</span> get_config</div><div class="line">        libs = mysql_config(<span class="string">"libs_r"</span>)</div><div class="line">      <span class="keyword">File</span> <span class="string">"setup_posix.py"</span>, line <span class="number">25</span>, <span class="keyword">in</span> mysql_config</div><div class="line">        <span class="keyword">raise</span> EnvironmentError(<span class="string">"%s not found"</span> % (mysql_config.path,))</div><div class="line">    EnvironmentError: mysql_config <span class="keyword">not</span> <span class="keyword">found</span></div><div class="line">    <span class="keyword">Complete</span> <span class="keyword">output</span> <span class="keyword">from</span> command python setup.py egg_info:</div><div class="line">    sh: mysql_config: command <span class="keyword">not</span> <span class="keyword">found</span></div><div class="line"></div><div class="line">Traceback (most recent <span class="keyword">call</span> <span class="keyword">last</span>):</div><div class="line"></div><div class="line">  <span class="keyword">File</span> <span class="string">"&lt;string&gt;"</span>, line <span class="number">16</span>, <span class="keyword">in</span> &lt;<span class="keyword">module</span>&gt;</div><div class="line"></div><div class="line">  <span class="keyword">File</span> <span class="string">"/home/jiapan/.virtualenvs/appstore-crawler/build/MySQL-python/setup.py"</span>, line <span class="number">17</span>, <span class="keyword">in</span> &lt;<span class="keyword">module</span>&gt;</div><div class="line"></div><div class="line">    metadata, options = get_config()</div><div class="line"></div><div class="line">  <span class="keyword">File</span> <span class="string">"setup_posix.py"</span>, line <span class="number">43</span>, <span class="keyword">in</span> get_config</div><div class="line"></div><div class="line">    libs = mysql_config(<span class="string">"libs_r"</span>)</div><div class="line"></div><div class="line">  <span class="keyword">File</span> <span class="string">"setup_posix.py"</span>, line <span class="number">25</span>, <span class="keyword">in</span> mysql_config</div><div class="line"></div><div class="line">    <span class="keyword">raise</span> EnvironmentError(<span class="string">"%s not found"</span> % (mysql_config.path,))</div><div class="line"></div><div class="line">EnvironmentError: mysql_config <span class="keyword">not</span> <span class="keyword">found</span></div></pre></td></tr></table></figure>
<p>解决方法：</p>
<p><code>sudo yum install python-devel mysql-devel</code></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;今天在服务器上用 pip 安装 MySQL-python 时报错，虽然之前处理过很多次，但都没有记录，这次记录一下。&lt;/p&gt;
&lt;p&gt;报错如下：&lt;/p&gt;
&lt;figure class=&quot;highlight sql&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>SpringMVC 常用注解</title>
    <link href="https://jpanj.com/2017/spring-chang-yong-zhu-jie/"/>
    <id>https://jpanj.com/2017/spring-chang-yong-zhu-jie/</id>
    <published>2017-07-19T02:44:33.000Z</published>
    <updated>2017-07-19T07:29:11.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="Controller"><a href="#Controller" class="headerlink" title="@Controller"></a>@Controller</h3><ul>
<li>用于标注控制层组件</li>
<li><code>@Controller</code> 用于标记在一个类上，使用它标记的类就是一个 <code>SpringMVC Controller</code> 对象，分发处理器将会扫描使用了该注解的类方法，并检测该方法是否使用了 <code>@RequestMapping</code> 注解</li>
<li>可以把 <code>Request</code> 请求 header 部分的值绑定到方法参数上</li>
</ul>
<h3 id="RestController"><a href="#RestController" class="headerlink" title="@RestController"></a>@RestController</h3><ul>
<li>相当于 <code>@Controller</code> 和 <code>@ResponseBody</code> 的组合效果</li>
</ul>
<h3 id="Component"><a href="#Component" class="headerlink" title="@Component"></a>@Component</h3><ul>
<li>泛指组件，当组件不好归类的时候，我们可以使用这个注解进行标注</li>
</ul>
<h3 id="Respository"><a href="#Respository" class="headerlink" title="@Respository"></a>@Respository</h3><ul>
<li>用于注解 <code>dao</code> 层，在 <code>daoImpl</code> 类上面注解</li>
</ul>
<h3 id="Service"><a href="#Service" class="headerlink" title="@Service"></a>@Service</h3><ul>
<li>用于注解业务组件</li>
</ul>
<hr>
<h3 id="ResponseBody"><a href="#ResponseBody" class="headerlink" title="@ResponseBody"></a>@ResponseBody</h3>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;Controller&quot;&gt;&lt;a href=&quot;#Controller&quot; class=&quot;headerlink&quot; title=&quot;@Controller&quot;&gt;&lt;/a&gt;@Controller&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;用于标注控制层组件&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@Cont
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>gradle 编译时强制刷新依赖</title>
    <link href="https://jpanj.com/2017/gradle-%E7%BC%96%E8%AF%91%E6%97%B6%E5%BC%BA%E5%88%B6%E5%88%B7%E6%96%B0%E4%BE%9D%E8%B5%96/"/>
    <id>https://jpanj.com/2017/gradle-编译时强制刷新依赖/</id>
    <published>2017-07-17T08:40:35.000Z</published>
    <updated>2017-07-17T09:11:20.000Z</updated>
    
    <content type="html"><![CDATA[<p>最近团队封装了个 <code>springboot</code> 的 <code>starter</code>，用起来很爽，后来优化代码的时候，看到下边的代码中已经指定了 <code>profile</code></p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="attr">cloud:</span></div><div class="line"><span class="attr">    config:</span></div><div class="line"><span class="attr">      discovery:</span></div><div class="line"><span class="attr">        enabled:</span> <span class="literal">true</span></div><div class="line"><span class="attr">        service-id:</span> <span class="string">config-server</span></div><div class="line"><span class="attr">      label:</span> <span class="string">master</span></div><div class="line"><span class="attr">      profile:</span> <span class="string">$&#123;spring.profiles.active:dev&#125;</span></div></pre></td></tr></table></figure>
<p>所以理所当然的认为不需要指定 <code>spring</code> 的 active 了，就把 <code>active</code> 给删掉了（如下）：</p>
<figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">spring:</span></div><div class="line"><span class="symbol">  profiles:</span></div><div class="line"><span class="symbol">    active:</span> dev</div></pre></td></tr></table></figure>
<p>发布到 <code>maven</code> 仓库后，重新测试没啥问题。结果过了个周末来了再编译，发现程序无法启动了，找了很多原因才发现是上边的操作导致的。</p>
<p>后来将配置改了回来，发现还是不行，又鼓捣了好久发现这次的问题是 <code>gradle</code> 编译缓存的问题，通过这个网站： <a href="https://pkaq.gitbooks.io/gradletraining/content/book/ch5/4.%E4%BE%9D%E8%B5%96%E7%9A%84%E6%9B%B4%E6%96%B0%E4%B8%8E%E7%BC%93%E5%AD%98.html" target="_blank" rel="external">https://pkaq.gitbooks.io/gradletraining/content/book/ch5/4.%E4%BE%9D%E8%B5%96%E7%9A%84%E6%9B%B4%E6%96%B0%E4%B8%8E%E7%BC%93%E5%AD%98.html</a> 找到了解决办法，编译的时候在后边加上 <code>--refresh-dependencies</code> 可以强制刷新缓存。</p>
<p>虽然问题解决了，但是我还有个疑问，我们的 <code>starter</code> 明明已经指定版本号为 <code>0.0.1-SNAPSHOT</code> 了，按理说应该在 <code>build</code> 的时候无条件的重新拉取最新的依赖，但是这个时候为什么没有生效？</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近团队封装了个 &lt;code&gt;springboot&lt;/code&gt; 的 &lt;code&gt;starter&lt;/code&gt;，用起来很爽，后来优化代码的时候，看到下边的代码中已经指定了 &lt;code&gt;profile&lt;/code&gt;&lt;/p&gt;
&lt;figure class=&quot;highlight y
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>virtualenvwrapper 安装 与 iPython for Python2</title>
    <link href="https://jpanj.com/2017/virtualenvwrapper-%E5%AE%89%E8%A3%85-%E4%B8%8E-iPython/"/>
    <id>https://jpanj.com/2017/virtualenvwrapper-安装-与-iPython/</id>
    <published>2017-07-12T03:43:12.000Z</published>
    <updated>2017-07-19T05:28:54.000Z</updated>
    
    <content type="html"><![CDATA[<p>这篇文章完全是要写两件事：</p>
<ol>
<li>安装 <code>virtualenvwrapper</code> 后如何配置</li>
<li>在 <code>Python 2</code> 上安装 <code>iPython</code></li>
</ol>
<p>如果分成两篇文章来写的话，每篇文章就会非常短，不值当的，所以直接合成一篇来写。</p>
<hr>
<h2 id="配置-virtualenvwrapper"><a href="#配置-virtualenvwrapper" class="headerlink" title="配置 virtualenvwrapper"></a>配置 virtualenvwrapper</h2><p>安装 <code>virtualenvwrapper</code> 的过程就不再讲解了，直接 <code>pip install</code> 就可以完成，主要是安装完成后的配置，因为每次我装完都需要问一下谷歌然后才能继续，所以不如记到自己的 Blog 下，即便下次再忘了也能快速找到解决方法。</p>
<p>安装完 <code>virtualenvwrapper</code> 后，要根据自己使用的 <code>shell</code> 来配置不同的文件，比如 <code>bash</code> 需要配置 <code>.bashrc</code>、<code>zsh</code> 配置 <code>.zshrc</code>。</p>
<p>配置如下：</p>
<figure class="highlight routeros"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="builtin-name">export</span> <span class="attribute">WORKON_HOME</span>=<span class="variable">$HOME</span>/.virtualenvs</div><div class="line">source /usr/local/bin/virtualenvwrapper.sh</div></pre></td></tr></table></figure>
<p>第一行是给定一个虚拟环境保存的目录，第二行是执行 <code>virtualenvwrapper</code> 的脚本使 <code>workon</code>, <code>mkvirtualenv</code> 等命令生效。</p>
<p>大多数时候都卡在第二行那个命令上，因为不同发行版的机器 <code>virtualenvwrapper.sh</code> 所在位置不同，所以需要通过:</p>
<p><code>find / -name virtualenvwrapper.sh</code> </p>
<p>找到 <code>virtualenvwrapper.sh</code> 所在的位置后，根据自己机器上的实际位置来写那一行脚本。</p>
<p>修改完成后保存退出，重新启动一个命令窗口，检查有没有配置成功。</p>
<hr>
<h2 id="在-Python-2-上安装-iPython"><a href="#在-Python-2-上安装-iPython" class="headerlink" title="在 Python 2 上安装 iPython"></a>在 Python 2 上安装 iPython</h2><p>最新版的 <code>iPython</code> 已经不支持 Py2 了，所以直接用 <code>pip</code> 安装 <code>iPython</code> 时，会提示安装失败，所以要手动指定安装版本。</p>
<p>最后一个支持 Py2 的版本是 <code>5.4.0</code>，所以用 <code>pip install ipython==5.4.0</code> 就行了。</p>
<blockquote>
<p>UPDATE AT 2017-07-19</p>
</blockquote>
<p>今天在一台服务器上将 pip 改为了<strong>阿里源</strong>后发现安装 <code>ipython==5.4.0</code> 时会报错：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">Running setup.py egg_info <span class="keyword">for</span> package ipython</div><div class="line">    error <span class="keyword">in</span> ipython setup <span class="keyword">command</span>: <span class="title">Invalid</span> <span class="title">environment</span> <span class="title">marker</span>: <span class="title">sys_platform</span> == <span class="string">"win32"</span> <span class="title">and</span> <span class="title">python_version</span> &lt; <span class="string">"3.6"</span></div><div class="line">    Complete output <span class="built_in">from</span> <span class="keyword">command</span> <span class="title">python</span> <span class="title">setup</span>.<span class="title">py</span> <span class="title">egg_info</span>:</div><div class="line">    error <span class="keyword">in</span> ipython setup <span class="keyword">command</span>: <span class="title">Invalid</span> <span class="title">environment</span> <span class="title">marker</span>: <span class="title">sys_platform</span> == <span class="string">"win32"</span> <span class="title">and</span> <span class="title">python_version</span> &lt; <span class="string">"3.6"</span></div></pre></td></tr></table></figure>
<p>这个问题使用 <code>pip install pip --upgrade</code> 将 pip 更新为最新版本就可以解决了。</p>
<p>顺便再记一下 pip 源的地址，虽然知道修改方法，但每次还要去网上搜一下源地址</p>
<p><code>vi ~/.pip/pip.conf</code></p>
<figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="section">[global]</span></div><div class="line"><span class="attr">trusted-host</span> =  mirrors.aliyun.com</div><div class="line"><span class="attr">index-url</span> = http://mirrors.aliyun.com/pypi/simple</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这篇文章完全是要写两件事：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;安装 &lt;code&gt;virtualenvwrapper&lt;/code&gt; 后如何配置&lt;/li&gt;
&lt;li&gt;在 &lt;code&gt;Python 2&lt;/code&gt; 上安装 &lt;code&gt;iPython&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>基于 Hystrix 的熔断器调研</title>
    <link href="https://jpanj.com/2017/%E5%9F%BA%E4%BA%8E-Hystrix-%E7%9A%84%E7%86%94%E6%96%AD%E5%99%A8%E8%B0%83%E7%A0%94/"/>
    <id>https://jpanj.com/2017/基于-Hystrix-的熔断器调研/</id>
    <published>2017-06-22T07:18:27.000Z</published>
    <updated>2017-06-22T07:19:49.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="基于-Hystrix-的熔断器调研"><a href="#基于-Hystrix-的熔断器调研" class="headerlink" title="基于 Hystrix 的熔断器调研"></a>基于 Hystrix 的熔断器调研</h1><h3 id="什么是雪崩效应"><a href="#什么是雪崩效应" class="headerlink" title="什么是雪崩效应"></a>什么是雪崩效应</h3><p>在微服务架构中通常会有多个服务层调用，基础服务的故障可能会导致级联故障，进而造成整个系统不可用的情况，这种现象被称为服务雪崩效应。服务雪崩效应是一种因 <strong>服务提供者</strong> 的不可用导致 <strong>服务消费者</strong> 的不可用，并将不可用逐渐放大的过程。</p>
<p>如果下图所示：A 作为服务提供者，B 为 A 的服务消费者，C 和 D 是 B 的服务消费者。A 不可用引起了 B 的不可用，并将不可用像滚雪球一样放大到 C 和 D 时，雪崩效应就形成了。</p>
<p><img src="1.png" alt=""></p>
<h3 id="熔断器（CircuitBreaker）"><a href="#熔断器（CircuitBreaker）" class="headerlink" title="熔断器（CircuitBreaker）"></a>熔断器（CircuitBreaker）</h3><p>熔断器的原理很简单，如同电力过载保护器。它可以实现快速失败，如果它在一段时间内侦测到许多类似的错误，会强迫其以后的多个调用快速失败，不再访问远程服务器，从而防止应用程序不断地尝试执行可能会失败的操作，使得应用程序继续执行而不用等待修正错误，或者浪费 CPU 时间去等到长时间的超时产生。熔断器也可以使应用程序能够诊断错误是否已经修正，如果已经修正，应用程序会再次尝试调用操作。</p>
<p>熔断器模式就像是那些容易导致错误的操作的一种代理。这种代理能够记录最近调用发生错误的次数，然后决定是否允许操作继续，或者立即返回错误。熔断器开关相互转换的逻辑如下图：</p>
<p><img src="2.png" alt=""></p>
<h3 id="Hystrix-是什么"><a href="#Hystrix-是什么" class="headerlink" title="Hystrix 是什么"></a>Hystrix 是什么</h3><p>在分布式系统，我们一定会依赖各种服务，那么这些个服务一定会出现失败的情况，Hystrix 就是这样的一个工具，它通过提供了逻辑上延时和错误容忍的解决力来协助我们完成分布式系统的交互。Hystrix 通过分离服务的调用点，阻止错误在各个系统的传播，并且提供了错误回调机制，这一系列的措施提高了系统的整体服务弹性。</p>
<h3 id="Hystrix-是干嘛的"><a href="#Hystrix-是干嘛的" class="headerlink" title="Hystrix 是干嘛的"></a>Hystrix 是干嘛的</h3><h4 id="Hystrix-被设计用来做了下面几件事"><a href="#Hystrix-被设计用来做了下面几件事" class="headerlink" title="Hystrix 被设计用来做了下面几件事:"></a>Hystrix 被设计用来做了下面几件事:</h4><ol>
<li>保护系统间的调用延时以及错误，特别是通过第三方的工具的网络调用</li>
<li>阻止错误在分布式系统之前的传播</li>
<li>快速失败和迅速恢复</li>
<li>错误回退和优雅的服务降级</li>
<li>提供近乎实时的系统监控，报警和动态操控</li>
</ol>
<h3 id="Hystrix特性"><a href="#Hystrix特性" class="headerlink" title="Hystrix特性"></a>Hystrix特性</h3><p>1.熔断器机制</p>
<p>熔断器很好理解，当 Hystrix Command 请求后端服务失败数量超过一定比例(默认50%)，熔断器会切换到开路状态(Open)。这时所有请求会直接失败而不会发送到后端服务。熔断器保持在开路状态一段时间后(默认5秒)，自动切换到半开路状态(HALF-OPEN)，这时会判断下一次请求的返回情况，如果请求成功，熔断器切回闭路状态(CLOSED)，否则重新切换到开路状态(OPEN)。Hystrix 的熔断器就像我们家庭电路中的保险丝，一旦后端服务不可用，熔断器会直接切断请求链，避免发送大量无效请求影响系统吞吐量，并且熔断器有自我检测并恢复的能力。</p>
<p>2.Fallback</p>
<p>Fallback 相当于是降级操作。对于查询操作, 我们可以实现一个 fallback 方法，当请求后端服务出现异常的时候，可以使用 fallback 方法返回的值。fallback 方法的返回值一般是设置的默认值或者来自缓存。</p>
<p>3.资源隔离</p>
<p>在Hystrix中, 主要通过线程池来实现资源隔离. 通常在使用的时候我们会根据调用的远程服务划分出多个线程池. 例如调用产品服务的 Command 放入 A 线程池，调用账户服务的 Command 放入 B 线程池. 这样做的主要优点是运行环境被隔离开了。这样就算调用服务的代码存在 bug 或者由于其他原因导致自己所在线程池被耗尽时，不会对系统的其他服务造成影响。但是带来的代价就是维护多个线程池会对系统带来额外的性能开销。如果是对性能有严格要求而且确信自己调用服务的客户端代码不会出问题的话，可以使用 Hystrix 的信号模式(Semaphores)来隔离资源。</p>
<h3 id="Hystrix工作方式如下"><a href="#Hystrix工作方式如下" class="headerlink" title="Hystrix工作方式如下:"></a>Hystrix工作方式如下:</h3><ul>
<li>阻止一个单独的依赖耗尽系统的所有线程，比如(tomcat)</li>
<li>使用快速失败代替将这个请求排队</li>
<li>在任何可能失败的地方提供后退机制来确保用户不会看到错误</li>
<li>使用隔离技术(比如: 隔板，泳道，环路切断模式)降低一个依赖的失败对整个系统的影响</li>
<li>优化使得系统可以近乎实时的收集，监控，报警</li>
<li>优化使得系统可以近乎实时的修改，并且可以近乎实时生效</li>
<li>保护系统不仅仅在网络层面，也包括客户端层面的依赖执行的失败</li>
</ul>
<h3 id="Hystrix-的使用"><a href="#Hystrix-的使用" class="headerlink" title="Hystrix 的使用"></a>Hystrix 的使用</h3><p>因为 <a href="https://github.com/Netflix/Hystrix" target="_blank" rel="external">Hystrix</a> 是一个开源的 Java 库，所以可以进行像官方示例那样直接用起来，下面我们介绍如何将 Hystrix 集成到 SpringBoot 项目中。</p>
<h4 id="在pox-xml文件中加入："><a href="#在pox-xml文件中加入：" class="headerlink" title="在pox.xml文件中加入："></a>在pox.xml文件中加入：</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-hystrix<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div></pre></td></tr></table></figure>
<h4 id="在程序入口类加上：-EnableHystrix"><a href="#在程序入口类加上：-EnableHystrix" class="headerlink" title="在程序入口类加上：@EnableHystrix"></a>在程序入口类加上：<code>@EnableHystrix</code></h4><h4 id="在需要使用熔断器的地方标记注解即可："><a href="#在需要使用熔断器的地方标记注解即可：" class="headerlink" title="在需要使用熔断器的地方标记注解即可："></a>在需要使用熔断器的地方标记注解即可：</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@HystrixCommand(fallbackMethod = <span class="meta-string">"yyy"</span>)</span></div><div class="line"><span class="keyword">public</span> String doSomething()</div></pre></td></tr></table></figure>
<p><code>yyy</code> 就是在 熔断器开启时 是我们要调用的函数。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;基于-Hystrix-的熔断器调研&quot;&gt;&lt;a href=&quot;#基于-Hystrix-的熔断器调研&quot; class=&quot;headerlink&quot; title=&quot;基于 Hystrix 的熔断器调研&quot;&gt;&lt;/a&gt;基于 Hystrix 的熔断器调研&lt;/h1&gt;&lt;h3 id=&quot;什么是雪崩
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>使用 spring-boot-starter-security 集成 CAS 遇到的问题</title>
    <link href="https://jpanj.com/2017/%E4%BD%BF%E7%94%A8-spring-boot-starter-security-%E9%9B%86%E6%88%90-CAS-%E9%81%87%E5%88%B0%E7%9A%84%E9%97%AE%E9%A2%98/"/>
    <id>https://jpanj.com/2017/使用-spring-boot-starter-security-集成-CAS-遇到的问题/</id>
    <published>2017-06-22T07:10:53.000Z</published>
    <updated>2017-06-22T07:18:06.000Z</updated>
    
    <content type="html"><![CDATA[<p>根据这篇教程：<a href="http://blog.csdn.net/cl_andywin/article/details/53998986" target="_blank" rel="external">http://blog.csdn.net/cl_andywin/article/details/53998986</a> 使用 <code>spring-boot-starter-security</code> 集成了 CAS 单点登录的功能，但是发现一个问题是，单点退出一直不成功。</p>
<p>然后我就拿出各种 debug 手段，最后发现问题是服务器在收到 <code>POST</code> 请求时需要 <code>CSRF</code> 验证（CAS 在单点退出时是发送的 <code>POST</code> 请求），原因是使用了 <code>spring-security</code> 导致的，它默认是开启 <code>CSRF</code> 的，所以解决办法就是关掉这个特性。</p>
<p>在重写的 <code>configure()</code> 方法最后加上 <code>http.csrf().disable();</code> 就行了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;根据这篇教程：&lt;a href=&quot;http://blog.csdn.net/cl_andywin/article/details/53998986&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://blog.csdn.net/cl_andywin/
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>基于 Eureka 的服务注册与发现调研</title>
    <link href="https://jpanj.com/2017/%E5%9F%BA%E4%BA%8E-Eureka-%E7%9A%84%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0%E8%B0%83%E7%A0%94/"/>
    <id>https://jpanj.com/2017/基于-Eureka-的服务注册与发现调研/</id>
    <published>2017-06-21T03:31:40.000Z</published>
    <updated>2017-06-21T07:11:59.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Eureka-是-Netflix-开源的一款提供服务注册和发现的产品。"><a href="#Eureka-是-Netflix-开源的一款提供服务注册和发现的产品。" class="headerlink" title="Eureka 是 Netflix 开源的一款提供服务注册和发现的产品。"></a><a href="https://github.com/Netflix/eureka" target="_blank" rel="external">Eureka</a> 是 Netflix 开源的一款提供服务注册和发现的产品。</h2><p>Eureka 由两个组件组成： <strong>Eureka 服务器</strong> 和 <strong>Eureka 客户端</strong>。Eureka 服务器 用作服务注册服务器。Eureka 客户端 是一个 Java 客户端，用来简化与服务器的交互、作为轮询负载均衡器，并提供服务的故障切换支持。</p>
<h3 id="官方对自己的定义是："><a href="#官方对自己的定义是：" class="headerlink" title="官方对自己的定义是："></a>官方对自己的定义是：</h3><blockquote>
<p>Eureka is a REST (Representational State Transfer) based service that is primarily used in the AWS cloud for locating services for the purpose of load balancing and failover of middle-tier servers.</p>
</blockquote>
<h3 id="通俗点讲什么是服务注册发现？"><a href="#通俗点讲什么是服务注册发现？" class="headerlink" title="通俗点讲什么是服务注册发现？"></a>通俗点讲什么是服务注册发现？</h3><p>服务注册与发现就像是在一个聊天室，每个用户来的时候去服务器上注册，这样你的好友们就能看到你，你同时也将获取好友的上线列表。在微服务中，服务就相当于聊天室的用户，而服务注册中心就像聊天室服务器一样。</p>
<h3 id="Eureka特性"><a href="#Eureka特性" class="headerlink" title="Eureka特性"></a>Eureka特性</h3><ol>
<li>Eureka Server 具有服务定位/发现的能力，在各个微服务启动时，会通过 Eureka Client 向Eureka Server 进行注册自己的信息（例如网络信息）。 </li>
<li>一般情况下，微服务启动后，Eureka Client 会周期性向 Eureka Server 发送心跳检测(默认周期为30秒)以注册/更新自己的信息。 </li>
<li>如果 Eureka Server 在一定时间内(默认90秒)没有收到 Eureka Client 的心跳检测，就会注销掉该微服务点。 </li>
<li>同时，Eureka Server 本身也是 Eureka Client，多个 Eureka Server 通过复制注册表的方法来完成服务注册表的同步从而达到集群的效果。</li>
</ol>
<h3 id="为什么选择-Eureka"><a href="#为什么选择-Eureka" class="headerlink" title="为什么选择 Eureka"></a>为什么选择 Eureka</h3><h4 id="1-它提供了完整的-Service-Registry-和-Service-Discovery-实现"><a href="#1-它提供了完整的-Service-Registry-和-Service-Discovery-实现" class="headerlink" title="1) 它提供了完整的 Service Registry 和 Service Discovery 实现"></a>1) 它提供了完整的 <code>Service Registry</code> 和 <code>Service Discovery</code> 实现</h4><p>首先是提供了完整的实现，并且也经受住了 Netflix 自己的生产环境考验，相对使用起来会比较省心。</p>
<h4 id="2-和-Spring-Cloud-无缝集成"><a href="#2-和-Spring-Cloud-无缝集成" class="headerlink" title="2) 和 Spring Cloud 无缝集成"></a>2) 和 <code>Spring Cloud</code> 无缝集成</h4><p>服务端和客户端都是 Java 编写的，针对微服务场景，并且和 Netflix 的其他开源项目以及 Spring Cloud 都有着非常好的整合，具备良好的生态。</p>
<h4 id="3-Open-Source"><a href="#3-Open-Source" class="headerlink" title="3) Open Source"></a>3) Open Source</h4><p>最后一点是开源，由于代码是开源的，所以非常便于我们了解它的实现原理和排查问题。</p>
<h3 id="Eureka-Server-使用介绍"><a href="#Eureka-Server-使用介绍" class="headerlink" title="Eureka Server 使用介绍"></a>Eureka Server 使用介绍</h3><h4 id="在-Spring-Boot-项目的-pom-xml-中加入-spring-cloud-starter-eureka-server"><a href="#在-Spring-Boot-项目的-pom-xml-中加入-spring-cloud-starter-eureka-server" class="headerlink" title="在 Spring Boot 项目的 pom.xml 中加入 spring-cloud-starter-eureka-server"></a>在 Spring Boot 项目的 pom.xml 中加入 <code>spring-cloud-starter-eureka-server</code></h4><p>使用 Spring Cloud 需要在 pom.xml 中加入 Spring Cloud 的父级引用，让 Spring 帮我们管理依赖版本。</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></div><div class="line">   <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">       <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">       <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka-server<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">   <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></div></pre></td></tr></table></figure>
<h4 id="在application-yml中配置"><a href="#在application-yml中配置" class="headerlink" title="在application.yml中配置"></a>在application.yml中配置</h4><p>Eureka 是一个高可用的组件，它没有后端缓存，每一个实例注册之后需要向注册中心发送心跳（因此可以在内存中完成），在默认情况下 Erureka Server 也是一个 Eureka Client，必须要指定一个 Server。</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="attr">server:</span></div><div class="line"><span class="attr">  port:</span> <span class="number">8761</span></div><div class="line"></div><div class="line"><span class="attr">eureka:</span></div><div class="line"><span class="attr">  instance:</span></div><div class="line"><span class="attr">    hostname:</span> <span class="string">localhost</span></div><div class="line"><span class="attr">  client:</span></div><div class="line"><span class="attr">    register-with-eureka:</span> <span class="literal">false</span>  <span class="comment"># 不用将自己注册到Eureka</span></div><div class="line"><span class="attr">    fetch-registry:</span> <span class="literal">false</span>  <span class="comment"># 不用发现Eureka中的服务</span></div><div class="line"><span class="attr">    service-url:</span></div><div class="line"><span class="attr">      default-zone:</span> <span class="attr">http://$&#123;eureka.instance.hostname&#125;:$&#123;server.port&#125;/eureka/</span></div></pre></td></tr></table></figure>
<h4 id="添加-Application-java-启动类-添加-EnableEurekaServer-注解"><a href="#添加-Application-java-启动类-添加-EnableEurekaServer-注解" class="headerlink" title="添加 Application.java 启动类 添加 @EnableEurekaServer 注解"></a>添加 Application.java 启动类 添加 <code>@EnableEurekaServer</code> 注解</h4><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="variable">@SpringBootApplication</span></div><div class="line"><span class="variable">@EnableEurekaServer</span></div><div class="line">public class Application &#123;</div><div class="line">    <span class="selector-tag">public</span> <span class="selector-tag">static</span> <span class="selector-tag">void</span> <span class="selector-tag">main</span>(String[] args) &#123;</div><div class="line">        <span class="selector-tag">SpringApplication</span><span class="selector-class">.run</span>(Application.class, args);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="Eureka-Client-使用介绍"><a href="#Eureka-Client-使用介绍" class="headerlink" title="Eureka Client 使用介绍"></a>Eureka Client 使用介绍</h3><h4 id="服务注册与服务发现都是使用-Eureka-Client，所以在-Spring-Boot-项目的-pom-xml-中加入"><a href="#服务注册与服务发现都是使用-Eureka-Client，所以在-Spring-Boot-项目的-pom-xml-中加入" class="headerlink" title="服务注册与服务发现都是使用 Eureka Client，所以在 Spring Boot 项目的 pom.xml 中加入"></a>服务注册与服务发现都是使用 Eureka Client，所以在 Spring Boot 项目的 pom.xml 中加入</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div></pre></td></tr></table></figure>
<h4 id="在-application-yml-中加入-Eureka-的-Server-配置"><a href="#在-application-yml-中加入-Eureka-的-Server-配置" class="headerlink" title="在 application.yml 中加入 Eureka 的 Server 配置"></a>在 application.yml 中加入 Eureka 的 Server 配置</h4><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">server</span>:</div><div class="line">  <span class="attribute">port</span>: <span class="number">8762</span></div><div class="line"></div><div class="line"><span class="attribute">spring</span>:</div><div class="line">  <span class="attribute">application</span>:</div><div class="line">    <span class="attribute">name</span>: service-hi</div><div class="line"></div><div class="line"><span class="attribute">eureka</span>:</div><div class="line">  <span class="attribute">client</span>:</div><div class="line">    <span class="attribute">serviceUrl</span>:</div><div class="line">      <span class="attribute">defaultZone</span>: <span class="attribute">http</span>:<span class="comment">//localhost:8761/eureka/</span></div></pre></td></tr></table></figure>
<p>需要指明 <code>spring.application.name</code>，这个很重要，这在以后的服务与服务之间相互调用一般都是根据这个 name。</p>
<p>启动上边两个程序后，访问 <code>http://localhost:8761/</code> 可以看到下边的页面，同时看到我们的 <code>service-hi</code> 也注册上来了。</p>
<p><img src="1.png" alt=""></p>
<p>在本地调试时出现了这样的问题，如上图所示，中间部分有一行红色大字 <strong>EMERGENCY! EUREKA MAY BE INCORRECTLY CLAIMING INSTANCES ARE UP WHEN THEY’RE NOT. RENEWALS ARE LESSER THAN THRESHOLD AND HENCE THE INSTANCES ARE NOT BEING EXPIRED JUST TO BE SAFE.</strong>，原因是 Eureka 的自我保护机制:</p>
<blockquote>
<p>Eureka Server 在运行期间，会统计心跳成功率在 15分钟 之内是否低于85%，如果出现低于的情况（在单机调试的时候很容易满足，实际在生产环境上通常是由于网络不稳定导致），Eureka Server 会将当前的实例注册信息保护起来，同时提示这个警告。至于为什么需要有这个自我保护机制，官方的解释是：Service 不是强一致的，所以会有部分情况下没发现新服务导致请求出错，对于Service 发现服务而言，宁可返回某服务 5分钟 之前在哪几个服务器上可用的信息，也不能因为暂时的网络故障而找不到可用的服务器，而不返回任何结果。</p>
</blockquote>
<h3 id="其他实现方式比较"><a href="#其他实现方式比较" class="headerlink" title="其他实现方式比较"></a>其他实现方式比较</h3><p>DNS 可以算是最为原始的服务发现系统，但是在服务变更较为频繁，即服务的动态性很强的时候，DNS 记录的传播速度可能会跟不上服务的变更速度，这将导致在一定的时间窗口内无法提供正确的服务位置信息，所以这种方案只适合在比较静态的环境中使用，不适用于微服务。</p>
<p>基于 ZooKeeper、Etcd 等分布式键值对存储服务来建立服务发现系统在现在看起来 也不是一种很好的方案，一方面是因为它们只能提供基本的数据存储功能，还需要在外围做大量的开发才能形成完整的服务发现方案。另一方面是因为它们都是强一致性系统，在集群发生分区时会优先保证一致性、放弃可用性，而服务发现方案更注重可用性，为了保证可用性可以选择最终一致性，这两方面原因共同导致了 ZooKeeper、Etcd 这类系统越来越远离服务发现方案的备选清单，像 SmartStack 这种依赖 ZooKeeper 的服务发现方案也逐渐发觉 ZooKeeper 成了它的薄弱环节。与 ZooKeeper、Etcd 或者依赖它们的方案不同，Eureka 是个专门为服务发现从零开始开发的项目，Eureka 以可用性为先，可以在多种故障期间保持服务发现和服务注册功能可用，虽然此时会存在一些数据错误，但是 Eureka 的设计原则是“<strong>存在少量的错误数据，总比完全不可用要好</strong>”，并且可以在故障恢复之后按最终一致性进行状态合并，清理掉错误数据。</p>
<p>Eureka 有个强大的对手 Consul。Consul 是 HashiCorp 公司的商业产品，它有一个开源的基础版本，这个版本在基本的服务发现功能之外，还提供了多数据中心部署能力，包括内存、存储使用情况在内的细粒度服务状态检测能力，和用于服务配置的键值对存储能力（这是一把双刃剑，使用它可以带来便捷，但是也意味着和 Consul 的较强耦合性），这几个能力 Eureka 目前都没有。但是 Consul 对业务的侵入性较大，在与 SpringBoot 项目对接时没有那么方便，而且 Consul 由一家商业软件公司提供，那么必然或多或少的存在商业软件的某些弊端。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;Eureka-是-Netflix-开源的一款提供服务注册和发现的产品。&quot;&gt;&lt;a href=&quot;#Eureka-是-Netflix-开源的一款提供服务注册和发现的产品。&quot; class=&quot;headerlink&quot; title=&quot;Eureka 是 Netflix 开源的一款
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>接入 sentry 时遇到的坑</title>
    <link href="https://jpanj.com/2017/%E6%8E%A5%E5%85%A5sentry%E6%97%B6%E9%81%87%E5%88%B0%E7%9A%84%E5%9D%91/"/>
    <id>https://jpanj.com/2017/接入sentry时遇到的坑/</id>
    <published>2017-06-20T03:29:07.000Z</published>
    <updated>2017-06-20T07:53:43.000Z</updated>
    
    <content type="html"><![CDATA[<p>将 进销存SaaS 接入 <a href="https://sentry.io" target="_blank" rel="external">Sentry</a>，但是接入后发现无法通过 ajax 来 <code>POST</code> 或者 <code>PUT</code> 数据， 会报：</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">title</span>&gt;</span>400 Bad Request<span class="tag">&lt;/<span class="name">title</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>Bad Request<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span>Failed to decode JSON object: No JSON object could be decoded<span class="tag">&lt;/<span class="name">p</span>&gt;</span></div></pre></td></tr></table></figure>
<p>解决办法是在 js 的 ajax 方法中加上：<code>contentType:&quot;application/json; charset=utf-8&quot;,</code>。</p>
<p><strong>注：只需要在提交的数据类型为 <code>Json</code> 的 <code>POST</code> 或 <code>PUT</code> 的方法上添加就行了，不用在提交 <code>Form 表单</code> 的地方添加，否则加上后 <code>Form 表单</code> 类型的 ajax 就无法提交了。</strong></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;将 进销存SaaS 接入 &lt;a href=&quot;https://sentry.io&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Sentry&lt;/a&gt;，但是接入后发现无法通过 ajax 来 &lt;code&gt;POST&lt;/code&gt; 或者 &lt;code&gt;PUT&lt;/cod
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>将博客部署到七牛</title>
    <link href="https://jpanj.com/2017/%E5%B0%86%E5%8D%9A%E5%AE%A2%E9%83%A8%E7%BD%B2%E5%88%B0%E4%B8%83%E7%89%9B/"/>
    <id>https://jpanj.com/2017/将博客部署到七牛/</id>
    <published>2017-06-16T03:38:47.000Z</published>
    <updated>2017-06-20T07:40:24.000Z</updated>
    
    <content type="html"><![CDATA[<p>我觉得我是不折腾就会死的人，现在的博客部署在 Github Pages 上，访问速度一直令我非常不爽，所以准备迁移到七牛。</p>
<p>七牛新建 bucket 后，在空间设置，把默认首页设置改成启动就完成了。</p>
<p>还有一个就是一定要绑定自己的独立域名，否则站内数据改变后，用七牛提供的临时域名来访问的话，缓存一时半会是不会刷新的，独立域名可以设置缓存刷新时间，前提是那个域名需要在国内进行备案。</p>
<p>缓存可以根据不同类型的数据有不同的策略，但我为了省事，直接将所有配置改为了 0，也就是不进行缓存，因为不是什么大流量站点，再加上七牛的 CDN 优化，所以即便不缓存速度也非常快，这样可以保证我每次在发布或者修改内容后能够及时更新。其实完全可以把非 HTML 类型的数据设置一些缓存时间。</p>
<p><img src="3.png" alt=""></p>
<p>可以<a href="http://blog.npyzdf.com/" target="_blank" rel="external">点这里</a>试一试部署在七牛上的速度，只是我之前的一个备案过的域名，但我没打算用这个域名来做我的国内博客地址，新地址正在备案中： <code>jpanj.com</code> 贾攀家。</p>
<p>接下来就是把博客生成出来的静态站按照目录结构完整上传到七牛就行了，但是这个工作非常麻烦，每次上传时如果有二级目录的话，需要自己填写前缀，而且每次生成后都需要自己登录到七牛上传一下。身为程序员的我，这不是在侮辱我的智商吗？所以我写了一个 Python 脚本，可以帮我自动完成这个工作。</p>
<p>我之前是使用的 hexo 的 github 插件来进行发布的，其实也非常简单，只需要执行: <code>hexo d -g</code> 即可完成生成静态站和部署的工作。（g=Generate static files. d=Deploy your website.），现在只需要在执行玩这个操作后，再执行下发布到七牛的脚本，就可以完成双发布了。</p>
<p>我是配合 <a href="https://www.alfredapp.com/" target="_blank" rel="external">Alfred</a> 来用的，如果没装 Alfred Workflow 的话，直接执行脚本也是可以的，把脚本简单修改下就行了。</p>
<p>上效果图：</p>
<p><img src="1.png" alt=""></p>
<p><img src="2.png" alt=""></p>
<p>代码已上传到 <a href="https://github.com/Panmax/qiniu-blog-deploy" target="_blank" rel="external">Github：https://github.com/Panmax/qiniu-blog-deploy</a></p>
<hr>
<h3 id="updateAt-2017-06-20"><a href="#updateAt-2017-06-20" class="headerlink" title="updateAt: 2017-06-20"></a>updateAt: 2017-06-20</h3><p>我把缓存策略改为了如下所示</p>
<p><img src="4.png" alt=""></p>
<p>图片和样式文件进行缓存，<code>html</code> 等文本文件不缓存。因为七牛会自动寻找 <code>index.html</code>，所以在真正访问时，不带 <code>/index.html</code> 后缀的页面也可以打开，所以我把全局配置设为了不缓存，也就是说不在这个配置中的文件也不进行缓存。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;我觉得我是不折腾就会死的人，现在的博客部署在 Github Pages 上，访问速度一直令我非常不爽，所以准备迁移到七牛。&lt;/p&gt;
&lt;p&gt;七牛新建 bucket 后，在空间设置，把默认首页设置改成启动就完成了。&lt;/p&gt;
&lt;p&gt;还有一个就是一定要绑定自己的独立域名，否则站内数
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>单点登录流程梳理</title>
    <link href="https://jpanj.com/2017/%E5%8D%95%E7%82%B9%E7%99%BB%E5%BD%95%E6%B5%81%E7%A8%8B%E6%A2%B3%E7%90%86/"/>
    <id>https://jpanj.com/2017/单点登录流程梳理/</id>
    <published>2017-06-15T01:47:02.000Z</published>
    <updated>2017-06-15T04:07:05.000Z</updated>
    
    <content type="html"><![CDATA[<p>之前研究了一段时间的单点登录系统，在这里做一下流程上的总结吧。</p>
<p>先说下我对几个词的认识：我觉得 <strong>统一认证、单点登录、集中认证、统一登录</strong> 这几个词的想表达的目的都是一样的，都是提供一个登录中心或者叫认证中心的地方，当某个系统需要用户进行登录时，统一跳转到这里来进行处理。</p>
<p>进入正文：</p>
<p>假定一个场景，现在有系统A(a.com)、系统B(b.com)、和认证中心(sso.com)。我们想实现的效果是，其中一个系统登录一次后，访问其他系统的需要登录页面时无需再次手动提交帐号密码。</p>
<p>注意：我这里说的是无需再次输入帐号密码，内部的登录流程还是要执行的，只是不需要用户的参与。</p>
<p>下边是基于 CAS 的 SSO 的流程介绍：</p>
<p>用户通过浏览器访问系统A <code>www.a.com/pageA</code>，这个 <code>pageA</code> 是个需要登录才能访问的页面，系统A发现用户没有登录，这时候系统A需要做一个额外的操作，就是重定向到认证中心: <code>www.sso.com/login?service=www.a.com/pageA</code>。</p>
<p>这个 <code>service</code> 参数的作用其实可以认为是一个回跳的 <code>url</code>，将来通过认证后，还要重定向到系统A，所以其实用 <code>redirect</code> 可能更合适一些，但是在这里还有一个作用就是注册服务，简单来说注册服务为的是让我们的认证中心能够知道有哪些系统在我们这里完成过登录，其中一个重要目的是为了完成单点退出的功能，单点退出的一会我再来介绍。</p>
<p>接下来浏览器会用 <code>www.sso.com/login?service=www.a.com/pageA</code> 访问认证中心，认证中心一看没登录过，就会展示一个登录框让用户去登录，登录成功以后，认证中心要做几件重要的事情：</p>
<ol>
<li>建立一个 <code>session</code></li>
<li>创建一个 <code>ticket</code> （可以认为是个随机字符串）</li>
<li>重定向到系统A，同时把 ticket 放在 url 中： <code>www.a.com/pageA?ticket=T123</code> 与此同时之前建立 session 对应的 cookie 也会发送给浏览器，比如： <code>Set cookie : ssoid=1234, sso.com</code></li>
</ol>
<p>到这里会产生一个疑惑，为什么认证中心要写一个 <code>cookie</code>，其他系统由于跨域的限制根本读不到它啊。</p>
<p>对于这个问题的回答是， <code>sso.com</code> 产生的 <code>cookie</code> 不是给其他系统用的（至于是给谁用的一会会说明），注意那个 <code>ticket</code>，这个东西是个重要标识，系统拿到以后需要再次向认证中心验证。所以 <code>ticket</code> 才是系统们要用到的东西。</p>
<p><img src="1.png" alt=""></p>
<p>系统A拿着这个 <code>ticket</code>，去问下认证中心：这是您签发的 <code>ticket</code> 吗，认证中心确认无误后，系统A就可以认为用户在认证中心登录过了。这时候系统A应该为这个用户建立 <code>session</code> 然后返回 <code>pageA</code> 的资源。也就是说，系统A也需要给浏览器发一个属于自己的 cookie：<code>Set cookie : sessionid=xxxx, a.com</code>。这时候浏览器实际上有两个 cookie，一个是系统A发的，一个是认证中心发的。</p>
<p>当用户再次访问系统A的另一个需要登录的页面时，因为系统A已经在浏览器中放入了自己的cookie，就知道它登录过了，不需要再次到认证中心去了。</p>
<p><img src="2.png" alt=""></p>
<p>接下来看看，当用户访问系统A时已经通过认证中心登录了，再访问系统B <code>www.b.com/pageB</code> 时是什么样的情况。</p>
<p>其实和首次访问 <code>www.a.com/pageA</code> 非常类似，<strong>唯一不同就是不需要用户输入用户名密码来登录了</strong>，因为浏览器已经有了认证中心的 cookie，直接发送给 <code>www.sso.com</code> 就可以了。这里解释了我上边提到的认证中心写入浏览器 cookie 的用途。</p>
<p><img src="3.png" alt=""></p>
<p>同样，认证中心会返回 <code>ticket</code>，系统B需要做验证。</p>
<p><img src="4.png" alt=""></p>
<p>整个流程的本质是一个认证中心的 <code>cookie</code>，加上多个子系统的 <code>cookie</code> 而已。</p>
<p>下边来说说单点退出的原理。</p>
<p>单点退出的作用是用户在一个地方退出，所有系统都要进行退出。这怎么来实现呢。还记得我前边提到的注册服务吗？没错，就是使用之前登录时给认证中心传的 <code>service</code> 参数，认证中心记录下来都有哪些系统进行过登录，当用户访问认证中心的 <code>/logout</code> 需要退出的时候，认证中心需要把自己的会话和 cookie 干掉，然后给之前注册过那些服务的地址发送退出登录的请求，默认是对根路径发一个 <code>POST</code> 请求，<code>Body</code> 中携带一些字段，比如比如之前登录时用到的<code>ticket</code>，这时候各个子系统根据传过来的这个 <code>ticket</code> 来将对应的用户 <code>session</code> 干掉即可。</p>
<p>所以用户在系统A点击退出登录后，系统A取消本地会话然后重定向到认证中心的退出登录地址，剩下的交给认证中心来处理就好了。这里也可以传一个回跳地址参数，当认证中心完成退出后，可以再跳会到设置的地址。</p>
<blockquote>
<p>我觉得用户在一个系统中退出登录时，系统此时结不结束会话其实都可以，因为最终还是要被认证中心调用一次退出。</p>
</blockquote>
<p>说一个我做单点退出时遇到的坑，由于我把 CAS Server 部署在了机房中的一台设备上，然后在我本地启了一个 WEB 服务，这个时候登录填的 <code>service</code> 是 <code>127.0.0.1:8081/xxx</code> 登录完之后的重定向是没有问题的，但是当我访问 CAS Logout 页面后，再访问我的系统，发现并没有退出登录，也没有访问退出登录的记录。原因是我注册服务时的 <code>127.0.0.1</code> 这个url认证中心根本访问不到。</p>
<p>后来我在本地起了一个 CAS Server 再次验证后没有问题。</p>
<p>还有人问道那个 <code>ticket</code> 如果被其他人截获了，岂不是就可以冒充我来登录了？并不会。</p>
<p>首先来说，默认情况下，CAS 要求子系统和它之间的通讯为 <code>https</code> ，再有就是这个 <code>ticket</code> 只有一次有效性，验证一次后即失效，而且有效期还很短，默认我记得只有5秒。最重要的是即便这个 <code>ticket</code> 被其他人获取了也没啥用，验证这个 <code>ticket</code> 时，还需要带上申请这个 <code>ticket</code> 时的 <code>url</code> 信息，而且认证中心鉴定 <code>ticket</code> 为真后也只是返回用户的用户名、认证时间等最基本的信息，由于子系统没有拿到这些信息，所以对于子系统来说，你还是没有登录的。</p>
<p>最后敬上官方的 CAS 协议流程图</p>
<p><img src="5.png" alt=""></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前研究了一段时间的单点登录系统，在这里做一下流程上的总结吧。&lt;/p&gt;
&lt;p&gt;先说下我对几个词的认识：我觉得 &lt;strong&gt;统一认证、单点登录、集中认证、统一登录&lt;/strong&gt; 这几个词的想表达的目的都是一样的，都是提供一个登录中心或者叫认证中心的地方，当某个系统需要
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>优化 blog 速度</title>
    <link href="https://jpanj.com/2017/%E4%BC%98%E5%8C%96blog%E9%80%9F%E5%BA%A6/"/>
    <id>https://jpanj.com/2017/优化blog速度/</id>
    <published>2017-06-02T07:28:42.000Z</published>
    <updated>2017-06-20T07:51:18.000Z</updated>
    
    <content type="html"><![CDATA[<p>之前已经做过对 Blog 静态资源的优化，但是都没有进行记录，今天又做了一个比较实用的优化，赶紧记一下。</p>
<p>我打开 Chrome 的调试器进入站点时，看到 Network 里有一条访问 <code>fonts.gstatic.com</code>，这个请求应该是访问的 谷歌 CDN，但是国内的访问速度是非常不友好的，由于我平时都是开着 Surge 所以没什么感觉。</p>
<p>这个资源是在由主题渲染成静态站点时插入进来的，因为我用的是自己修改的 <code>Next</code> 模板，所以在 <code>themes/next</code> 目录下查找带 <code>fonts.gstatic.com</code> 关键字的文件竟然没有找到，很是费解。后来我又看那条请求，发现它的 <code>referer</code> 是 <code>fonts.googleapis.com</code>，所以我又尝试用这个关键字进行查找，最后在 <code>themes/next/layout/_partials/head/external-fonts.swig</code> 找到了它。</p>
<figure class="highlight twig"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="xml"></span><span class="template-tag">&#123;% <span class="name"><span class="keyword">if</span></span> font_families !== '' %&#125;</span><span class="xml"></span></div><div class="line">  <span class="template-tag">&#123;% <span class="name"><span class="keyword">set</span></span> font_families += '&amp;subset=latin,latin-ext' %&#125;</span><span class="xml"></span></div><div class="line">  <span class="template-tag">&#123;% <span class="name"><span class="keyword">set</span></span> font_host = font_config.host | default('//fonts.googleapis.com') %&#125;</span><span class="xml"></span></div><div class="line">  <span class="tag">&lt;<span class="name">link</span> <span class="attr">href</span>=<span class="string">"</span></span><span class="template-variable">&#123;&#123; font_host &#125;&#125;</span><span class="xml"><span class="tag"><span class="string">/css?family=</span></span></span><span class="template-variable">&#123;&#123; font_families &#125;&#125;</span><span class="xml"><span class="tag"><span class="string">"</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span></div><div class="line"><span class="template-tag">&#123;% <span class="name"><span class="keyword">endif</span></span> %&#125;</span><span class="xml"></span></div></pre></td></tr></table></figure>
<p>然后我在网上找了找 <code>fonts.googleapis.com</code> 国内镜像，有两个用的比较多的：360网站卫士 和 中科大。但是一些地方写到 360网站卫士 提供的源不支持 HTTPS，虽然我的博客现在并不是 HTTPS 的，但保不齐以后我要改呢。所以我选择使用中科大镜像。只需用 <code>fonts.lug.ustc.edu.cn</code> 替代之即可。然后再 <code>hexo g</code> 重新生成下站点就可以了。</p>
<p>下边是几个常用的替代镜像：</p>
<ol>
<li>ajax.googleapis.com =&gt; ajax.lug.ustc.edu.cn</li>
<li>fonts.googleapis.com =&gt; fonts.lug.ustc.edu.cn</li>
<li>themes.googleusercontent.com =&gt; google-themes.lug.ustc.edu.cn</li>
</ol>
<hr>
<h3 id="updateAt-2017-06-20"><a href="#updateAt-2017-06-20" class="headerlink" title="updateAt: 2017-06-20"></a>updateAt: 2017-06-20</h3><p>然鹅，在我用了一段时间后，发现中科大的源速度我也不满意，最终将那个 <code>css</code> 文件包括 <code>css</code> 文件里边用到的 <code>ttf</code> 文件都下载到了本地，使用本地路径来路由，这样的话我部署到七牛后的速度比之前快了很多。</p>
<p><img src="1.png" alt=""></p>
<p>在 <code>themes/next/source/css</code> 下新建 <code>fontes</code> 目录，把 <code>https://fonts.proxy.ustclug.org/css?family=Lato:300,300italic,400,400italic,700,700italic&amp;subset=latin,latin-ext</code> 下载到 <code>fonts</code> 目录并重命名为 <code>fonts.css</code>，然后把里边的 url 对应的 ttf 文件也都下载到此目录，并修改 <code>fonts.css</code> 内的 url 地址为本地地址:</p>
<p><img src="2.png" alt=""></p>
<p>最后将之前修改的 <code>themes/next/layout/_partials/head/external-fonts.swig</code> 文件中的地址改为：<code>/css/fonts.css</code></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前已经做过对 Blog 静态资源的优化，但是都没有进行记录，今天又做了一个比较实用的优化，赶紧记一下。&lt;/p&gt;
&lt;p&gt;我打开 Chrome 的调试器进入站点时，看到 Network 里有一条访问 &lt;code&gt;fonts.gstatic.com&lt;/code&gt;，这个请求应该是
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>解决在 SAE 中使用 Flask-SQLAlchemy 出现 MySQL server has gone away 的问题</title>
    <link href="https://jpanj.com/2017/%E8%A7%A3%E5%86%B3%E5%9C%A8-SAE-%E4%B8%AD%E4%BD%BF%E7%94%A8-Flask-SQLAlchemy-%E5%87%BA%E7%8E%B0-MySQL-server-has-gone-away-%E7%9A%84%E9%97%AE%E9%A2%98/"/>
    <id>https://jpanj.com/2017/解决在-SAE-中使用-Flask-SQLAlchemy-出现-MySQL-server-has-gone-away-的问题/</id>
    <published>2017-05-31T09:01:32.000Z</published>
    <updated>2017-05-31T09:21:27.000Z</updated>
    
    <content type="html"><![CDATA[<p>这段时间尝试把 进销存SAAS 迁移到 新浪云（SAE），这样的话减少了运维的麻烦而且降低了成本，我现在暂时只用到了一个最基础的容器+一个最低配置的 <code>MySQL</code> 服务，每天的成本只有 2 块多。打算迁移完之后改成两个容器实例。</p>
<p>但是在迁移中发现一个问题，当过一会不访问服务后，再次访问时会出现 <code>MySQL server has gone away</code> 的错误，在 SAE 提供的数据库中用 <code>SHOW VARIABLES;</code> 检查了下，SAE 给数据库配置的 <code>wait_timeout</code> 是 300 秒，我之前阿里云上的数据库没有改配置，所以默认为 8 小时，而且 SAE 数据库 的这个值是不允许修改的，所以既然<strong>无法改变环境，就来适应环境吧</strong>。</p>
<p>尝试了很多解决办法，比如 配置<code>SQLALCHEMY_COMMIT_ON_TEARDOWN=True</code> 和 在每次请求完成后关闭 db 的 <code>session</code> 都没有解决问题，再次阅读文档时看到了这个配置：<code>SQLALCHEMY_POOL_RECYCLE</code>，作用是设置多少秒后回收连接，在使用 <code>MySQL</code> 时是必须设置的。如果不提供值，默认为 2 小时，而我之前的数据库默认 <code>wait_timeout</code> 为 8 小时，所以一直没有出过问题。我在我的配置文件中，将这个值设置为 280 秒（小于 300 秒），最终解决了问题。</p>
<p>最后帖一下<code>SQLALCHEMY_POOL_RECYCLE</code>参数的原文解释：</p>
<blockquote>
<p>Number of seconds after which a connection is automatically recycled. This is required for MySQL, which removes connections after 8 hours idle by default. Note that Flask-SQLAlchemy automatically sets this to 2 hours if MySQL is used.</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这段时间尝试把 进销存SAAS 迁移到 新浪云（SAE），这样的话减少了运维的麻烦而且降低了成本，我现在暂时只用到了一个最基础的容器+一个最低配置的 &lt;code&gt;MySQL&lt;/code&gt; 服务，每天的成本只有 2 块多。打算迁移完之后改成两个容器实例。&lt;/p&gt;
&lt;p&gt;但是在
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>SQLAlchemy 分表实践</title>
    <link href="https://jpanj.com/2017/SQLAlchemy-%E5%88%86%E8%A1%A8%E5%AE%9E%E8%B7%B5/"/>
    <id>https://jpanj.com/2017/SQLAlchemy-分表实践/</id>
    <published>2017-05-27T08:40:09.000Z</published>
    <updated>2017-05-31T09:02:03.000Z</updated>
    
    <content type="html"><![CDATA[<p>去年年底利用工作之余开发了一个进销存相关的 SAAS 项目，ORM 用的 <code>SQLAlchemy</code>，并且进行了一些分表操作，这里来做个简单的记录（也只能是简单记录了，我是小半年前进行的分表调研）。</p>
<p>我没有直接使用 继承自 <code>db.Model</code> 的 ORM 类来操作数据库，而是在其之上又封装了一层，将更具体的一些数据库操作进行了封装。</p>
<p>举个例子，我有一个继承自 <code>db.Model</code> 的 <code>Item</code> 类，同时还有一个自己的 <code>Item</code> 类，然后在自己的 <code>Item</code> 类中引用继承自 <code>db.Model</code> 的类，为了防止名称冲突，在引用时我会将继承自 <code>db.Model</code> 的类叫做 <code>SqlaItem</code>。</p>
<p>分表是如何实现的呢，我通过 <code>SQLAlchemy</code> 的 <code>automap_base()</code> 将数据库中所有表进行了映射，然后自己实现了分表函数，通过分表函数得到分表的名称，然后动态的拿到那个表所对应的 ORM。</p>
<p>直接看代码：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># -*- coding: utf-8 -*-</span></div><div class="line"></div><div class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> unicode_literals, absolute_import</div><div class="line"></div><div class="line"><span class="keyword">from</span> sqlalchemy <span class="keyword">import</span> create_engine</div><div class="line"><span class="keyword">from</span> sqlalchemy.ext.automap <span class="keyword">import</span> automap_base</div><div class="line"></div><div class="line"><span class="keyword">from</span> fuxi.config <span class="keyword">import</span> SQLALCHEMY_DATABASE_URI</div><div class="line"></div><div class="line">engine = create_engine(SQLALCHEMY_DATABASE_URI)</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">ab_cls</span><span class="params">()</span>:</span></div><div class="line">    ab_cls = automap_base()</div><div class="line">    ab_cls.prepare(engine, reflect=<span class="keyword">True</span>)</div><div class="line">    <span class="keyword">return</span> ab_cls</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">_get_ab_cls</span><span class="params">()</span>:</span></div><div class="line">    <span class="keyword">if</span> <span class="keyword">not</span> getattr(_get_ab_cls, <span class="string">'_ab_cls'</span>, <span class="keyword">None</span>):</div><div class="line">        _ab_cls = ab_cls()</div><div class="line">        _get_ab_cls._ab_cls = _ab_cls</div><div class="line">    <span class="keyword">return</span> _get_ab_cls._ab_cls</div><div class="line"></div><div class="line">ab_cls = _get_ab_cls()</div></pre></td></tr></table></figure>
<p>所有分了表的类，都要通过 ab_cls 来获取表映射出来的对象。</p>
<p>还是以 <code>Item</code> 为例，看一下我的相关代码</p>
<figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="variable">@classmethod</span></div><div class="line">def _get_db_branch_name_by_user(cls, user_id):</div><div class="line">   branch = str(user_id)[-<span class="number">1</span>]</div><div class="line">   return branch</div><div class="line"></div><div class="line"><span class="variable">@classmethod</span></div><div class="line">def _get_item_dao(cls, branch_name):</div><div class="line">   tablename = <span class="string">'item_%s'</span> % branch_name</div><div class="line">   dao = getattr(ab_cls.classes, tablename)</div><div class="line">   return dao</div><div class="line"></div><div class="line"><span class="variable">@classmethod</span></div><div class="line">def get_dao(cls, user_id):</div><div class="line">   branch = cls._get_db_branch_name_by_user(user_id)</div><div class="line">   return cls._get_item_dao(branch)</div></pre></td></tr></table></figure>
<p>我通过这几个方法实现了获取分表映射的功能，在具体使用时，可以直接用 <code>get_dao(user_id)</code> 获取表映射（我是通过用户ID的规则进行的分表）。</p>
<p>随便看一个操作：</p>
<figure class="highlight elixir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="variable">@classmethod</span></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">gets_by_name</span></span>(cls, user_id, name):</div><div class="line">   SqlaItem = cls.get_dao(user_id)</div><div class="line"></div><div class="line">   <span class="keyword">cond</span> = (SqlaItem.user_id == user_id)</div><div class="line">   <span class="keyword">cond</span> &amp;= (SqlaItem.status != ItemStatus.DELETE.value)</div><div class="line">   if <span class="symbol">name:</span></div><div class="line">       <span class="keyword">cond</span> &amp;= (SqlaItem.name == name)</div><div class="line">   query = db.session.query(SqlaItem).filter(<span class="keyword">cond</span>)</div><div class="line">   <span class="keyword">return</span> [cls.init_from_sqla(x) <span class="keyword">for</span> x <span class="keyword">in</span> query]</div></pre></td></tr></table></figure>
<p>我先通过 <code>get_dao(user_id)</code> 获取到这个用户数据所在的表的映射，然后就可进行各种 <code>CURD</code> 操作了。</p>
<p>也就是说，在我的项目中其实有两种获取 <code>SqlaXxxx</code> 的方法，如果没有分表，那么直接用继承自 <code>db.Model</code> 的类即可，如果是分了表的，就用动态映射出来的，所以后者实际上是不需要写继承自 <code>db.Model</code> 的类的，但是为了在初始化时生成所有表结构，我还是写了这些类，只不过这些类所对应的表都是分表中的第一张表，以 <code>Item</code> 为例</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Item</span><span class="params">(db.Model)</span>:</span></div><div class="line">    <span class="string">"""</span></div><div class="line">    商品</div><div class="line">    分表策略： 用户ID最后 1 位</div><div class="line">    """</div><div class="line">    __tablename__ = <span class="string">'item_1'</span></div><div class="line">    ...</div></pre></td></tr></table></figure>
<p>这样的话，我在执行 <code>create_db</code> 时所有需要分表的第一个表都会被建好，这个时候，我只需要再写个简单的脚本，就可以帮我把剩余的表建出来了，因为我所有分表结尾都是以下划线<code>1</code>或者<code>01</code>组成的，意思是，如果表需要分成 10 个，那么对应的第一个表的名称就是 <code>xxx_1</code>，如果需要分成 100 个，那么对应的第一个表的名称就是 <code>xxx_01</code>，所以我根据这个规则写了生成剩余表的脚本，如下：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div><div class="line">71</div><div class="line">72</div><div class="line">73</div><div class="line">74</div><div class="line">75</div><div class="line">76</div><div class="line">77</div><div class="line">78</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># -*- coding: utf-8 -*-</span></div><div class="line"></div><div class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> unicode_literals, absolute_import</div><div class="line"></div><div class="line"><span class="keyword">import</span> os</div><div class="line"><span class="keyword">from</span> urlparse <span class="keyword">import</span> urlparse</div><div class="line"></div><div class="line"><span class="keyword">import</span> MySQLdb</div><div class="line"></div><div class="line"><span class="keyword">from</span> envcfg.json.fuxi <span class="keyword">import</span> SQLALCHEMY_DATABASE_URI</div><div class="line"></div><div class="line">p = urlparse(SQLALCHEMY_DATABASE_URI)</div><div class="line"></div><div class="line">DATABASE_HOST = p.hostname</div><div class="line">DATABASE_USER = p.username</div><div class="line">DATABASE_PASSWD = p.password <span class="keyword">if</span> p.password <span class="keyword">else</span> <span class="string">""</span></div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">_get_table_name</span><span class="params">(table)</span>:</span></div><div class="line">    table_name_start = table.find(<span class="string">'`'</span>) + <span class="number">1</span></div><div class="line">    table_name_end = table.find(<span class="string">'`'</span>, table_name_start)</div><div class="line">    <span class="keyword">return</span> table[table_name_start:table_name_end]</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_table</span><span class="params">(table)</span>:</span></div><div class="line">    <span class="string">"""</span></div><div class="line">    首先根据表名确定当前表是否需要分表</div><div class="line">    如果表名是已1结尾，那么就创建0-9结尾的表</div><div class="line">    如果表名是已01结尾，那么就创建00-99结尾的表</div><div class="line">    如果以上情况都不是，直接创建</div><div class="line">    """</div><div class="line">    db = MySQLdb.connect(DATABASE_HOST, DATABASE_USER, DATABASE_PASSWD, <span class="string">"fuxi"</span>)</div><div class="line">    cursor = db.cursor()</div><div class="line"></div><div class="line">    <span class="keyword">assert</span> table.startswith(<span class="string">'CREATE TABLE'</span>)</div><div class="line">    table_name = _get_table_name(table)</div><div class="line">    <span class="keyword">if</span> table_name.endswith(<span class="string">'01'</span>):</div><div class="line">        replace_table = table.replace(table_name, <span class="string">'%s'</span>)</div><div class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">100</span>):</div><div class="line">            sharding_table_name = <span class="string">'%s_%02d'</span> % (table_name[:<span class="number">-3</span>], i)</div><div class="line">            <span class="keyword">try</span>:</div><div class="line">                cursor.execute(replace_table % sharding_table_name)</div><div class="line">                <span class="keyword">print</span> <span class="string">'%s create success'</span> % sharding_table_name</div><div class="line">            <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</div><div class="line">                <span class="keyword">if</span> e.args[<span class="number">0</span>] != <span class="number">1050</span>:</div><div class="line">                    <span class="keyword">raise</span> e</div><div class="line">                <span class="keyword">print</span> <span class="string">'%s exsit'</span> % sharding_table_name</div><div class="line">    <span class="keyword">elif</span> table_name.endswith(<span class="string">'1'</span>):</div><div class="line">        replace_table = table.replace(table_name, <span class="string">'%s'</span>)</div><div class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</div><div class="line">            sharding_table_name = <span class="string">'%s_%s'</span> % (table_name[:<span class="number">-2</span>], i)</div><div class="line">            <span class="keyword">try</span>:</div><div class="line">                cursor.execute(replace_table % sharding_table_name)</div><div class="line">                <span class="keyword">print</span> <span class="string">'%s create success'</span> % sharding_table_name</div><div class="line">            <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</div><div class="line">                <span class="keyword">if</span> e.args[<span class="number">0</span>] != <span class="number">1050</span>:</div><div class="line">                    <span class="keyword">raise</span> e</div><div class="line">                <span class="keyword">print</span> <span class="string">'%s exsit'</span> % sharding_table_name</div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        <span class="keyword">try</span>:</div><div class="line">            cursor.execute(table)</div><div class="line">            <span class="keyword">print</span> <span class="string">'%s create success'</span> % table_name</div><div class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</div><div class="line">            <span class="keyword">if</span> e.args[<span class="number">0</span>] != <span class="number">1050</span>:</div><div class="line">                <span class="keyword">raise</span> e</div><div class="line">            <span class="keyword">print</span> <span class="string">'%s exsit'</span> % table_name</div><div class="line">    db.close()</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">gets_all_tables</span><span class="params">()</span>:</span></div><div class="line">    <span class="keyword">with</span> open(os.path.dirname(os.path.realpath(__file__)) + <span class="string">'/fuxi.sql'</span>, <span class="string">'r'</span>) <span class="keyword">as</span> f:</div><div class="line">        content = f.read()</div><div class="line">    tables = content.split(<span class="string">'\n\n'</span>)</div><div class="line">    <span class="keyword">for</span> table <span class="keyword">in</span> tables:</div><div class="line">        create_table(table)</div><div class="line"></div><div class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</div><div class="line">    gets_all_tables()</div></pre></td></tr></table></figure>
<p>这种方式有个弊端，在项目启动时就需要将所有表结构读入到内存中，直接的表现是启动比较慢，占用内存比较多。</p>
<p>我不觉得这是个最佳方案，所以如果有更好的方案或者有任何疑问请通过邮件(jiapan.china#gmail.com)的方式告诉我，谢谢。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;去年年底利用工作之余开发了一个进销存相关的 SAAS 项目，ORM 用的 &lt;code&gt;SQLAlchemy&lt;/code&gt;，并且进行了一些分表操作，这里来做个简单的记录（也只能是简单记录了，我是小半年前进行的分表调研）。&lt;/p&gt;
&lt;p&gt;我没有直接使用 继承自 &lt;code&gt;db
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>对 Python 列表常见操作的理解</title>
    <link href="https://jpanj.com/2017/%E5%AF%B9%20Python%20%E5%88%97%E8%A1%A8%E5%B8%B8%E8%A7%81%E6%93%8D%E4%BD%9C%E7%9A%84%E7%90%86%E8%A7%A3/"/>
    <id>https://jpanj.com/2017/对 Python 列表常见操作的理解/</id>
    <published>2017-05-27T02:52:10.000Z</published>
    <updated>2017-05-27T03:30:39.000Z</updated>
    
    <content type="html"><![CDATA[<p>写这篇文章的原因是前几天做了一道面试题，问题大致是这样的：</p>
<figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">l = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</div><div class="line"></div><div class="line">l.insert(<span class="number">1</span>, <span class="number">6</span>)</div><div class="line">和</div><div class="line">l.append(<span class="number">6</span>)</div><div class="line"> </div><div class="line">的时间复杂度分表是什么</div></pre></td></tr></table></figure>
<p>我理所应当的认为，Python 列表的内部实现应该是一个链表，而链表的插入和追加操作应该都是 <code>O(1)</code>，但今天看到一篇文章<a href="http://python.jobbole.com/82549/" target="_blank" rel="external">原文地址</a>，发现并不是我认为的那样。</p>
<p>Python 在 C 实现中，存储数据的部分是一块连续的内存数组，不过这个数组里存放的也是指针，指向具体的元素，并且会在 结构体 中记录元素的实际个数，结构体如下。</p>
<figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">typedef struct &#123;</div><div class="line">    <span class="comment"># 列表元素的长度</span></div><div class="line">    int ob_size<span class="comment">; </span></div><div class="line">    <span class="comment"># 真正存放列表元素容器的指针，list[0] 就是 ob_item[0]</span></div><div class="line">    PyObject **ob_item<span class="comment">;</span></div><div class="line">    <span class="comment"># 当前列表可容纳的元素大小</span></div><div class="line">    Py_ssize_t allocated<span class="comment">;</span></div><div class="line">&#125; PyListObject<span class="comment">;</span></div></pre></td></tr></table></figure>
<p>当追加新元素的时候，可以直接通过 <code>ob_item[ob_size]=n</code> 来完成，所以时间复杂度为 <code>O(1)</code>。</p>
<p>在插入元素时，操作如下，将要插入位置下方的所有元素向下移动一个位置，然后将要插入位置指向我们插入的元素即可。所以时间复杂度其实是 <code>O(n)</code>。</p>
<p>（以上都没有考虑 <code>allocated</code> 分配的情况）</p>
<p>再来看下 <code>pop</code> 这个操作，<code>pop</code> 时只需将 <code>ob_size</code> 减一即可，所以时间复杂的也是 <code>O(1)</code>。</p>
<p><code>remove</code> 就没这么简单了，需要先通过遍历的方式找到要移除的元素，然后将找到的位置到最后一个有效位置这之间的指针都指向其 <code>next</code> 指向的元素（也就是 <code>ob_item[i]=ob_item[i+1]</code>），然后 <code>ob_size-1</code>，所以时间复杂度也为 <code>O(n)</code></p>
<p>这就是我对 <code>list</code> 几个常见操作的理解，如有错误之处请通过邮件方式指出: <code>jiapan.china#gmail.com</code></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;写这篇文章的原因是前几天做了一道面试题，问题大致是这样的：&lt;/p&gt;
&lt;figure class=&quot;highlight lsl&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;l
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>使用 KM 处理 HHKB 方向键</title>
    <link href="https://jpanj.com/2017/%E4%BD%BF%E7%94%A8-KM-%E5%A4%84%E7%90%86-HHKB-%E6%96%B9%E5%90%91%E9%94%AE/"/>
    <id>https://jpanj.com/2017/使用-KM-处理-HHKB-方向键/</id>
    <published>2017-05-21T00:53:02.000Z</published>
    <updated>2017-06-14T07:03:46.000Z</updated>
    
    <content type="html"><![CDATA[<p>对于上了 HHKB 这条贼船的人来说，刚开始使用起来最大的别扭可能就是没有方向键的问题了。</p>
<p>最早的我使用 <a href="https://pqrs.org/osx/karabiner/" target="_blank" rel="external">Karabiner</a> 来解决，里边有一些内置的组合可以替代方向键，我用 <code>control + hjkl</code>（同vi） 替代四个方向键，因为 HHKB 的 control 在 caps lock 的位置，所以使用起来还是很舒服的，But 当系统升级到 macOS Sierra 后，Karabiner 就不能工作了，作者也在官网中写了：</p>
<blockquote>
<p>Karabiner does not work on macOS Sierra at the moment.</p>
</blockquote>
<p>同时也给出了替代方案，使用 Karabiner-Elements，但是新版的 Karabiner 并不支持这样的组合，所以我就又走上了寻找解决方向键之路。</p>
<p>后来找到了 Keyboard Maestro（简称 KM） 这个神器，这个软功能非常多，不过我只用了里边的设置组合键的功能，我自定义了 5 个组合，用来解决 HHKB 中的不方便的方向键问题。</p>
<p>分别是 <code>control + hjkl</code> 来操作方向和 <code>control + delete</code> 来反向删除（也就是删除光标后边的内容），但是用起来有些问题：不能连击（比如按住 <code>control + h</code> 光标不可以一直前移，需要手动敲击多次），然鹅就在我将就用了小一年后，今天尝试将触发方式 <code>is pressed</code> 改成了 <code>is down</code>，成功解决了不能连击的问题，所以 <strong>HHKB 方向键</strong> 的问题现在可以说是完美解决了。</p>
<p>不过我现在并没有将 Karabiner-Elements 删掉，因为里边有一个比较实用的功能：可以在插入外置键盘时禁用内置键盘的功能，防止意外点击。因为我之前是把 HHKB 垫到 Mac 上用的，经常不小心按住了某个键，现在我把 Mac 放在了支架上，已经不会再出现这个问题了，不过我还是留下了它。</p>
<p>最后上一张设置截图（我将 KM 中自带的其他组合全都关闭了，只留下 5 个我自己写的组合）： </p>
<p><img src="1.png" alt=""></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;对于上了 HHKB 这条贼船的人来说，刚开始使用起来最大的别扭可能就是没有方向键的问题了。&lt;/p&gt;
&lt;p&gt;最早的我使用 &lt;a href=&quot;https://pqrs.org/osx/karabiner/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Kara
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>mycli fzf thefuck</title>
    <link href="https://jpanj.com/2017/mycli-fzf-thefuck/"/>
    <id>https://jpanj.com/2017/mycli-fzf-thefuck/</id>
    <published>2017-05-05T07:15:12.000Z</published>
    <updated>2017-06-16T02:56:16.000Z</updated>
    
    <content type="html"><![CDATA[<p>今天装了3个命令行下的神器，分别是 <code>mycli</code> <code>fzf</code> <code>thefuck</code>，都是通过 <code>Homebrew</code> 装的。</p>
<p><code>thefuck</code> 装完后在 <code>.zshrc</code> 的 plugin 中配上了插件，这样的话用起来就更方便了，当输错命令或者需要 root 权限却没加 <code>sudo</code> 时，只需要双击 esc 就可以了。</p>
<p><img src="2.png" alt=""></p>
<p><code>mycli</code> 是一个支持语法高亮和命令补全 mysql 客户端，类似于 ipython。安装过程比较长，主要是中间安装 <code>Pyhton 2.7.13</code> 占用了很长时间。装完后直接 <code>mycli -uroot</code> 就进入数据库的交互状态了。有一个地方不太习惯，没执行一个命令出来结果后，需要再按一下 <code>q</code> 才能返回交互状态。</p>
<p><img src="1.png" alt=""></p>
<p><code>fzf</code> 是命令行下模糊搜索工具。通过 <code>brew install fzf</code> 安装完后，还需要执行 <code>/usr/local/opt/fzf/install</code> 安装 shell 扩展，之后 <code>Control+r</code> 时出来的就不是之前那种很简单的历史命令搜索结果了，而是交互性很棒的结果。输入 <code>kill -9 + &lt;TAB&gt;</code> 能通过模糊搜索的方式搜到需要杀掉的进程，再也不用先 <code>ps -ef | grep xxx</code> 找到对应的进程然后在执行 <code>kill</code> 或者 通过管道 + <code>xargs</code> 的方式来杀进程了。</p>
<p><img src="3.png" alt=""></p>
<p><code>fzf</code> 还有几种用法我感觉没多少用： </p>
<figure class="highlight vim"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">cd</span> **<span class="symbol">&lt;TAB&gt;</span></div><div class="line"><span class="keyword">vim</span> **<span class="symbol">&lt;TAB&gt;</span></div><div class="line">ssh **<span class="symbol">&lt;TAB&gt;</span></div><div class="line">telnet **<span class="symbol">&lt;TAB&gt;</span></div><div class="line">unset **<span class="symbol">&lt;TAB&gt;</span></div><div class="line">export **<span class="symbol">&lt;TAB&gt;</span></div><div class="line">unalias **<span class="symbol">&lt;TAB&gt;</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;今天装了3个命令行下的神器，分别是 &lt;code&gt;mycli&lt;/code&gt; &lt;code&gt;fzf&lt;/code&gt; &lt;code&gt;thefuck&lt;/code&gt;，都是通过 &lt;code&gt;Homebrew&lt;/code&gt; 装的。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;thefuck&lt;/code&gt; 装完后
    
    </summary>
    
    
  </entry>
  
</feed>
